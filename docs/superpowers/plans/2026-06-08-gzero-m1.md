# Gravidade Zero — O Jogo · Plano M1 v2 (5 personagens + Humanware + inimigos)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Passos `- [ ]`.

**Goal:** Sobre o M0, adicionar selecao de personagem, 5 personagens com habilidade, Humanware (medidor+Modo), inimigos Tolo (patrulha/stomp/dano) e dano/coracoes/vidas/over.

**Architecture:** Estende o motor M0. Modulos novos de DONO UNICO: ability.ts, humanware.ts, enemy.ts, selectScreen.ts; integracao concentrada em game.ts (1 dono). Interfaces internas FIXADAS no contrato abaixo.

**Tech Stack:** Vite, TypeScript strict, Canvas 2D, Vitest, Playwright.

> Plano 2 (M1). Spec: docs/superpowers/specs/2026-06-08-gravidade-zero-game-design.md (§0 canonico).

---

## ⚠️ Errata M1 — Correções Canônicas (LER PRIMEIRO; prevalece sobre as tarefas)

A revisão confirmou **dono único OK** e **sem placeholders**; restaram correções pontuais. **Onde uma Task divergir, a errata vence.**

### E1 — (HIGH) Caminho de import da selectScreen (Task 8)
A `selectScreen.ts` é criada em **`src/ui/selectScreen.ts`** (Task 7). Em `game.ts` (Task 8) importe de **`../ui/selectScreen`** (NÃO `./selectScreen`): `import { createSelect, updateSelect, drawSelect, type SelectState } from '../ui/selectScreen'`. Corrija também a lista de "consumo" da Task 8 para `src/ui/selectScreen.ts`.

### E2 — (HIGH) Chamar respawnPlayer ao perder vida (Task 8)
`damagePlayer` retorna `'hit'` tanto para perda simples de coração quanto para perda de vida (já resetou hearts). O `game.ts` DEVE reposicionar no spawn ao perder vida. Importe `respawnPlayer` de `./player` e no laço de colisão:
```ts
const livesBefore = p.lives
const result = damagePlayer(p, e.x)
if (result === 'death') state.set('over')
else if (result === 'hit' && p.lives < livesBefore) respawnPlayer(p, level.playerSpawn)
```
Adicione no teste de integração da Task 8: ao perder uma vida com `lives>0`, o player volta a `level.playerSpawn`.

### E3 — (MED) Parâmetro `ctx` não usado em updateAbility (Task 3)
`tsconfig` tem `noUnusedParameters`. Mantenha a assinatura canônica `updateAbility(player,input,dt,ctx)` mas adicione `void ctx` no início do corpo (ou referencie `ctx.level`/`ctx.enemies`) para não quebrar o `tsc`.

### E4 — (MED) Gate de aceite das tarefas que dependem de módulos ainda não criados
Durante uma tarefa isolada (ex. Task 3 ability.ts, que tipa contra Player.ability/Enemy/ABILITY_PARAMS de outras tarefas), o **`tsc --noEmit` do projeto inteiro pode acusar módulos ainda ausentes** — isso é esperado, não é falha da tarefa. Critério de PASS por tarefa = **`npm run test`** (Vitest isolado) verde para os testes daquela tarefa. O **`npx tsc -p tsconfig.json --noEmit` + `npm run build` do projeto inteiro** são o gate **da Task 10 (verificação final)**, quando todos os módulos já existem. (Ordem de execução das tasks: 1→2→5→6→3→4→7→8→9→10, para que cada tarefa encontre suas dependências; mas o ownership garante que qualquer ordem converge no final.)

### E5 — (MED) Remover teste-stub morto na Task 8
Remova o teste "KeyH com medidor cheio... CONGELA" que contém `void`/`break` e só afirma `state==='playing'` (no-op). Mantenha o teste **real** de congelamento (encher o medidor via moedas/stomp → ativar Modo → assertar `e.frozen===true` e `e.x` imóvel por N updates).

### E6 — (LOW) Cor do inimigo congelado (Task 5)
Em `drawEnemy`, use `COLOR_TEXT` importado de `../engine/constants` para o tom "frozen" (não o literal `'#f7f3f6'`).

> LOW aceitos (sem ação): o bloco do Builder afeta a colisão do player a partir do frame seguinte (à frente, não prende) — ok; e o teste do HUD deve contar corações pela **quantidade esperada**, não só pela cor, para robustez.

---

## Contrato Tecnico do M1 (API M0 + interfaces internas fixadas + §0)

CONTRATO M1 v2 (estende o M0). Cada arquivo tem UM DONO. Modulos compartilhados sao CRIADOS por uma unica tarefa com a API COMPLETA abaixo; outras tarefas apenas CONSOMEM (import). NUNCA recriar um arquivo de outra tarefa. Use os nomes/assinaturas EXATOS abaixo e os valores canonicos §0.

ALVO M1: o jogo M0 jogavel ganha (1) tela de SELECAO; (2) 5 personagens com habilidade unica; (3) HUMANWARE (medidor+Modo); (4) inimigos "Tolo" (patrulha/stomp/dano); (5) dano/coracoes/i-frames/morte/estado 'over'. Arte = placeholder em codigo. Vite+TS, Canvas 960x528, Vitest+Playwright.

=== API EXISTENTE M0 (NAO QUEBRAR) ===
constants.ts: TILE48 VIEW_W960 VIEW_H528 FIXED_DT=1/60 MAX_SUBSTEPS5 GRAVITY0.8 MAX_FALL17 WALK_ACCEL0.7 RUN_ACCEL0.95 WALK_MAX4.6 RUN_MAX7.3 GROUND_DECEL0.6 AIR_DECEL0.18 JUMP_VEL-15.4 COYOTE_FRAMES7 JUMP_BUFFER_FRAMES8 STOMP_BOUNCE-11.5 ENEMY_SPEED1.25 PLAYER_W34 PLAYER_H42 COIN_SIZE26 TIME_START250 START_LIVES3 HW_TIME_SCALE0.35 HW_METER_MAX1000 + COLOR_BG#09090b COLOR_SURFACE#121216 COLOR_INK#050505 COLOR_MAGENTA#ff0055 COLOR_MAGENTA2#e802cf COLOR_BLUE#0099ff COLOR_LIME#cdf140 COLOR_VIOLET#7112ff COLOR_TEXT#f7f3f6.
schema.ts: TileType='empty'|'ground'|'brick'|'platform'|'block'|'spike'|'goal'; AbilityId='salto_visionario'|'dash_criativo'|'escudo_governanca'|'builder'|'emc2'|'amplificador'; CharacterDef{id,name,abilityId,color,accent,hearts,jumpVelMul,walkMul,runMul,weightMul}; LevelDef{id,world,zone,rows}; SpawnPoint{x,y}; ParsedLevel{widthTiles,heightTiles,widthPx,heightPx,tiles:TileType[][],playerSpawn,goal,coins:SpawnPoint[],enemies:Array<{x,y,kind:string}>}.
state.ts: GameState 'loading'|'title'|'select'|'playing'|'paused'|'win'|'over'; createStateMachine(initial).
loop.ts: createLoop(step:(dt)=>void,render:(alpha)=>void,now?) — step recebe 1 por passo fixo.
input.ts: InputAction (M0)='left'|'right'|'jump'|'run'|'down'; interface Input{isDown(a),pressed(a),update(),attach(target),detach(target)} (detach existe via InputWithDetach retornado por createInput); createInput(). Rastreia e.code (teclas fisicas). Testes usam window.dispatchEvent(new KeyboardEvent('keydown',{code})). SCROLLING_KEYS={Space,ArrowLeft,ArrowRight,ArrowUp,ArrowDown}.
render.ts: Renderer{ctx,clear(color),beginWorld(camX,camY),endWorld(),drawRect(x,y,w,h,color),present()}; createRenderer(canvas).
physics.ts: Body{x,y,w,h,vx,vy,onGround}; stepBody(body,level,dt); collideTiles(body,level). Solidos=ground/brick/block; platform one-way.
camera.ts: Camera{x,y}; createCamera(); followCamera(cam,target,level).
player.ts (M0): Player extends Body{char,facing:1|-1,coyote,jumpBuffer,lives,hearts}; createPlayer(char,spawn); updatePlayer(player,input,level,dt) (input->accel, pulo coyote/buffer vy=JUMP_VEL*char.jumpVelMul, depois stepBody; NAO chama collideTiles depois). FakeInput canonico em player.test.ts implementa Input (attach() no-op, pressed-edge via prev set).
characters.ts (M0): CHARACTERS (so renan); DEFAULT_CHARACTER_ID='renan'.
levelParser.ts: parseLevel(def). Legenda: # ground,B brick,= platform,? block,^ spike,o coin,* star,F fool,g enemy,L lifecard,S spawn,G goal. F->enemies kind'fool'; g->kind'enemy'. data/levels/world1-zona1.ts: world1Zona1 (40x11 plano, chao rows 9-10 todo '#', spawn col2 row8, goal col36 row8).
sprites.ts: drawPlaceholder(r,char,x,y,w,h,facing).
goal.ts: checkGoal(player,level).
game.ts (M0): createGame(renderer,input,level):Game{update(dt),render(alpha),state,player}; comeca 'playing'; update: updatePlayer->followCamera->time=Math.max(0,time-dt*FIXED_DT)->coin pickup->checkGoal->'win'; input.update() POR ULTIMO. render: clear,beginWorld,tiles+goal+coins+player,endWorld,drawHud,win overlay.
hud.ts: drawHud(r,{time,lives,coins,hwMeter}) screen space.
main.ts: canvas,renderer,input.attach(window),parseLevel(world1Zona1),createGame,createLoop(...).start(); window.__GAME_STATE=()=>game.state.get().
Testes: tests/unit/*.test.ts (Vitest jsdom), tests/e2e/world1.spec.ts (Playwright webServer=npm run build&&npm run preview:4173). Comandos: npm run test | npm run test:e2e | npx tsc -p tsconfig.json --noEmit | npm run build.

=== CANONE §0 (valores fixos) ===
§0.2 HUMANWARE: METER_MAX=1000 (4 seg de 250). Ganhos: moeda=8, stomp=60, reparar zona=250, lifecard=500, heart-orb=25. Modo: duracao=300f, cooldown=300f, mundo desacelera para HW_TIME_SCALE=0.35 (player escala 1.0), tolos CONGELAM, timer da fase PAUSADO no Modo. slow-mo = dt continuo escalado. Modo NAO da invuln total.
§0.4 STATS: Renan jumpVelMul1.06/walk1/run1/weight0.96/hearts3/salto_visionario; Dante 1/1.06/1.08/1/3/dash_criativo; Julio 1/1/1/1.08/4/escudo_governanca; Artur 1/1/1/1/3/builder; Einstein 0.94/0.92/1/1/2/emc2.
§0.5 hearts=HP da fase; lives=global (START_LIVES3,max9); hearts0->lives-1+respawn hearts cheios; lives0->'over'; i-frames=90f.
§0.6 time-scale: Math.min (nunca multiplicar). emc2 pessoal=0.25; com Humanware: min(0.35,0.25)=0.25.
§0.9 amplificador (Renante) = M3+; NAO implementar (so existe a chave no enum/params com flag desligada).

=== INTERFACES INTERNAS M1 (CANONICAS — use EXATAMENTE; nao invente variantes) ===
[chars] characters.ts ganha os 5 e os params. Cores canonicas: renan COLOR_MAGENTA/COLOR_BLUE; dante COLOR_BLUE/COLOR_LIME; julio COLOR_VIOLET/COLOR_MAGENTA; artur COLOR_LIME/COLOR_BLUE; einstein COLOR_TEXT/COLOR_MAGENTA2.
  interface AbilityParams { kind: AbilityId; cooldown: number; m1Implemented: boolean; maxAirJumps?: number; airJumpMul?: number; dashSpeed?: number; dashFrames?: number; dashIFrames?: number; shieldStamina?: number; builderTtl?: number; einsteinScale?: number; einsteinDuration?: number }
  export const ABILITY_PARAMS: Record<AbilityId, AbilityParams> = {
    salto_visionario:{kind:'salto_visionario',cooldown:0,m1Implemented:true,maxAirJumps:1,airJumpMul:0.92},
    dash_criativo:{kind:'dash_criativo',cooldown:24,m1Implemented:true,dashSpeed:12.0,dashFrames:12,dashIFrames:16},
    escudo_governanca:{kind:'escudo_governanca',cooldown:240,m1Implemented:true,shieldStamina:180},
    builder:{kind:'builder',cooldown:90,m1Implemented:true,builderTtl:240},
    emc2:{kind:'emc2',cooldown:300,m1Implemented:true,einsteinScale:0.25,einsteinDuration:210},
    amplificador:{kind:'amplificador',cooldown:0,m1Implemented:false} }
[input] InputAction passa a incluir 'ability'|'humanware'|'pause'|'confirm'. KEY_MAP add: KeyJ->ability, KeyH->humanware, Escape->pause, Enter->confirm. SCROLLING_KEYS inalterado. (Uma unica tarefa muda input.ts.)
[ability] ability.ts (UM DONO cria TUDO):
  interface AbilityState { id: AbilityId; cooldown: number; active: boolean; timer: number; airJumps: number; shield: boolean; shieldTimer: number; builder: { col: number; row: number; ttl: number } | null; einsteinTimer: number }
  createAbilityState(char: CharacterDef): AbilityState   // zera tudo; id=char.abilityId
  interface AbilityCtx { level: ParsedLevel; enemies: Enemy[] }   // (Enemy importado de enemy.ts)
  updateAbility(player: Player, input: Input, dt: number, ctx: AbilityCtx): void
    // chamada no game APOS updatePlayer, no tempo do PLAYER (dt do player, escala 1). Despacha por player.ability.id. Decrementa cooldown/shieldTimer/einsteinTimer/builder.ttl. Em input.pressed('ability') e cooldown<=0, executa a habilidade do personagem:
    //  salto_visionario: se !onGround e airJumps<maxAirJumps -> vy=JUMP_VEL*char.jumpVelMul*airJumpMul; airJumps++. (airJumps zera quando onGround — resetar no inicio de updateAbility se player.onGround.)
    //  dash_criativo: active=true,timer=dashFrames; player.vx=facing*dashSpeed; durante timer>0 mantem vx e concede i-frames (via abilityIFramesOnHit). cooldown=cooldown(24) ao terminar. Mata tolos no contato durante o dash (o game chama abilityKillsEnemy).
    //  escudo_governanca: shield=true; shieldTimer=shieldStamina(180); cooldown=240. Enquanto shield e shieldTimer>0, o proximo dano e absorvido.
    //  builder: builder={col,row,ttl:builderTtl(240)} na celula a frente (facing) na linha dos pes; cooldown=90; some quando ttl<=0.
    //  emc2: einsteinTimer=einsteinDuration(210); cooldown=300.
  // helpers exportados (consumidos pelo game/player):
  abilityWorldScale(player: Player): number   // emc2 ativo (einsteinTimer>0) ? einsteinScale(0.25) : 1
  abilityHasShield(player: Player): boolean    // shield && shieldTimer>0
  abilityConsumeShield(player: Player): void   // shield=false; shieldTimer=0
  abilityIFramesOnHit(player: Player): boolean // true se dash ativo (concede i-frames)
  abilityKillsEnemy(player: Player): boolean   // true se dash ativo (mata tolo por contato)
  abilityBuilderTile(player: Player): {col:number;row:number}|null // bloco temporario solido atual (ou null)
  drawAbilityFx(r: Renderer, player: Player): void // aura do escudo + bloco do builder (world space)
  NOTA bloco do Builder: a fisica deve tratar abilityBuilderTile como SOLIDO. Mecanismo canonico: o game, antes de mover o player/inimigos, escreve o tile temporario em level.tiles[row][col]='block' se a celula era 'empty', e o restaura para 'empty' quando some/expira. (Documente e implemente exatamente assim — sem segundo sistema de colisao.)
[enemy] enemy.ts (UM DONO):
  interface Enemy extends Body { kind: 'tolo'; dir: 1 | -1; alive: boolean; frozen: boolean }
  spawnEnemies(level: ParsedLevel): Enemy[]   // de level.enemies (kind 'fool'|'enemy' -> Enemy kind 'tolo', w=38,h=34, dir=-1, alive=true, frozen=false)
  updateEnemy(e: Enemy, level: ParsedLevel, dt: number): void  // se !alive ou frozen: nao move. senao: e.vx=ENEMY_SPEED*e.dir; stepBody(e,level,dt); inverte e.dir ao bater em parede (vx zerou) ou na iminencia de cair de borda.
  isStomp(player: Player, e: Enemy): boolean  // player.vy>0 && (player.y+player.h) <= e.y + e.h*0.5 && AABB overlap
  drawEnemy(r: Renderer, e: Enemy): void  // retangulo (COLOR_MAGENTA2 corpo, COLOR_INK detalhe); se frozen, tom mais claro
[damage] player.ts ganha (UM DONO = a tarefa de dano, que tambem altera a interface Player):
  Player passa a ser: Player extends Body { char; facing; coyote; jumpBuffer; lives; hearts; iframes: number; ability: AbilityState }  // (importa AbilityState de ability.ts; createPlayer inicia iframes=0, ability=createAbilityState(char))
  novas consts em constants.ts: IFRAME_FRAMES=90; KNOCKBACK_VX=4.5; KNOCKBACK_VY=-8.
  damagePlayer(player: Player, fromX: number): 'blocked'|'hit'|'death'
    // se player.iframes>0 -> 'blocked'. senao se abilityHasShield(player) -> abilityConsumeShield(player); player.iframes=IFRAME_FRAMES -> 'blocked'.
    // senao: player.hearts-=1; player.iframes=IFRAME_FRAMES; player.vx = (player.x<fromX? -KNOCKBACK_VX: KNOCKBACK_VX); player.vy=KNOCKBACK_VY;
    //        se player.hearts<=0 -> player.lives-=1; se player.lives>0 -> respawnPlayer(player,spawn) (hearts=char.hearts, iframes=IFRAME_FRAMES) e 'hit'; senao 'death'.
  respawnPlayer(player, spawn): void; tickPlayerTimers(player, dt): void  // decrementa player.iframes (min 0)
[humanware] humanware.ts (UM DONO; ESTILO = funcoes puras sobre HumanwareState):
  interface HumanwareState { meter: number; modeTimer: number; cooldown: number }
  createHumanware(): HumanwareState   // {0,0,0}
  addMeter(hw: HumanwareState, n: number): void  // meter=min(HW_METER_MAX, meter+n)
  tryActivate(hw: HumanwareState): boolean // se meter>=HW_METER_MAX && cooldown<=0 && modeTimer<=0 -> modeTimer=300; meter=0; return true; senao false
  updateHumanware(hw: HumanwareState, dt: number): void // se modeTimer>0: modeTimer-=dt; se chegou a <=0 nesta chamada -> cooldown=300. senao se cooldown>0: cooldown-=dt.
  isActive(hw: HumanwareState): boolean  // modeTimer>0
  humanwareWorldScale(hw): number        // isActive? HW_TIME_SCALE(0.35) : 1
[select] selectScreen.ts (UM DONO; UMA API):
  interface SelectState { index: number }
  createSelect(): SelectState  // {index:0}
  updateSelect(sel: SelectState, input: Input, chars: CharacterDef[]): string | null  // pressed('left')/'right' move index com wrap; pressed('jump')||pressed('confirm') -> retorna chars[index].id; senao null
  drawSelect(r: Renderer, sel: SelectState, chars: CharacterDef[]): void  // mostra os 5 (drawPlaceholder + nome + habilidade) com cursor
[game] game.ts (UM DONO = tarefa de integracao; NENHUMA outra tarefa reescreve game.ts):
  estado inicial 'select'. createGame mantem {update,render,state,player} (player passa a poder ser null ate selecionar — use um campo interno; manter 'player' exposto apos selecao).
  ORDEM no update(dt):
   'select': const picked = updateSelect(sel,input,chars); se picked -> player=createPlayer(CHARACTERS[picked],level.playerSpawn); state.set('playing'). input.update() por ultimo.
   'playing':
     ws = Math.min(humanwareWorldScale(hw), abilityWorldScale(player), 1)   // §0.6 Math.min
     updatePlayer(player,input,level,dt)            // player SEMPRE dt (escala 1)
     updateAbility(player,input,dt,{level,enemies}) // tempo do player
     escrever bloco temporario do builder em level.tiles (se abilityBuilderTile)
     if input.pressed('humanware') tryActivate(hw); updateHumanware(hw,dt)
     for (e of enemies){ e.frozen = isActive(hw); updateEnemy(e, level, dt*ws) }   // mundo desacelera
     colisao player x enemies: para cada e alive: se isStomp -> e.alive=false; player.vy=STOMP_BOUNCE; addMeter(hw,60); senao se abilityKillsEnemy(player) && overlap -> e.alive=false; senao se overlap -> damagePlayer(player, e.x) (=> se 'death' state.set('over'))
     coin pickup -> addMeter(hw,8)
     if !isActive(hw) time=Math.max(0,time-dt*FIXED_DT); se time<=0 -> state.set('over')   // timer pausado no Modo
     tickPlayerTimers(player,dt); restaurar bloco temporario expirado (level.tiles -> 'empty')
     followCamera(cam,player,level); if checkGoal -> state.set('win'); input.update() por ultimo
   'win'/'over': if input.pressed('confirm') -> reset (novo estado 'select', novo hw, etc).
  render: clear COLOR_BG; se 'select' drawSelect; senao beginWorld(cam): tiles+goal+coins+enemies(drawEnemy)+drawAbilityFx(player)+player(drawPlaceholder); endWorld; drawHud({time,lives:player.lives,coins,hwMeter:hw.meter}); overlay win/over.
[hud] hud.ts: drawHud passa a desenhar o medidor Humanware (barra de 4 segmentos a partir de hwMeter/1000) + coracoes (player hearts) alem de TIME/LIVES/COINS. (A tarefa de HUD/nivel altera hud.ts.)

=== DECISOES M1 ===
- Os 5 personagens selecionaveis desde o inicio. Gating por Lifecard fica para depois.
- Gatilho do Humanware: tecla 'humanware' (KeyH) quando o medidor esta cheio.
- world1-zona1 ganha 2-3 'F' (tolos) andaveis; manter 40x11 e chao continuo; verificar 40 chars/linha programaticamente; manter vencivel andando a direita (posicione tolos com folga / stompaveis, evitando E2E flaky).
- Player roda sempre em dt (escala 1); o MUNDO (inimigos/timer) usa dt*ws. Builder block via level.tiles temporario (sem 2o sistema de colisao).

---

### Task 1: Input: acoes ability/humanware/pause/confirm (DONO de input.ts)

Escopo: Unica tarefa que altera `src/engine/input.ts`. Estende o tipo `InputAction` (M0) com as quatro novas acoes `'ability' | 'humanware' | 'pause' | 'confirm'` e adiciona as entradas em `KEY_MAP` (`KeyJ`->`ability`, `KeyH`->`humanware`, `Escape`->`pause`, `Enter`->`confirm`). `SCROLLING_KEYS` permanece **inalterado** (continua exatamente `{Space, ArrowLeft, ArrowRight, ArrowUp, ArrowDown}`). A interface `Input`, a fabrica `createInput()`, `InputWithDetach`/`detach`, a logica de `e.code`/`heldKeys`/`pressedEdges`/edge-detection e o guard de duplo-attach NAO mudam (so o tipo de uniao e o mapa crescem). Atualiza os testes em `tests/unit/input.test.ts` reusando o helper `key(type, code)` ja existente (`window.dispatchEvent(new KeyboardEvent(type, { code, bubbles: true }))`). Nenhuma outra tarefa toca `input.ts`.

**Files:**
- Modify: `src/engine/input.ts` (arquivo M0; estender `InputAction` + `KEY_MAP`; manter todo o resto)
- Test: `tests/unit/input.test.ts` (arquivo M0; adicionar casos das novas acoes; reusar helper `key()` ja no arquivo)
- (Sem Create: este arquivo ja existe no M0; esta tarefa NAO cria/recria arquivos de outras tarefas — `ability.ts`, `enemy.ts`, `humanware.ts`, `selectScreen.ts`, etc. nao sao tocados aqui.)

Passos (TDD):

- [ ] **Teste que falha (codigo):** adicionar ao final do `describe('createInput', ...)` em `tests/unit/input.test.ts`, ANTES do `})` que fecha o describe (linha 187), o bloco abaixo. Os testes reusam o helper `key()` (linhas 8-10) e o `input`/`beforeEach`/`afterEach` ja existentes.

```ts
  // --- M1: novas acoes ability/humanware/pause/confirm ---

  it('mapeia KeyJ para "ability"', () => {
    key('keydown', 'KeyJ')
    expect(input.isDown('ability')).toBe(true)
    key('keyup', 'KeyJ')
    expect(input.isDown('ability')).toBe(false)
  })

  it('mapeia KeyH para "humanware"', () => {
    key('keydown', 'KeyH')
    expect(input.isDown('humanware')).toBe(true)
    key('keyup', 'KeyH')
    expect(input.isDown('humanware')).toBe(false)
  })

  it('mapeia Escape para "pause"', () => {
    key('keydown', 'Escape')
    expect(input.isDown('pause')).toBe(true)
    key('keyup', 'Escape')
    expect(input.isDown('pause')).toBe(false)
  })

  it('mapeia Enter para "confirm"', () => {
    key('keydown', 'Enter')
    expect(input.isDown('confirm')).toBe(true)
    key('keyup', 'Enter')
    expect(input.isDown('confirm')).toBe(false)
  })

  it('pressed-edge das novas acoes: true so no frame ate o proximo update', () => {
    // ability
    key('keydown', 'KeyJ')
    expect(input.pressed('ability')).toBe(true)
    input.update()
    expect(input.pressed('ability')).toBe(false)
    expect(input.isDown('ability')).toBe(true) // ainda mantida
    key('keyup', 'KeyJ')
    input.update()

    // humanware
    key('keydown', 'KeyH')
    expect(input.pressed('humanware')).toBe(true)
    input.update()
    expect(input.pressed('humanware')).toBe(false)
    key('keyup', 'KeyH')
    input.update()

    // confirm
    key('keydown', 'Enter')
    expect(input.pressed('confirm')).toBe(true)
    input.update()
    expect(input.pressed('confirm')).toBe(false)
    key('keyup', 'Enter')
    input.update()

    // pause
    key('keydown', 'Escape')
    expect(input.pressed('pause')).toBe(true)
    input.update()
    expect(input.pressed('pause')).toBe(false)
  })

  it('auto-repeat de KeyJ nao re-dispara pressed("ability") sem update', () => {
    key('keydown', 'KeyJ')
    expect(input.pressed('ability')).toBe(true)
    key('keydown', 'KeyJ')
    key('keydown', 'KeyJ')
    input.update()
    expect(input.pressed('ability')).toBe(false)
  })

  it('as novas teclas NAO chamam preventDefault (SCROLLING_KEYS inalterado)', () => {
    // SCROLLING_KEYS so cobre Space/ArrowLeft/ArrowRight/ArrowUp/ArrowDown.
    // KeyJ/KeyH/Escape/Enter nao devem ter defaultPrevented marcado.
    for (const code of ['KeyJ', 'KeyH', 'Escape', 'Enter']) {
      const ev = new KeyboardEvent('keydown', { code, bubbles: true, cancelable: true })
      window.dispatchEvent(ev)
      expect(ev.defaultPrevented).toBe(false)
      window.dispatchEvent(new KeyboardEvent('keyup', { code, bubbles: true }))
    }
  })
```

  Tambem atualizar o teste M0 existente `'teclas não mapeadas são ignoradas'` (linhas 130-136) para incluir as novas acoes na lista verificada, garantindo que `KeyZ` (nao mapeada) nao ative nenhuma das 9 acoes:

```ts
  it('teclas não mapeadas são ignoradas', () => {
    const actions: InputAction[] = [
      'left', 'right', 'jump', 'run', 'down',
      'ability', 'humanware', 'pause', 'confirm',
    ]
    key('keydown', 'KeyZ')
    for (const a of actions) {
      expect(input.isDown(a)).toBe(false)
    }
  })
```

- [ ] **Rodar (comando + saida esperada):** `npm run test -- input` (ou `npx vitest run tests/unit/input.test.ts`). Esperado FAIL — TypeScript/Vitest acusa que `'ability' | 'humanware' | 'pause' | 'confirm'` nao sao atribuiveis a `InputAction` (erro de tipo nos `expect(input.isDown('ability'))` etc.) e/ou os novos `it(...)` falham porque `KEY_MAP` ainda nao mapeia `KeyJ/KeyH/Escape/Enter` (`isDown` retorna `false`, `pressed` retorna `false`). Confirmar que os testes M0 anteriores ainda passam.

- [ ] **Implementar (CODIGO TS COMPLETO):** substituir o arquivo `src/engine/input.ts` inteiro pelo conteudo abaixo. Unicas mudancas vs M0: a uniao `InputAction` ganha 4 membros e `KEY_MAP` ganha 4 entradas (`KeyJ`, `KeyH`, `Escape`, `Enter`). `SCROLLING_KEYS`, `Input`, `InputWithDetach`, `createInput` e toda a logica permanecem identicos ao M0.

```ts
// src/engine/input.ts
export type InputAction =
  | 'left'
  | 'right'
  | 'jump'
  | 'run'
  | 'down'
  | 'ability'
  | 'humanware'
  | 'pause'
  | 'confirm'

export interface Input {
  isDown(a: InputAction): boolean
  pressed(a: InputAction): boolean
  update(): void
  attach(target: Window | HTMLElement): void
}

// Mapeia KeyboardEvent.code (físico, independente de layout) -> ação.
const KEY_MAP: Record<string, InputAction> = {
  ArrowLeft: 'left',
  KeyA: 'left',
  ArrowRight: 'right',
  KeyD: 'right',
  ArrowUp: 'jump',
  KeyW: 'jump',
  Space: 'jump',
  ShiftLeft: 'run',
  ShiftRight: 'run',
  ArrowDown: 'down',
  KeyS: 'down',
  KeyJ: 'ability',
  KeyH: 'humanware',
  Escape: 'pause',
  Enter: 'confirm',
}

// Teclas que causam scroll da página — só para estas chamamos preventDefault.
// INALTERADO no M1: as novas teclas (KeyJ/KeyH/Escape/Enter) não fazem scroll.
const SCROLLING_KEYS = new Set(['Space', 'ArrowLeft', 'ArrowRight', 'ArrowUp', 'ArrowDown'])

interface InputWithDetach extends Input {
  detach(target: Window | HTMLElement): void
}

export function createInput(): InputWithDetach {
  // Estado mantido por tecla FÍSICA (KeyboardEvent.code), não por ação.
  const heldKeys = new Set<string>()
  const pressedEdges = new Set<InputAction>()

  // Retorna true se alguma tecla atualmente mantida mapeia para a ação `a`.
  function isDown(a: InputAction): boolean {
    for (const code of heldKeys) {
      if (KEY_MAP[code] === a) return true
    }
    return false
  }

  function onKeyDown(e: KeyboardEvent): void {
    const action = KEY_MAP[e.code]
    if (action === undefined) return

    // Registra edge só se a ação NÃO estava ativa antes desta tecla.
    // Isso cobre: auto-repeat do SO, segunda tecla alias enquanto a outra está mantida.
    const wasDown = isDown(action)
    heldKeys.add(e.code)
    if (!wasDown) {
      pressedEdges.add(action)
    }

    // Evita scroll da página apenas para as teclas que disparam scroll.
    if (SCROLLING_KEYS.has(e.code)) {
      e.preventDefault()
    }
  }

  function onKeyUp(e: KeyboardEvent): void {
    // Remove apenas a tecla física — não toca nas ações.
    // isDown recalcula automaticamente a partir das teclas restantes.
    heldKeys.delete(e.code)
  }

  // Guard de duplo attach: armazena pares (target, handlers) já registrados.
  const attached = new Map<
    Window | HTMLElement,
    { down: EventListener; up: EventListener }
  >()

  return {
    isDown,
    pressed(a: InputAction): boolean {
      return pressedEdges.has(a)
    },
    update(): void {
      // Consome os edges deste frame; pressed só vale até o próximo update.
      pressedEdges.clear()
    },
    attach(target: Window | HTMLElement): void {
      // Não adiciona listeners duplicados ao mesmo target.
      if (attached.has(target)) return

      const boundDown = onKeyDown as EventListener
      const boundUp = onKeyUp as EventListener
      target.addEventListener('keydown', boundDown)
      target.addEventListener('keyup', boundUp)
      attached.set(target, { down: boundDown, up: boundUp })
    },
    detach(target: Window | HTMLElement): void {
      const handlers = attached.get(target)
      if (!handlers) return
      target.removeEventListener('keydown', handlers.down)
      target.removeEventListener('keyup', handlers.up)
      attached.delete(target)
    },
  }
}
```

- [ ] **PASS (comando + saida esperada):** `npm run test -- input` (ou `npx vitest run tests/unit/input.test.ts`) — todos os testes verdes (M0 originais + os novos casos `ability/humanware/pause/confirm`, edge das novas acoes, auto-repeat de `KeyJ`, ausencia de `preventDefault` nas novas teclas, e o teste atualizado de teclas nao mapeadas). Rodar tambem `npx tsc -p tsconfig.json --noEmit` (sem erros: a uniao estendida e atribuivel em todos os `expect(input.isDown(...))`/`pressed(...)`) e `npm run test` completo para confirmar que nenhum outro teste M0 quebrou (a extensao e puramente aditiva).

- [ ] **Commit:** `git add src/engine/input.ts tests/unit/input.test.ts && git commit -m "M1 Task1: input ganha acoes ability/humanware/pause/confirm (KeyJ/KeyH/Escape/Enter)"`.

Notas de contrato respeitadas:
- `InputAction` estendido de forma **aditiva** (M0 nao quebra): ordem `'ability' | 'humanware' | 'pause' | 'confirm'` conforme contrato `[input]`.
- `KEY_MAP` add exatamente `KeyJ->ability, KeyH->humanware, Escape->pause, Enter->confirm`.
- `SCROLLING_KEYS` **inalterado** (`Space, ArrowLeft, ArrowRight, ArrowUp, ArrowDown`) — verificado por teste que as novas teclas nao disparam `preventDefault`.
- Rastreamento por `e.code` (tecla fisica) mantido; testes usam `window.dispatchEvent(new KeyboardEvent('keydown', { code }))` como no M0.
- Esta tarefa NAO cria nem importa `ability.ts`/`enemy.ts`/`humanware.ts`/`selectScreen.ts`; apenas `input.ts` e seu teste sao tocados.

---

### Task 2: characters.ts: 5 personagens + ABILITY_PARAMS (DONO de characters.ts/schema)

Esta tarefa e a UNICA DONA de `src/data/characters.ts` e `src/data/schema.ts`. Adiciona os 4 personagens faltantes (dante/julio/artur/einstein) a `CHARACTERS` com os stats §0.4 e as cores canonicas do contrato, define o tipo `AbilityParams` em `schema.ts` e a tabela `ABILITY_PARAMS` em `characters.ts` (as 6 chaves de `AbilityId`, com `amplificador` em `m1Implemented:false` por §0.9). Nao reescreve nenhum arquivo de outra tarefa; apenas importa cores de `constants.ts` (M0) e o tipo `CharacterDef`/`AbilityId` de `schema.ts`.

**Files:**
- Modify: `src/data/schema.ts` (M0 — adicionar o tipo `AbilityParams`; nao tocar nos tipos existentes)
- Modify: `src/data/characters.ts` (DONO — adicionar os 4 chars + `ABILITY_PARAMS` + imports de cor)
- Test (Modify): `tests/unit/characters.test.ts` (DONO — o teste M0 `Object.keys(CHARACTERS)).toEqual(['renan'])` deixa de valer; substituir por asserts dos 5 chars + `ABILITY_PARAMS`)
- Test (Modify): `tests/unit/schema.test.ts` (DONO — adicionar 1 teste de tipo para `AbilityParams`; manter os testes existentes intactos)

Notas de fato (verificadas no codigo M0 atual):
- `src/data/schema.ts` JA contem `AbilityId` com os 6 ids e a interface `CharacterDef` (com `weightMul`). NAO redefinir esses; so adicionar `AbilityParams`.
- `src/engine/constants.ts` JA exporta todas as cores canonicas necessarias: `COLOR_MAGENTA` `#ff0055`, `COLOR_MAGENTA2` `#e802cf`, `COLOR_BLUE` `#0099ff`, `COLOR_LIME` `#cdf140`, `COLOR_VIOLET` `#7112ff`, `COLOR_TEXT` `#f7f3f6`.
- `CHARACTERS` e `Record<string, CharacterDef>` e hoje so tem `renan`. `characters.ts` hoje importa apenas `COLOR_MAGENTA, COLOR_BLUE`.
- Comandos: testes unit = `npm run test` (vitest run); typecheck = `npx tsc -p tsconfig.json --noEmit`.

---

- [ ] **1. Teste que falha (schema: tipo `AbilityParams`).** Em `tests/unit/schema.test.ts`, adicionar `AbilityParams` ao import de tipos e um novo `it`. Codigo do teste a inserir:

  No import existente (no topo do arquivo) adicionar `type AbilityParams`:
  ```ts
  import {
    TILE_SIZE,
    type TileType,
    type AbilityId,
    type CharacterDef,
    type LevelDef,
    type SpawnPoint,
    type ParsedLevel,
    type AbilityParams,
  } from '../../src/data/schema'
  ```
  E, dentro de `describe('schema', ...)`, adicionar:
  ```ts
  it('AbilityParams aceita os campos canonicos do CONTRATO', () => {
    const dash: AbilityParams = {
      kind: 'dash_criativo',
      cooldown: 24,
      m1Implemented: true,
      dashSpeed: 12.0,
      dashFrames: 12,
      dashIFrames: 16,
    }
    expect(dash.kind).toBe('dash_criativo')
    expect(dash.m1Implemented).toBe(true)
    expect(dash.cooldown).toBe(24)
    const amp: AbilityParams = {
      kind: 'amplificador',
      cooldown: 0,
      m1Implemented: false,
    }
    expect(amp.m1Implemented).toBe(false)
  })
  ```

- [ ] **2. Rodar e ver FALHAR.** `npx tsc -p tsconfig.json --noEmit` (esperado: erro `Module '"../../src/data/schema"' has no exported member 'AbilityParams'`). E/ou `npm run test` -> falha de compilacao no `schema.test.ts`.

- [ ] **3. Implementar `AbilityParams` em `src/data/schema.ts`.** Adicionar a interface ABAIXO de `AbilityId` (sem alterar `AbilityId`/`CharacterDef`/etc). Todos os campos especificos sao opcionais, EXATAMENTE como no contrato:
  ```ts
  export interface AbilityParams {
    kind: AbilityId
    cooldown: number
    m1Implemented: boolean
    maxAirJumps?: number
    airJumpMul?: number
    dashSpeed?: number
    dashFrames?: number
    dashIFrames?: number
    shieldStamina?: number
    builderTtl?: number
    einsteinScale?: number
    einsteinDuration?: number
  }
  ```

- [ ] **4. Rodar e ver PASSAR (schema).** `npx tsc -p tsconfig.json --noEmit` (sem erros) e `npm run test` (suite `schema` verde, incluindo o novo teste). Commit: `feat(schema): add AbilityParams interface (M1 contract)`.

- [ ] **5. Teste que falha (characters: 5 chars + `ABILITY_PARAMS`).** SUBSTITUIR o conteudo de `tests/unit/characters.test.ts` (o assert M0 `toEqual(['renan'])` deixa de valer). Conteudo COMPLETO do novo arquivo:
  ```ts
  // tests/unit/characters.test.ts
  import { describe, it, expect } from 'vitest'
  import { CHARACTERS, DEFAULT_CHARACTER_ID, ABILITY_PARAMS } from '../../src/data/characters'
  import {
    COLOR_MAGENTA,
    COLOR_MAGENTA2,
    COLOR_BLUE,
    COLOR_LIME,
    COLOR_VIOLET,
    COLOR_TEXT,
  } from '../../src/engine/constants'
  import type { AbilityId } from '../../src/data/schema'

  describe('CHARACTERS registry', () => {
    it('expoe renan como personagem default', () => {
      expect(DEFAULT_CHARACTER_ID).toBe('renan')
      expect(CHARACTERS[DEFAULT_CHARACTER_ID]).toBeDefined()
    })

    it('tem os 5 personagens selecionaveis do M1', () => {
      expect(Object.keys(CHARACTERS)).toEqual([
        'renan',
        'dante',
        'julio',
        'artur',
        'einstein',
      ])
    })

    it('renan: stats §0.4 + cores canonicas', () => {
      const c = CHARACTERS['renan']
      expect(c.id).toBe('renan')
      expect(c.name).toBe('Renan')
      expect(c.abilityId).toBe('salto_visionario')
      expect(c.jumpVelMul).toBe(1.06)
      expect(c.walkMul).toBe(1.0)
      expect(c.runMul).toBe(1.0)
      expect(c.weightMul).toBe(0.96)
      expect(c.hearts).toBe(3)
      expect(c.color).toBe(COLOR_MAGENTA)
      expect(c.accent).toBe(COLOR_BLUE)
    })

    it('dante: stats §0.4 + cores canonicas', () => {
      const c = CHARACTERS['dante']
      expect(c.id).toBe('dante')
      expect(c.name).toBe('Dante')
      expect(c.abilityId).toBe('dash_criativo')
      expect(c.jumpVelMul).toBe(1.0)
      expect(c.walkMul).toBe(1.06)
      expect(c.runMul).toBe(1.08)
      expect(c.weightMul).toBe(1.0)
      expect(c.hearts).toBe(3)
      expect(c.color).toBe(COLOR_BLUE)
      expect(c.accent).toBe(COLOR_LIME)
    })

    it('julio: stats §0.4 + cores canonicas', () => {
      const c = CHARACTERS['julio']
      expect(c.id).toBe('julio')
      expect(c.name).toBe('Julio')
      expect(c.abilityId).toBe('escudo_governanca')
      expect(c.jumpVelMul).toBe(1.0)
      expect(c.walkMul).toBe(1.0)
      expect(c.runMul).toBe(1.0)
      expect(c.weightMul).toBe(1.08)
      expect(c.hearts).toBe(4)
      expect(c.color).toBe(COLOR_VIOLET)
      expect(c.accent).toBe(COLOR_MAGENTA)
    })

    it('artur: stats §0.4 + cores canonicas', () => {
      const c = CHARACTERS['artur']
      expect(c.id).toBe('artur')
      expect(c.name).toBe('Artur')
      expect(c.abilityId).toBe('builder')
      expect(c.jumpVelMul).toBe(1.0)
      expect(c.walkMul).toBe(1.0)
      expect(c.runMul).toBe(1.0)
      expect(c.weightMul).toBe(1.0)
      expect(c.hearts).toBe(3)
      expect(c.color).toBe(COLOR_LIME)
      expect(c.accent).toBe(COLOR_BLUE)
    })

    it('einstein: stats §0.4 + cores canonicas', () => {
      const c = CHARACTERS['einstein']
      expect(c.id).toBe('einstein')
      expect(c.name).toBe('Einstein')
      expect(c.abilityId).toBe('emc2')
      expect(c.jumpVelMul).toBe(0.94)
      expect(c.walkMul).toBe(0.92)
      expect(c.runMul).toBe(1.0)
      expect(c.weightMul).toBe(1.0)
      expect(c.hearts).toBe(2)
      expect(c.color).toBe(COLOR_TEXT)
      expect(c.accent).toBe(COLOR_MAGENTA2)
    })

    it('cada char aponta para um abilityId valido', () => {
      const ids: AbilityId[] = [
        'salto_visionario',
        'dash_criativo',
        'escudo_governanca',
        'builder',
        'emc2',
        'amplificador',
      ]
      for (const c of Object.values(CHARACTERS)) {
        expect(ids).toContain(c.abilityId)
      }
    })
  })

  describe('ABILITY_PARAMS', () => {
    it('tem as 6 chaves de AbilityId', () => {
      expect(Object.keys(ABILITY_PARAMS).sort()).toEqual(
        [
          'salto_visionario',
          'dash_criativo',
          'escudo_governanca',
          'builder',
          'emc2',
          'amplificador',
        ].sort(),
      )
    })

    it('salto_visionario: valores canonicos', () => {
      const p = ABILITY_PARAMS.salto_visionario
      expect(p.kind).toBe('salto_visionario')
      expect(p.cooldown).toBe(0)
      expect(p.m1Implemented).toBe(true)
      expect(p.maxAirJumps).toBe(1)
      expect(p.airJumpMul).toBe(0.92)
    })

    it('dash_criativo: valores canonicos', () => {
      const p = ABILITY_PARAMS.dash_criativo
      expect(p.kind).toBe('dash_criativo')
      expect(p.cooldown).toBe(24)
      expect(p.m1Implemented).toBe(true)
      expect(p.dashSpeed).toBe(12.0)
      expect(p.dashFrames).toBe(12)
      expect(p.dashIFrames).toBe(16)
    })

    it('escudo_governanca: valores canonicos', () => {
      const p = ABILITY_PARAMS.escudo_governanca
      expect(p.kind).toBe('escudo_governanca')
      expect(p.cooldown).toBe(240)
      expect(p.m1Implemented).toBe(true)
      expect(p.shieldStamina).toBe(180)
    })

    it('builder: valores canonicos', () => {
      const p = ABILITY_PARAMS.builder
      expect(p.kind).toBe('builder')
      expect(p.cooldown).toBe(90)
      expect(p.m1Implemented).toBe(true)
      expect(p.builderTtl).toBe(240)
    })

    it('emc2: valores canonicos', () => {
      const p = ABILITY_PARAMS.emc2
      expect(p.kind).toBe('emc2')
      expect(p.cooldown).toBe(300)
      expect(p.m1Implemented).toBe(true)
      expect(p.einsteinScale).toBe(0.25)
      expect(p.einsteinDuration).toBe(210)
    })

    it('amplificador: chave existe mas desligada no M1 (§0.9)', () => {
      const p = ABILITY_PARAMS.amplificador
      expect(p.kind).toBe('amplificador')
      expect(p.cooldown).toBe(0)
      expect(p.m1Implemented).toBe(false)
    })
  })
  ```

- [ ] **6. Rodar e ver FALHAR.** `npm run test` -> falhas em `characters.test.ts` (`ABILITY_PARAMS` nao exportado; `dante/julio/artur/einstein` indefinidos). `npx tsc -p tsconfig.json --noEmit` -> erro `has no exported member 'ABILITY_PARAMS'`.

- [ ] **7. Implementar `src/data/characters.ts`.** SUBSTITUIR o conteudo COMPLETO do arquivo (preserva `renan` identico ao M0 + adiciona os 4 chars com stats §0.4/cores canonicas + `ABILITY_PARAMS` exatamente como no contrato). `CHARACTERS` mantem o tipo `Record<string, CharacterDef>`; a ordem das chaves e `renan, dante, julio, artur, einstein` (consumida pela tela de selecao e pelo teste do passo 5). Conteudo:
  ```ts
  // src/data/characters.ts
  import type { CharacterDef, AbilityId, AbilityParams } from './schema'
  import {
    COLOR_MAGENTA,
    COLOR_MAGENTA2,
    COLOR_BLUE,
    COLOR_LIME,
    COLOR_VIOLET,
    COLOR_TEXT,
  } from '../engine/constants'

  export const DEFAULT_CHARACTER_ID = 'renan'

  export const CHARACTERS: Record<string, CharacterDef> = {
    renan: {
      id: 'renan',
      name: 'Renan',
      abilityId: 'salto_visionario',
      color: COLOR_MAGENTA,
      accent: COLOR_BLUE,
      hearts: 3,
      jumpVelMul: 1.06,
      walkMul: 1.0,
      runMul: 1.0,
      weightMul: 0.96,
    },
    dante: {
      id: 'dante',
      name: 'Dante',
      abilityId: 'dash_criativo',
      color: COLOR_BLUE,
      accent: COLOR_LIME,
      hearts: 3,
      jumpVelMul: 1.0,
      walkMul: 1.06,
      runMul: 1.08,
      weightMul: 1.0,
    },
    julio: {
      id: 'julio',
      name: 'Julio',
      abilityId: 'escudo_governanca',
      color: COLOR_VIOLET,
      accent: COLOR_MAGENTA,
      hearts: 4,
      jumpVelMul: 1.0,
      walkMul: 1.0,
      runMul: 1.0,
      weightMul: 1.08,
    },
    artur: {
      id: 'artur',
      name: 'Artur',
      abilityId: 'builder',
      color: COLOR_LIME,
      accent: COLOR_BLUE,
      hearts: 3,
      jumpVelMul: 1.0,
      walkMul: 1.0,
      runMul: 1.0,
      weightMul: 1.0,
    },
    einstein: {
      id: 'einstein',
      name: 'Einstein',
      abilityId: 'emc2',
      color: COLOR_TEXT,
      accent: COLOR_MAGENTA2,
      hearts: 2,
      jumpVelMul: 0.94,
      walkMul: 0.92,
      runMul: 1.0,
      weightMul: 1.0,
    },
  }

  // Parametros canonicos das habilidades (CONTRATO M1). amplificador = M3+, desligado (§0.9).
  export const ABILITY_PARAMS: Record<AbilityId, AbilityParams> = {
    salto_visionario: {
      kind: 'salto_visionario',
      cooldown: 0,
      m1Implemented: true,
      maxAirJumps: 1,
      airJumpMul: 0.92,
    },
    dash_criativo: {
      kind: 'dash_criativo',
      cooldown: 24,
      m1Implemented: true,
      dashSpeed: 12.0,
      dashFrames: 12,
      dashIFrames: 16,
    },
    escudo_governanca: {
      kind: 'escudo_governanca',
      cooldown: 240,
      m1Implemented: true,
      shieldStamina: 180,
    },
    builder: {
      kind: 'builder',
      cooldown: 90,
      m1Implemented: true,
      builderTtl: 240,
    },
    emc2: {
      kind: 'emc2',
      cooldown: 300,
      m1Implemented: true,
      einsteinScale: 0.25,
      einsteinDuration: 210,
    },
    amplificador: {
      kind: 'amplificador',
      cooldown: 0,
      m1Implemented: false,
    },
  }
  ```

- [ ] **8. Rodar e ver PASSAR.** `npm run test` (suites `characters` e `schema` verdes) e `npx tsc -p tsconfig.json --noEmit` (sem erros). Verificar tambem que o restante da suite continua verde — `renan` permanece identico, entao `player`/`game`/`sprites` que ja consomem `CHARACTERS.renan` nao quebram (nenhum teste M0 dependia de `CHARACTERS` ter so 1 chave alem do proprio `characters.test.ts`, que esta sob esta tarefa).

- [ ] **9. Commit.** `feat(characters): add 5 selectable chars + ABILITY_PARAMS (M1 §0.4/§0.9)`.

Fora de escopo (NAO fazer aqui; sao de outras tarefas — apenas consumir via import): `AbilityState`/`ability.ts`, `Enemy`/`enemy.ts`, `InputAction`/`input.ts`, `selectScreen.ts`, `humanware.ts`, `player.ts` (dano), `game.ts`, `hud.ts`, e o nivel `world1-zona1.ts`. Esta tarefa nao toca em `constants.ts` (todas as cores ja existem no M0).

---

### Task 3: ability.ts: modulo completo das 5 habilidades + helpers (DONO de ability.ts)

Escopo: unica tarefa que CRIA `src/game/ability.ts` com todo o sistema de habilidades: `AbilityState`, `createAbilityState(char)`, `AbilityCtx`, `updateAbility(player,input,dt,ctx)` despachando por `player.ability.id` e implementando AS 5 habilidades (`salto_visionario`, `dash_criativo`, `escudo_governanca`, `builder`, `emc2`) EXATAMENTE com os valores de `ABILITY_PARAMS`, mais os helpers `abilityWorldScale` / `abilityHasShield` / `abilityConsumeShield` / `abilityIFramesOnHit` / `abilityKillsEnemy` / `abilityBuilderTile` / `drawAbilityFx`. Esta tarefa NAO cria `enemy.ts` nem `characters.ts` nem `player.ts` — apenas importa tipos/valores deles (`Enemy` de `enemy.ts`, `ABILITY_PARAMS` de `../data/characters`, `Player` de `player.ts`).

Dependencia de tipos entre arquivos (decidida no contrato, nao recriar nada aqui):
- `AbilityState` e definido NESTE arquivo; o `Player` (alterado pela tarefa [damage]) importa `AbilityState` daqui e ganha `iframes:number; ability:AbilityState`.
- `Enemy` vem de `enemy.ts` (tarefa [enemy]); aqui so o tipo e usado em `AbilityCtx`.
- `ABILITY_PARAMS` vem de `../data/characters` (tarefa [chars]).
Como [damage], [enemy] e [chars] podem ainda nao estar prontos quando esta tarefa roda, o TESTE desta tarefa NAO importa nada desses arquivos: ele constroi um `Player`-like e um `Enemy`-like locais (structural typing) e define localmente os params com os valores canonicos §0. O CODIGO de producao importa os modulos reais. Isso mantem a Task 3 verde de forma independente; a integracao real e exercida pela tarefa [game].

Mecanismo do Builder (documentar e implementar exatamente assim): a habilidade so registra `player.ability.builder = {col,row,ttl}`. A FISICA do bloco e feita pelo `game` (tarefa [game]), que ANTES de mover player/inimigos escreve `level.tiles[row][col]='block'` se a celula era `'empty'`, e restaura `'empty'` quando expira. Aqui exponho apenas `abilityBuilderTile(player)` (a celula atual ou `null`) e desenho o bloco em `drawAbilityFx`. NAO existe segundo sistema de colisao.

**Files:**
- Create: `src/game/ability.ts` (DONO)
- Create: `tests/unit/ability.test.ts` (DONO do teste desta tarefa)
- Modify: nenhum arquivo M0 e alterado por esta tarefa.
- Consumo (import apenas, NUNCA Create/recriar):
  - `import type { Player } from './player'` (campos `iframes`/`ability` adicionados pela tarefa [damage])
  - `import type { Enemy } from './enemy'` (tarefa [enemy])
  - `import { ABILITY_PARAMS } from '../data/characters'` (tarefa [chars])
  - `import type { CharacterDef, AbilityId, ParsedLevel } from '../data/schema'` (M0)
  - `import type { Input } from '../engine/input'` (M0)
  - `import type { Renderer } from '../engine/render'` (M0)
  - `import { JUMP_VEL, TILE, COLOR_BLUE, COLOR_LIME, COLOR_TEXT } from '../engine/constants'` (M0; nomes SEM sufixo: `TILE`, `JUMP_VEL`)

Notas de valores §0/ABILITY_PARAMS usados (nao redefinir, vem de `../data/characters`):
salto_visionario `{maxAirJumps:1,airJumpMul:0.92,cooldown:0}` · dash_criativo `{dashSpeed:12.0,dashFrames:12,dashIFrames:16,cooldown:24}` · escudo_governanca `{shieldStamina:180,cooldown:240}` · builder `{builderTtl:240,cooldown:90}` · emc2 `{einsteinScale:0.25,einsteinDuration:210,cooldown:300}` · amplificador `{m1Implemented:false}` (no-op).
Convencao de dt = frames (E1): testes usam dt=1; `updateAbility` recebe sempre o dt do PLAYER (escala 1).

#### Passos TDD

- [ ] **Passo 0 — Helpers de teste (sem assercao ainda).** No topo de `tests/unit/ability.test.ts`, criar stand-ins locais (structural typing) para nao depender de outras tarefas:

```ts
// tests/unit/ability.test.ts
import { describe, it, expect, beforeEach } from 'vitest'
import {
  createAbilityState,
  updateAbility,
  abilityWorldScale,
  abilityHasShield,
  abilityConsumeShield,
  abilityIFramesOnHit,
  abilityKillsEnemy,
  abilityBuilderTile,
  drawAbilityFx,
  type AbilityState,
  type AbilityCtx,
} from '../../src/game/ability'
import type { Player } from '../../src/game/player'
import type { Enemy } from '../../src/game/enemy'
import type { CharacterDef, AbilityId, ParsedLevel, TileType } from '../../src/data/schema'
import type { Input, InputAction } from '../../src/engine/input'
import type { Renderer } from '../../src/engine/render'
import { JUMP_VEL, TILE } from '../../src/engine/constants'

// FakeInput canonico (mesmo padrao de player.test.ts): pressed = edge desde o ultimo update().
class FakeInput implements Input {
  private down = new Set<InputAction>()
  private prev = new Set<InputAction>()
  set(a: InputAction, v: boolean): void { if (v) this.down.add(a); else this.down.delete(a) }
  isDown(a: InputAction): boolean { return this.down.has(a) }
  pressed(a: InputAction): boolean { return this.down.has(a) && !this.prev.has(a) }
  update(): void { this.prev = new Set(this.down) }
  attach(): void {}
}

// Char minimo por habilidade.
function makeChar(abilityId: AbilityId): CharacterDef {
  return {
    id: abilityId, name: abilityId, abilityId,
    color: '#ffffff', accent: '#000000',
    hearts: 3, jumpVelMul: 1, walkMul: 1, runMul: 1, weightMul: 1,
  }
}

// Player-like: cobre Body + campos M1 (iframes, ability) usados pela ability.ts.
function makePlayer(abilityId: AbilityId): Player {
  const char = makeChar(abilityId)
  return {
    x: 100, y: 100, w: 34, h: 42, vx: 0, vy: 0, onGround: true,
    char, facing: 1, coyote: 0, jumpBuffer: 0, lives: 3, hearts: char.hearts,
    iframes: 0, ability: createAbilityState(char),
  } as unknown as Player
}

// Enemy-like minimo (Body + campos de enemy.ts).
function makeEnemy(x: number, y: number): Enemy {
  return {
    x, y, w: 38, h: 34, vx: 0, vy: 0, onGround: true,
    kind: 'tolo', dir: -1, alive: true, frozen: false,
  } as unknown as Enemy
}

// Level plano 40x11, chao nas rows 9-10 (igual canone world1-zona1).
function makeLevel(): ParsedLevel {
  const widthTiles = 40, heightTiles = 11
  const tiles: TileType[][] = Array.from({ length: heightTiles }, (_, r) =>
    Array.from({ length: widthTiles }, () => (r >= 9 ? 'ground' : 'empty') as TileType))
  return {
    widthTiles, heightTiles, widthPx: widthTiles * TILE, heightPx: heightTiles * TILE,
    tiles, playerSpawn: { x: 2 * TILE, y: 8 * TILE }, goal: { x: 36 * TILE, y: 8 * TILE },
    coins: [], enemies: [],
  }
}

function makeCtx(level: ParsedLevel, enemies: Enemy[]): AbilityCtx { return { level, enemies } }

// Renderer fake que apenas conta drawRect.
function makeRenderer(): { r: Renderer; rects: number } {
  const state = { rects: 0 }
  const r: Renderer = {
    ctx: {} as CanvasRenderingContext2D,
    clear() {}, beginWorld() {}, endWorld() {}, present() {},
    drawRect() { state.rects++ },
  }
  return { r, get rects() { return state.rects } } as unknown as { r: Renderer; rects: number }
}
```

- [ ] **Passo 1 — Teste FALHA: `createAbilityState` zera tudo e copia o id do char.** Adicionar:

```ts
describe('createAbilityState', () => {
  it('zera todos os campos e usa char.abilityId', () => {
    const s = createAbilityState(makeChar('dash_criativo'))
    expect(s.id).toBe('dash_criativo')
    expect(s.cooldown).toBe(0)
    expect(s.active).toBe(false)
    expect(s.timer).toBe(0)
    expect(s.airJumps).toBe(0)
    expect(s.shield).toBe(false)
    expect(s.shieldTimer).toBe(0)
    expect(s.builder).toBeNull()
    expect(s.einsteinTimer).toBe(0)
  })
})
```

  Rodar `npm run test -- ability` → FAIL: `Failed to resolve import "../../src/game/ability"` (arquivo nao existe ainda).

- [ ] **Passo 2 — IMPLEMENTAR base do arquivo (tipos + `createAbilityState` + helpers de leitura).** Criar `src/game/ability.ts` com o cabecalho e o estado:

```ts
// src/game/ability.ts
// DONO do sistema de habilidades (M1). Cria AbilityState + 5 habilidades + helpers.
// dt = frames (E1). updateAbility e chamado APOS updatePlayer, sempre no tempo do PLAYER (escala 1).
import type { Player } from './player'
import type { Enemy } from './enemy'
import { ABILITY_PARAMS } from '../data/characters'
import type { CharacterDef, AbilityId, ParsedLevel } from '../data/schema'
import type { Input } from '../engine/input'
import type { Renderer } from '../engine/render'
import { JUMP_VEL, TILE, COLOR_BLUE, COLOR_LIME, COLOR_TEXT } from '../engine/constants'

export interface AbilityState {
  id: AbilityId
  cooldown: number
  active: boolean
  timer: number
  airJumps: number
  shield: boolean
  shieldTimer: number
  builder: { col: number; row: number; ttl: number } | null
  einsteinTimer: number
}

export function createAbilityState(char: CharacterDef): AbilityState {
  return {
    id: char.abilityId,
    cooldown: 0,
    active: false,
    timer: 0,
    airJumps: 0,
    shield: false,
    shieldTimer: 0,
    builder: null,
    einsteinTimer: 0,
  }
}

export interface AbilityCtx {
  level: ParsedLevel
  enemies: Enemy[]
}

// emc2 ativo (einsteinTimer>0) => escala 0.25; senao 1. (§0.6: o game faz Math.min.)
export function abilityWorldScale(player: Player): number {
  const p = ABILITY_PARAMS[player.ability.id]
  return player.ability.einsteinTimer > 0 ? (p.einsteinScale ?? 1) : 1
}

export function abilityHasShield(player: Player): boolean {
  return player.ability.shield && player.ability.shieldTimer > 0
}

export function abilityConsumeShield(player: Player): void {
  player.ability.shield = false
  player.ability.shieldTimer = 0
}

// Dash ativo concede i-frames (consumido por damagePlayer via game).
export function abilityIFramesOnHit(player: Player): boolean {
  return player.ability.id === 'dash_criativo' && player.ability.active && player.ability.timer > 0
}

// Dash ativo mata tolo por contato (consumido pelo game).
export function abilityKillsEnemy(player: Player): boolean {
  return player.ability.id === 'dash_criativo' && player.ability.active && player.ability.timer > 0
}

// Bloco temporario solido atual do builder (ou null). O game escreve/restaura level.tiles.
export function abilityBuilderTile(player: Player): { col: number; row: number } | null {
  const b = player.ability.builder
  return b ? { col: b.col, row: b.row } : null
}
```

  Rodar `npm run test -- ability` → PASS (Passo 1 verde). `npx tsc -p tsconfig.json --noEmit` → sem erros nesta unidade.
  Commit: `feat(ability): AbilityState + createAbilityState + helpers de leitura`.

- [ ] **Passo 3 — Teste FALHA: salto_visionario (pulo duplo).** Adicionar:

```ts
describe('updateAbility — salto_visionario', () => {
  let input: FakeInput, level: ParsedLevel, ctx: AbilityCtx
  beforeEach(() => { input = new FakeInput(); level = makeLevel(); ctx = makeCtx(level, []) })

  it('no AR concede 1 pulo extra (vy=JUMP_VEL*jumpVelMul*airJumpMul) e incrementa airJumps', () => {
    const p = makePlayer('salto_visionario')
    p.onGround = false; p.vy = 2 // caindo
    input.set('ability', true)
    updateAbility(p, input, 1, ctx); input.update()
    const expected = JUMP_VEL * p.char.jumpVelMul * (ABILITY_PARAMS.salto_visionario.airJumpMul ?? 1)
    expect(p.vy).toBeCloseTo(expected, 5)
    expect(p.ability.airJumps).toBe(1)
  })

  it('nao concede um segundo pulo extra alem de maxAirJumps', () => {
    const p = makePlayer('salto_visionario')
    p.onGround = false
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    const vyApos1 = p.vy
    // segundo aperto de ability ainda no ar: airJumps ja == maxAirJumps(1) -> sem mudanca de vy
    input.set('ability', false); input.update()
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    expect(p.ability.airJumps).toBe(1)
    expect(p.vy).toBe(vyApos1) // updateAbility nao integra fisica; vy inalterado
  })

  it('airJumps reseta para 0 quando onGround', () => {
    const p = makePlayer('salto_visionario')
    p.ability.airJumps = 1; p.onGround = true
    updateAbility(p, input, 1, ctx)
    expect(p.ability.airJumps).toBe(0)
  })

  it('no chao a habilidade nao dispara pulo extra', () => {
    const p = makePlayer('salto_visionario'); p.onGround = true; p.vy = 0
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    expect(p.vy).toBe(0); expect(p.ability.airJumps).toBe(0)
  })
})
```

  Rodar `npm run test -- ability` → FAIL: `updateAbility is not a function` (ainda nao exportado).

- [ ] **Passo 4 — IMPLEMENTAR `updateAbility` + dispatch + salto_visionario.** Adicionar ao final de `ability.ts`:

```ts
// Decrementa timers comuns (cooldown/shield/einstein/builder.ttl) sem ir abaixo de 0.
function tickTimers(s: AbilityState, dt: number): void {
  if (s.cooldown > 0) s.cooldown = Math.max(0, s.cooldown - dt)
  if (s.shieldTimer > 0) {
    s.shieldTimer = Math.max(0, s.shieldTimer - dt)
    if (s.shieldTimer <= 0) s.shield = false
  }
  if (s.einsteinTimer > 0) s.einsteinTimer = Math.max(0, s.einsteinTimer - dt)
  if (s.builder) {
    s.builder.ttl -= dt
    if (s.builder.ttl <= 0) s.builder = null
  }
}

export function updateAbility(player: Player, input: Input, dt: number, ctx: AbilityCtx): void {
  const s = player.ability
  // Pulo duplo: airJumps zera ao tocar o chao.
  if (player.onGround) s.airJumps = 0

  // Dash em andamento: mantem vx e decrementa timer ANTES do gatilho.
  if (s.id === 'dash_criativo' && s.active && s.timer > 0) {
    const p = ABILITY_PARAMS.dash_criativo
    player.vx = player.facing * (p.dashSpeed ?? 0)
    s.timer -= dt
    if (s.timer <= 0) { s.active = false; s.timer = 0; s.cooldown = p.cooldown }
  }

  tickTimers(s, dt)

  const params = ABILITY_PARAMS[s.id]
  if (!params.m1Implemented) return // amplificador: no-op no M1
  if (!input.pressed('ability') || s.cooldown > 0) return
  if (s.active) return // dash ja ativo nao reinicia

  switch (s.id) {
    case 'salto_visionario': {
      if (!player.onGround && s.airJumps < (params.maxAirJumps ?? 0)) {
        player.vy = JUMP_VEL * player.char.jumpVelMul * (params.airJumpMul ?? 1)
        s.airJumps += 1
      }
      break
    }
    case 'dash_criativo': {
      s.active = true
      s.timer = params.dashFrames ?? 0
      player.vx = player.facing * (params.dashSpeed ?? 0)
      break
    }
    case 'escudo_governanca': {
      s.shield = true
      s.shieldTimer = params.shieldStamina ?? 0
      s.cooldown = params.cooldown
      break
    }
    case 'builder': {
      const col = player.facing === 1
        ? Math.floor((player.x + player.w) / TILE)
        : Math.floor(player.x / TILE) - 1
      const row = Math.floor((player.y + player.h - 1) / TILE)
      s.builder = { col, row, ttl: params.builderTtl ?? 0 }
      s.cooldown = params.cooldown
      break
    }
    case 'emc2': {
      s.einsteinTimer = params.einsteinDuration ?? 0
      s.cooldown = params.cooldown
      break
    }
  }
}
```

  Rodar `npm run test -- ability` → PASS (Passos 1, 3 verdes).
  Commit: `feat(ability): updateAbility + dispatch + salto_visionario`.

- [ ] **Passo 5 — Teste FALHA: dash_criativo (acelera + cooldown + i-frames + mata tolo).** Adicionar:

```ts
describe('updateAbility — dash_criativo', () => {
  let input: FakeInput, level: ParsedLevel, ctx: AbilityCtx
  beforeEach(() => { input = new FakeInput(); level = makeLevel(); ctx = makeCtx(level, []) })

  it('ativa: vx=facing*dashSpeed, active=true, timer=dashFrames, e concede i-frames/kill', () => {
    const p = makePlayer('dash_criativo'); p.facing = 1
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    expect(p.vx).toBeCloseTo(ABILITY_PARAMS.dash_criativo.dashSpeed ?? 0, 5)
    expect(p.ability.active).toBe(true)
    expect(p.ability.timer).toBeGreaterThan(0)
    expect(abilityIFramesOnHit(p)).toBe(true)
    expect(abilityKillsEnemy(p)).toBe(true)
  })

  it('respeita facing=-1 (vx negativo)', () => {
    const p = makePlayer('dash_criativo'); p.facing = -1
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    expect(p.vx).toBeCloseTo(-(ABILITY_PARAMS.dash_criativo.dashSpeed ?? 0), 5)
  })

  it('ao terminar dashFrames: active=false e cooldown=24; depois i-frames/kill ficam false', () => {
    const p = makePlayer('dash_criativo')
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    input.set('ability', false)
    // gasta dashFrames frames adicionais
    const frames = ABILITY_PARAMS.dash_criativo.dashFrames ?? 0
    for (let i = 0; i < frames; i++) { updateAbility(p, input, 1, ctx); input.update() }
    expect(p.ability.active).toBe(false)
    expect(p.ability.cooldown).toBe(ABILITY_PARAMS.dash_criativo.cooldown)
    expect(abilityIFramesOnHit(p)).toBe(false)
    expect(abilityKillsEnemy(p)).toBe(false)
  })

  it('mantem vx durante o dash (nao reinicia ao soltar a tecla)', () => {
    const p = makePlayer('dash_criativo'); p.facing = 1
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    input.set('ability', false); p.vx = 0 // simula deceleracao externa
    updateAbility(p, input, 1, ctx)
    expect(p.vx).toBeCloseTo(ABILITY_PARAMS.dash_criativo.dashSpeed ?? 0, 5) // dash reescreve vx
  })
})
```

  Rodar `npm run test -- ability` → PASS (o dash ja foi implementado no Passo 4). Se algum caso falhar, ajustar a ordem "manter vx -> decrementar timer -> ao zerar setar cooldown" ate ficar verde.
  Commit: `test(ability): cobertura completa do dash_criativo`.

- [ ] **Passo 6 — Teste FALHA: escudo_governanca (absorve 1 hit).** Adicionar:

```ts
describe('updateAbility — escudo_governanca', () => {
  let input: FakeInput, ctx: AbilityCtx
  beforeEach(() => { input = new FakeInput(); ctx = makeCtx(makeLevel(), []) })

  it('ativa shield + shieldTimer=180 + cooldown=240', () => {
    const p = makePlayer('escudo_governanca')
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    expect(abilityHasShield(p)).toBe(true)
    expect(p.ability.shieldTimer).toBe(ABILITY_PARAMS.escudo_governanca.shieldStamina)
    expect(p.ability.cooldown).toBe(ABILITY_PARAMS.escudo_governanca.cooldown)
  })

  it('abilityConsumeShield desliga o escudo (absorve 1 hit)', () => {
    const p = makePlayer('escudo_governanca')
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    expect(abilityHasShield(p)).toBe(true)
    abilityConsumeShield(p)
    expect(abilityHasShield(p)).toBe(false)
    expect(p.ability.shield).toBe(false)
    expect(p.ability.shieldTimer).toBe(0)
  })

  it('shield expira por tempo (shieldTimer -> 0 desliga shield)', () => {
    const p = makePlayer('escudo_governanca')
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    input.set('ability', false)
    const ticks = (ABILITY_PARAMS.escudo_governanca.shieldStamina ?? 0)
    for (let i = 0; i < ticks; i++) { updateAbility(p, input, 1, ctx); input.update() }
    expect(abilityHasShield(p)).toBe(false)
  })
})
```

  Rodar `npm run test -- ability` → PASS (escudo ja implementado no Passo 4; `tickTimers` desliga shield ao zerar). Verde direto = confirma o comportamento.
  Commit: `test(ability): escudo_governanca absorve 1 hit e expira`.

- [ ] **Passo 7 — Teste FALHA: builder (bloco solido a frente + expira).** Adicionar:

```ts
describe('updateAbility — builder', () => {
  let input: FakeInput, level: ParsedLevel, ctx: AbilityCtx
  beforeEach(() => { input = new FakeInput(); level = makeLevel(); ctx = makeCtx(level, []) })

  it('cria bloco na celula a FRENTE (facing=1) na linha dos pes + cooldown=90', () => {
    const p = makePlayer('builder'); p.facing = 1
    p.x = 5 * TILE; p.y = 8 * TILE - p.h + (p.h - 1) // pes na row 7? garantir pes dentro de uma row
    p.x = 5 * TILE; p.y = 7 * TILE // pes em (7*48 + 41) -> row 7
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    const tile = abilityBuilderTile(p)
    expect(tile).not.toBeNull()
    const expectedCol = Math.floor((p.x + p.w) / TILE)
    const expectedRow = Math.floor((p.y + p.h - 1) / TILE)
    expect(tile!.col).toBe(expectedCol)
    expect(tile!.row).toBe(expectedRow)
    expect(p.ability.cooldown).toBe(ABILITY_PARAMS.builder.cooldown)
  })

  it('facing=-1 cria bloco na celula a esquerda', () => {
    const p = makePlayer('builder'); p.facing = -1; p.x = 5 * TILE; p.y = 7 * TILE
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    const tile = abilityBuilderTile(p)!
    expect(tile.col).toBe(Math.floor(p.x / TILE) - 1)
  })

  it('o bloco EXPIRA quando ttl<=0 (builder volta a null)', () => {
    const p = makePlayer('builder'); p.x = 5 * TILE; p.y = 7 * TILE
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    input.set('ability', false)
    const ttl = ABILITY_PARAMS.builder.builderTtl ?? 0
    for (let i = 0; i < ttl; i++) { updateAbility(p, input, 1, ctx); input.update() }
    expect(abilityBuilderTile(p)).toBeNull()
  })
})
```

  Rodar `npm run test -- ability` → PASS (builder ja implementado no Passo 4; `tickTimers` zera `builder` ao expirar ttl). Verde = confirma celula/linha-dos-pes e expiracao.
  Commit: `test(ability): builder cria bloco a frente e expira`.

- [ ] **Passo 8 — Teste FALHA: emc2 (worldScale 0.25 por 210f).** Adicionar:

```ts
describe('updateAbility — emc2 / abilityWorldScale', () => {
  let input: FakeInput, ctx: AbilityCtx
  beforeEach(() => { input = new FakeInput(); ctx = makeCtx(makeLevel(), []) })

  it('inativo: worldScale=1', () => {
    const p = makePlayer('emc2')
    expect(abilityWorldScale(p)).toBe(1)
  })

  it('ativa: einsteinTimer=210, cooldown=300 e worldScale=0.25', () => {
    const p = makePlayer('emc2')
    input.set('ability', true); updateAbility(p, input, 1, ctx)
    expect(p.ability.einsteinTimer).toBe(ABILITY_PARAMS.emc2.einsteinDuration)
    expect(p.ability.cooldown).toBe(ABILITY_PARAMS.emc2.cooldown)
    expect(abilityWorldScale(p)).toBeCloseTo(ABILITY_PARAMS.emc2.einsteinScale ?? 1, 5)
  })

  it('apos einsteinDuration frames volta a worldScale=1', () => {
    const p = makePlayer('emc2')
    input.set('ability', true); updateAbility(p, input, 1, ctx); input.update()
    input.set('ability', false)
    const dur = ABILITY_PARAMS.emc2.einsteinDuration ?? 0
    for (let i = 0; i < dur; i++) { updateAbility(p, input, 1, ctx); input.update() }
    expect(abilityWorldScale(p)).toBe(1)
  })
})

describe('updateAbility — amplificador (M3, desligado)', () => {
  it('e no-op: nada muda no estado', () => {
    const p = makePlayer('amplificador')
    const input = new FakeInput(); input.set('ability', true)
    const before = JSON.stringify(p.ability)
    updateAbility(p, input, 1, makeCtx(makeLevel(), []))
    expect(JSON.stringify(p.ability)).toBe(before)
  })
})
```

  Rodar `npm run test -- ability` → PASS (emc2 e o guard `!m1Implemented` ja existem do Passo 4). Verde = confirma duracao e scale.
  Commit: `test(ability): emc2 worldScale 0.25 por 210f + amplificador no-op`.

- [ ] **Passo 9 — Teste FALHA: `drawAbilityFx` desenha aura do escudo + bloco do builder.** Adicionar:

```ts
describe('drawAbilityFx', () => {
  it('com escudo ativo, desenha pelo menos 1 retangulo (aura)', () => {
    const p = makePlayer('escudo_governanca')
    const input = new FakeInput(); input.set('ability', true)
    updateAbility(p, input, 1, makeCtx(makeLevel(), []))
    const m = makeRenderer()
    drawAbilityFx(m.r, p)
    expect(m.rects).toBeGreaterThan(0)
  })

  it('com bloco do builder ativo, desenha pelo menos 1 retangulo (bloco)', () => {
    const p = makePlayer('builder'); p.x = 5 * TILE; p.y = 7 * TILE
    const input = new FakeInput(); input.set('ability', true)
    updateAbility(p, input, 1, makeCtx(makeLevel(), []))
    const m = makeRenderer()
    drawAbilityFx(m.r, p)
    expect(m.rects).toBeGreaterThan(0)
  })

  it('sem FX ativo nao desenha nada', () => {
    const p = makePlayer('salto_visionario')
    const m = makeRenderer()
    drawAbilityFx(m.r, p)
    expect(m.rects).toBe(0)
  })
})
```

  Rodar `npm run test -- ability` → FAIL: `drawAbilityFx is not a function` (ainda nao implementado).

- [ ] **Passo 10 — IMPLEMENTAR `drawAbilityFx` (world space).** Adicionar ao final de `ability.ts`:

```ts
// FX em ESPACO DE MUNDO (chamado pelo game dentro de beginWorld/endWorld).
// Aura do escudo (contorno azul ao redor do player) + bloco do builder (lime).
export function drawAbilityFx(r: Renderer, player: Player): void {
  const s = player.ability
  if (abilityHasShield(player)) {
    const pad = 4
    // contorno: 4 barras finas ao redor da AABB do player
    r.drawRect(player.x - pad, player.y - pad, player.w + pad * 2, 2, COLOR_BLUE)
    r.drawRect(player.x - pad, player.y + player.h + pad - 2, player.w + pad * 2, 2, COLOR_BLUE)
    r.drawRect(player.x - pad, player.y - pad, 2, player.h + pad * 2, COLOR_BLUE)
    r.drawRect(player.x + player.w + pad - 2, player.y - pad, 2, player.h + pad * 2, COLOR_BLUE)
  }
  if (s.builder) {
    r.drawRect(s.builder.col * TILE, s.builder.row * TILE, TILE, TILE, COLOR_LIME)
  }
  if (s.einsteinTimer > 0) {
    // marca leve do emc2: faixa no topo do player
    r.drawRect(player.x, player.y - 4, player.w, 2, COLOR_TEXT)
  }
}
```

  Rodar `npm run test -- ability` → PASS (todos os describes verdes).
  Rodar `npx tsc -p tsconfig.json --noEmit` → sem erros (confere imports `Player`/`Enemy`/`ABILITY_PARAMS` resolvidos; se `enemy.ts`/`ABILITY_PARAMS` ainda nao existirem no momento desta tarefa, o tsc do projeto inteiro acusara os modulos faltantes das OUTRAS tarefas — isso e esperado; o teste unitario da Task 3 nao depende deles e passa).
  Commit: `feat(ability): drawAbilityFx (aura do escudo + bloco do builder)`.

- [ ] **Passo 11 — Verificacao final da suite e do tipo.** Rodar:
  - `npm run test` → toda a suite Vitest passa (incluindo `ability.test.ts`).
  - `npx tsc -p tsconfig.json --noEmit` → 0 erros (apos [chars] e [enemy] integrados; em isolamento, so faltam os modulos das outras tarefas).
  Commit final: `chore(ability): modulo das 5 habilidades completo (Task 3)`.

#### Resumo de exports publicos de `src/game/ability.ts`
`AbilityState` (interface), `createAbilityState(char)`, `AbilityCtx` (interface), `updateAbility(player,input,dt,ctx)`, `abilityWorldScale(player)`, `abilityHasShield(player)`, `abilityConsumeShield(player)`, `abilityIFramesOnHit(player)`, `abilityKillsEnemy(player)`, `abilityBuilderTile(player)`, `drawAbilityFx(r,player)` — assinaturas EXATAS do contrato M1. Nenhum arquivo de outra tarefa e criado/recriado; consumo somente por import.

---

### Task 4: humanware.ts: medidor + Modo (DONO)

Cria o medidor Humanware e o "Modo" como funcoes puras sobre `HumanwareState`. Esta e a UNICA tarefa que cria `src/game/humanware.ts`. Nenhuma outra tarefa recria este arquivo: o game (Task de integracao), o HUD e a colisao/coin apenas IMPORTAM (`addMeter`, `tryActivate`, `updateHumanware`, `isActive`, `humanwareWorldScale`) — nunca redefinem.

Valores canonicos (§0.2): `HW_METER_MAX=1000`, ativacao so com medidor cheio E cooldown livre E sem Modo ativo, `modeTimer` inicial `300` frames, `cooldown` `300` frames ao terminar o Modo, mundo desacelera para `HW_TIME_SCALE=0.35`. Consome essas duas constantes de `src/engine/constants.ts` (ja existem no M0 — NAO redeclarar). Ganhos canonicos do medidor (moeda=8, stomp=60, reparar zona=250, lifecard=500, heart-orb=25) sao apenas valores que OUTRAS tarefas passam para `addMeter`; `addMeter` so soma+satura, nao conhece a origem.

NOTA DE CAMINHO (layout real do repo, diferente do alias do contrato): `constants.ts` = `src/engine/constants.ts` e exporta `TILE`/`VIEW_W`/`VIEW_H` (nao `TILE48`); `HW_TIME_SCALE` e `HW_METER_MAX` ja existem la. Mantenha o estilo M0: import relativo, sufixo `.ts` omitido, sem ponto-e-virgula final, aspas simples (ver `src/game/goal.ts` e `tests/unit/goal.test.ts`).

**Files:**
- Create: `src/game/humanware.ts` (DONO — todo o conteudo abaixo)
- Create: `tests/unit/humanware.test.ts` (DONO — testes desta tarefa)
- Modify: nenhum. (`src/engine/constants.ts` ja contem `HW_TIME_SCALE`/`HW_METER_MAX` do M0; NAO alterar.)
- Import/consumo (NAO criar/alterar): `src/engine/constants.ts` (so `import { HW_TIME_SCALE, HW_METER_MAX }`).

**Passos (TDD):**

- [ ] **Teste que falha** — criar `tests/unit/humanware.test.ts` cobrindo todas as funcoes e regras §0.2:

```ts
// tests/unit/humanware.test.ts
import { describe, it, expect } from 'vitest'
import {
  createHumanware,
  addMeter,
  tryActivate,
  updateHumanware,
  isActive,
  humanwareWorldScale,
} from '../../src/game/humanware'
import { HW_METER_MAX, HW_TIME_SCALE } from '../../src/engine/constants'

describe('createHumanware', () => {
  it('zera meter, modeTimer e cooldown', () => {
    const hw = createHumanware()
    expect(hw.meter).toBe(0)
    expect(hw.modeTimer).toBe(0)
    expect(hw.cooldown).toBe(0)
  })
})

describe('addMeter', () => {
  it('soma ganhos canonicos (moeda 8, stomp 60, heart-orb 25)', () => {
    const hw = createHumanware()
    addMeter(hw, 8)
    addMeter(hw, 60)
    addMeter(hw, 25)
    expect(hw.meter).toBe(93)
  })

  it('reparar zona (250) e lifecard (500) tambem somam', () => {
    const hw = createHumanware()
    addMeter(hw, 250)
    addMeter(hw, 500)
    expect(hw.meter).toBe(750)
  })

  it('satura em HW_METER_MAX (nunca passa de 1000)', () => {
    const hw = createHumanware()
    addMeter(hw, 900)
    addMeter(hw, 500)
    expect(hw.meter).toBe(HW_METER_MAX)
    expect(hw.meter).toBe(1000)
  })
})

describe('tryActivate', () => {
  it('ativa quando cheio e cooldown/modo livres: modeTimer=300, meter=0, retorna true', () => {
    const hw = createHumanware()
    hw.meter = HW_METER_MAX
    const ok = tryActivate(hw)
    expect(ok).toBe(true)
    expect(hw.modeTimer).toBe(300)
    expect(hw.meter).toBe(0)
  })

  it('nao ativa se o medidor nao esta cheio', () => {
    const hw = createHumanware()
    hw.meter = HW_METER_MAX - 1
    const ok = tryActivate(hw)
    expect(ok).toBe(false)
    expect(hw.modeTimer).toBe(0)
    expect(hw.meter).toBe(HW_METER_MAX - 1)
  })

  it('nao ativa enquanto o Modo ja esta ativo (modeTimer>0)', () => {
    const hw = createHumanware()
    hw.meter = HW_METER_MAX
    hw.modeTimer = 120
    const ok = tryActivate(hw)
    expect(ok).toBe(false)
    expect(hw.modeTimer).toBe(120)
    expect(hw.meter).toBe(HW_METER_MAX)
  })

  it('nao ativa enquanto cooldown>0, mesmo cheio', () => {
    const hw = createHumanware()
    hw.meter = HW_METER_MAX
    hw.cooldown = 50
    const ok = tryActivate(hw)
    expect(ok).toBe(false)
    expect(hw.modeTimer).toBe(0)
    expect(hw.meter).toBe(HW_METER_MAX)
  })
})

describe('updateHumanware', () => {
  it('decrementa modeTimer enquanto o Modo dura, sem mexer no cooldown', () => {
    const hw = createHumanware()
    hw.meter = HW_METER_MAX
    tryActivate(hw)
    updateHumanware(hw, 1)
    expect(hw.modeTimer).toBe(299)
    expect(hw.cooldown).toBe(0)
  })

  it('ao terminar o Modo (chega a <=0) inicia cooldown=300', () => {
    const hw = createHumanware()
    hw.modeTimer = 1
    updateHumanware(hw, 1)
    expect(hw.modeTimer).toBeLessThanOrEqual(0)
    expect(hw.cooldown).toBe(300)
  })

  it('com Modo inativo, decrementa cooldown (nao reinicia)', () => {
    const hw = createHumanware()
    hw.cooldown = 300
    updateHumanware(hw, 1)
    expect(hw.cooldown).toBe(299)
    expect(hw.modeTimer).toBe(0)
  })

  it('Modo dura exatamente 300 frames; no 300o tick termina e arma cooldown', () => {
    const hw = createHumanware()
    hw.meter = HW_METER_MAX
    tryActivate(hw)
    for (let i = 0; i < 299; i++) updateHumanware(hw, 1)
    expect(hw.modeTimer).toBe(1)
    expect(hw.cooldown).toBe(0)
    updateHumanware(hw, 1)
    expect(hw.modeTimer).toBeLessThanOrEqual(0)
    expect(hw.cooldown).toBe(300)
  })

  it('cooldown drena ao longo de 300 frames ate 0', () => {
    const hw = createHumanware()
    hw.cooldown = 300
    for (let i = 0; i < 300; i++) updateHumanware(hw, 1)
    expect(hw.cooldown).toBeLessThanOrEqual(0)
  })

  it('nao arma cooldown duas vezes na mesma transicao', () => {
    const hw = createHumanware()
    hw.modeTimer = 1
    updateHumanware(hw, 1) // termina, cooldown=300
    updateHumanware(hw, 1) // ja com modeTimer<=0: so drena cooldown
    expect(hw.cooldown).toBe(299)
  })
})

describe('isActive / humanwareWorldScale', () => {
  it('isActive reflete modeTimer>0', () => {
    const hw = createHumanware()
    expect(isActive(hw)).toBe(false)
    hw.modeTimer = 1
    expect(isActive(hw)).toBe(true)
  })

  it('worldScale = HW_TIME_SCALE (0.35) com Modo ativo, 1 fora', () => {
    const hw = createHumanware()
    expect(humanwareWorldScale(hw)).toBe(1)
    hw.modeTimer = 10
    expect(humanwareWorldScale(hw)).toBe(HW_TIME_SCALE)
    expect(humanwareWorldScale(hw)).toBe(0.35)
  })
})
```

- [ ] **Rodar (deve FALHAR)** — `npx vitest run tests/unit/humanware.test.ts`
  Saida esperada: erro de resolucao de modulo `Failed to resolve import "../../src/game/humanware"` / `Cannot find module` (arquivo ainda nao existe) → suite em vermelho.

- [ ] **Implementar** — criar `src/game/humanware.ts` com o conteudo COMPLETO abaixo:

```ts
// src/game/humanware.ts
import { HW_TIME_SCALE, HW_METER_MAX } from '../engine/constants'

/**
 * Estado do medidor Humanware e do "Modo".
 * - meter: carga atual (0..HW_METER_MAX). Cheio em 1000 (= 4 seg de 250).
 * - modeTimer: frames restantes do Modo ativo (>0 enquanto ativo).
 * - cooldown: frames restantes ate poder ativar de novo (armado ao fim do Modo).
 * Estilo: funcoes puras que MUTAM o HumanwareState recebido (sem retornar novo objeto),
 * exceto tryActivate (boolean) e os getters isActive/humanwareWorldScale.
 */
export interface HumanwareState {
  meter: number
  modeTimer: number
  cooldown: number
}

/** Cria um estado zerado: {0,0,0}. */
export function createHumanware(): HumanwareState {
  return { meter: 0, modeTimer: 0, cooldown: 0 }
}

/**
 * Adiciona n ao medidor, saturando em HW_METER_MAX.
 * Ganhos canonicos (chamados por outras tarefas): moeda=8, stomp=60,
 * reparar zona=250, lifecard=500, heart-orb=25.
 */
export function addMeter(hw: HumanwareState, n: number): void {
  hw.meter = Math.min(HW_METER_MAX, hw.meter + n)
}

/**
 * Tenta ativar o Modo. So ativa com o medidor CHEIO, sem cooldown e sem Modo ja ativo.
 * Em sucesso: modeTimer=300, meter=0, retorna true. Caso contrario nao muda nada e retorna false.
 */
export function tryActivate(hw: HumanwareState): boolean {
  if (hw.meter >= HW_METER_MAX && hw.cooldown <= 0 && hw.modeTimer <= 0) {
    hw.modeTimer = 300
    hw.meter = 0
    return true
  }
  return false
}

/**
 * Avanca os timers em dt frames.
 * - Se o Modo esta ativo: decrementa modeTimer; se cruzar para <=0 NESTA chamada, arma cooldown=300.
 * - Senao, se ha cooldown: decrementa cooldown (sem rearmar).
 * Garante que o cooldown e armado uma unica vez na transicao (modeTimer>0 -> <=0).
 */
export function updateHumanware(hw: HumanwareState, dt: number): void {
  if (hw.modeTimer > 0) {
    hw.modeTimer -= dt
    if (hw.modeTimer <= 0) {
      hw.cooldown = 300
    }
  } else if (hw.cooldown > 0) {
    hw.cooldown -= dt
  }
}

/** true enquanto o Modo esta ativo (modeTimer>0). */
export function isActive(hw: HumanwareState): boolean {
  return hw.modeTimer > 0
}

/** Escala de tempo do MUNDO: HW_TIME_SCALE (0.35) com Modo ativo, 1 fora. */
export function humanwareWorldScale(hw: HumanwareState): number {
  return isActive(hw) ? HW_TIME_SCALE : 1
}
```

- [ ] **Rodar (deve PASSAR)** — `npx vitest run tests/unit/humanware.test.ts`
  Saida esperada: 1 arquivo, todos os `it` verdes (createHumanware/addMeter/tryActivate/updateHumanware/isActive/humanwareWorldScale).

- [ ] **Typecheck** — `npx tsc -p tsconfig.json --noEmit`
  Saida esperada: sem erros (humanware.ts so importa `HW_TIME_SCALE`/`HW_METER_MAX` ja existentes; tipos batem).

- [ ] **Suite completa (nao regredir M0)** — `npm run test`
  Saida esperada: todos os testes existentes + o novo `humanware.test.ts` passam.

- [ ] **Commit** — `git add src/game/humanware.ts tests/unit/humanware.test.ts && git commit -m "M1 Task 4: humanware meter + Modo (HumanwareState, funcoes puras)"`

**Notas de contrato cumpridas:**
- Assinaturas EXATAS: `createHumanware()`, `addMeter(hw,n)`, `tryActivate(hw)`, `updateHumanware(hw,dt)`, `isActive(hw)`, `humanwareWorldScale(hw)` sobre `HumanwareState{meter,modeTimer,cooldown}`.
- §0.2: cap 1000 via `Math.min`; ativa so cheio+sem cooldown+sem modo; Modo 300f; cooldown 300f ao terminar; worldScale 0.35.
- O timer da fase PAUSADO no Modo e o `Math.min(humanwareWorldScale, abilityWorldScale, 1)` (§0.6) sao responsabilidade da Task de integracao (`game.ts`), que CONSOME `isActive`/`humanwareWorldScale` — fora do escopo desta tarefa.
- Esta tarefa NAO toca `input.ts`, `ability.ts`, `enemy.ts`, `player.ts`, `game.ts`, `hud.ts` nem `constants.ts`.

---

### Task 5: enemy.ts: Tolo (patrulha/stomp/freeze) (DONO)

Esta tarefa cria e e DONA UNICA de `src/game/enemy.ts`. Implementa o tipo `Enemy` e as quatro funcoes do contrato (`spawnEnemies`, `updateEnemy`, `isStomp`, `drawEnemy`) EXATAMENTE com as assinaturas das INTERFACES INTERNAS M1. So consome APIs M0 (nunca recria arquivos de outras tarefas). Nenhuma constante nova: usa `ENEMY_SPEED=1.25`, `COLOR_MAGENTA2`, `COLOR_INK` ja existentes em `constants.ts`.

Notas de design verificadas contra o codigo M0:
- `Body` (`{x,y,w,h,vx,vy,onGround}`) e `stepBody(body,level,dt)` vem de `src/engine/physics.ts`. `stepBody` ja aplica gravidade, integra X e resolve colisao por eixo; ao bater em parede solida ele ZERA `vx` (vide `resolveAxisX`). Por isso a inversao "ao bater em parede" e detectada por `e.vx === 0` apos `stepBody` enquanto o inimigo deveria estar andando.
- Solidos M0 = `ground`/`brick`/`block` (platform e one-way). A deteccao de borda le `level.tiles` direto (mesmo padrao do `physics.test.ts`), pois o helper `tileAt`/`isFullSolid` do physics e privado. "Iminencia de cair de borda" = nao ha tile solido logo abaixo da celula DIANTE dos pes do inimigo na direcao atual.
- `isStomp(player,e)` usa apenas campos do `Body` base do `Player` M0 (`vy`,`y`,`h`,`x`,`w`) — NAO toca em `iframes`/`ability` (campos que pertencem a outra tarefa), entao compila independentemente da ordem de merge.
- `spawnEnemies` consome `level.enemies` (`Array<{x,y,kind:string}>`): tanto `kind:'fool'` quanto `kind:'enemy'` viram `Enemy` com `kind:'tolo'`, `w=38`, `h=34`, `dir=-1`, `alive=true`, `frozen=false`, `vx=0`, `vy=0`, `onGround=false`.
- `dt` em `updateEnemy` ja chega escalado pelo mundo (`dt*ws`) — o game faz a escala; aqui so repassa para `stepBody`.

**Files:**
- Create: `src/game/enemy.ts` (DONO — unico criador deste arquivo)
- Create: `tests/unit/enemy.test.ts` (DONO — testes desta tarefa)
- Modify: nenhum. (Importa de `src/engine/physics.ts`, `src/engine/constants.ts`, `src/engine/render.ts`, `src/data/schema.ts`, `src/game/player.ts` — todos M0, apenas consumo via import.)

#### Passos (TDD)

- [ ] **1. Escrever teste que falha** — criar `tests/unit/enemy.test.ts` cobrindo: spawn (fool->tolo, dims/flags), patrulha anda em `ENEMY_SPEED*dir`, inverte ao bater em parede, inverte na iminencia de borda (nao cai), `!alive`/`frozen` nao movem, `isStomp` true ao cair sobre o topo e false em colisao lateral.

```ts
// tests/unit/enemy.test.ts
import { describe, it, expect } from 'vitest'
import { ENEMY_SPEED, TILE } from '../../src/engine/constants'
import type { ParsedLevel, TileType, CharacterDef } from '../../src/data/schema'
import {
  spawnEnemies,
  updateEnemy,
  isStomp,
  type Enemy,
} from '../../src/game/enemy'
import { createPlayer, type Player } from '../../src/game/player'

// Constroi um ParsedLevel a partir de um mapa ASCII (mesmo padrao do physics.test.ts).
function makeLevel(rows: string[], enemies: ParsedLevel['enemies'] = []): ParsedLevel {
  const heightTiles = rows.length
  const widthTiles = Math.max(...rows.map((r) => r.length))
  const tiles: TileType[][] = []
  for (let ty = 0; ty < heightTiles; ty++) {
    const row: TileType[] = []
    for (let tx = 0; tx < widthTiles; tx++) {
      const ch = rows[ty][tx] ?? '.'
      let t: TileType = 'empty'
      if (ch === '#') t = 'ground'
      else if (ch === 'B') t = 'brick'
      else if (ch === '?') t = 'block'
      else if (ch === '=') t = 'platform'
      row.push(t)
    }
    tiles.push(row)
  }
  return {
    widthTiles,
    heightTiles,
    widthPx: widthTiles * TILE,
    heightPx: heightTiles * TILE,
    tiles,
    playerSpawn: { x: 0, y: 0 },
    goal: { x: 0, y: 0 },
    coins: [],
    enemies,
  }
}

const CHAR: CharacterDef = {
  id: 'test',
  name: 'Test',
  abilityId: 'salto_visionario',
  color: '#fff',
  accent: '#000',
  hearts: 3,
  jumpVelMul: 1,
  walkMul: 1,
  runMul: 1,
  weightMul: 1,
}

describe('spawnEnemies', () => {
  it('converte fool e enemy em Enemy kind tolo com dims/flags canonicas', () => {
    const level = makeLevel(['....', '####'], [
      { x: 48, y: 0, kind: 'fool' },
      { x: 96, y: 0, kind: 'enemy' },
    ])
    const es = spawnEnemies(level)
    expect(es).toHaveLength(2)
    for (const e of es) {
      expect(e.kind).toBe('tolo')
      expect(e.w).toBe(38)
      expect(e.h).toBe(34)
      expect(e.dir).toBe(-1)
      expect(e.alive).toBe(true)
      expect(e.frozen).toBe(false)
      expect(e.vx).toBe(0)
      expect(e.vy).toBe(0)
      expect(e.onGround).toBe(false)
    }
    expect(es[0].x).toBe(48)
    expect(es[1].x).toBe(96)
  })
})

describe('updateEnemy patrulha', () => {
  it('anda em ENEMY_SPEED*dir sobre chao continuo', () => {
    // Chao continuo (linha 1). Inimigo sobre a celula (col 2), dir=-1 (esquerda).
    const level = makeLevel(['........', '########'])
    const e: Enemy = {
      x: 2 * TILE, y: TILE - 34, w: 38, h: 34, vx: 0, vy: 0,
      onGround: true, kind: 'tolo', dir: -1, alive: true, frozen: false,
    }
    const x0 = e.x
    updateEnemy(e, level, 1)
    // Andou para a esquerda ~ ENEMY_SPEED (sem parede no caminho).
    expect(e.x).toBeLessThan(x0)
    expect(x0 - e.x).toBeCloseTo(ENEMY_SPEED, 5)
    expect(e.dir).toBe(-1)
  })

  it('inverte dir ao bater em parede (vx zerou no stepBody)', () => {
    // Parede de brick na coluna 2; piso na linha 2. Inimigo encostando indo a direita (dir=1).
    const level = makeLevel(['..B.....', '..B.....', '########'])
    const e: Enemy = {
      // borda direita encostando na parede (x+w bem proximo de 2*TILE)
      x: 2 * TILE - 38 - 0.5, y: 2 * TILE - 34, w: 38, h: 34, vx: 0, vy: 0,
      onGround: true, kind: 'tolo', dir: 1, alive: true, frozen: false,
    }
    updateEnemy(e, level, 1)
    // Bateu na parede: stepBody zerou vx -> inverteu para a esquerda.
    expect(e.dir).toBe(-1)
  })

  it('inverte na iminencia de borda (nao cai do penhasco)', () => {
    // Chao so nas colunas 0..2 (linha 1). Coluna 3+ e abismo.
    // Inimigo na col 2 indo a direita (dir=1): a celula diante (col 3) nao tem chao abaixo -> inverte.
    const level = makeLevel(['........', '###.....'])
    const e: Enemy = {
      x: 2 * TILE, y: TILE - 34, w: 38, h: 34, vx: 0, vy: 0,
      onGround: true, kind: 'tolo', dir: 1, alive: true, frozen: false,
    }
    updateEnemy(e, level, 1)
    // Deve ter invertido ANTES de andar para o vazio.
    expect(e.dir).toBe(-1)
    // E nao deve ter avancado para alem do tile de chao (sem cair).
    expect(e.x).toBeLessThanOrEqual(2 * TILE + 1)
  })

  it('nao move quando alive=false', () => {
    const level = makeLevel(['........', '########'])
    const e: Enemy = {
      x: 2 * TILE, y: TILE - 34, w: 38, h: 34, vx: 9, vy: 9,
      onGround: true, kind: 'tolo', dir: -1, alive: false, frozen: false,
    }
    const x0 = e.x, y0 = e.y
    updateEnemy(e, level, 1)
    expect(e.x).toBe(x0)
    expect(e.y).toBe(y0)
  })

  it('nao move quando frozen=true (Modo Humanware)', () => {
    const level = makeLevel(['........', '########'])
    const e: Enemy = {
      x: 2 * TILE, y: TILE - 34, w: 38, h: 34, vx: 0, vy: 0,
      onGround: true, kind: 'tolo', dir: -1, alive: true, frozen: true,
    }
    const x0 = e.x, y0 = e.y
    updateEnemy(e, level, 1)
    expect(e.x).toBe(x0)
    expect(e.y).toBe(y0)
  })
})

describe('isStomp', () => {
  it('true quando o player cai (vy>0) sobre o topo do tolo', () => {
    const player: Player = createPlayer(CHAR, { x: 0, y: 0 })
    const e: Enemy = {
      x: 100, y: 200, w: 38, h: 34, vx: 0, vy: 0,
      onGround: false, kind: 'tolo', dir: -1, alive: true, frozen: false,
    }
    // Player descendo, pe acima da metade do tolo, sobreposto em X/Y.
    player.x = 100
    player.y = 200 - player.h + 4 // pe em ~204, abaixo do topo 200 mas acima de 200+17
    player.vy = 6
    expect(isStomp(player, e)).toBe(true)
  })

  it('false em colisao lateral (player subindo / pe baixo demais)', () => {
    const player: Player = createPlayer(CHAR, { x: 0, y: 0 })
    const e: Enemy = {
      x: 100, y: 200, w: 38, h: 34, vx: 0, vy: 0,
      onGround: false, kind: 'tolo', dir: -1, alive: true, frozen: false,
    }
    // Player na mesma altura, vy<=0 (nao descendo) -> nao e stomp.
    player.x = 100
    player.y = 200
    player.vy = -2
    expect(isStomp(player, e)).toBe(false)
  })
})
```

- [ ] **2. Rodar e ver falhar** — `npx vitest run tests/unit/enemy.test.ts`
  Saida esperada: falha de resolucao de modulo, algo como `Error: Failed to load url ../../src/game/enemy.ts` / `Cannot find module 'src/game/enemy.ts'` (arquivo ainda nao existe). RED.

- [ ] **3. Implementar** — criar `src/game/enemy.ts` com o codigo COMPLETO abaixo:

```ts
// src/game/enemy.ts
import type { Body } from '../engine/physics'
import { stepBody } from '../engine/physics'
import type { ParsedLevel, TileType } from '../data/schema'
import type { Renderer } from '../engine/render'
import type { Player } from './player'
import { ENEMY_SPEED, TILE, COLOR_MAGENTA2, COLOR_INK } from '../engine/constants'

// Inimigo "Tolo": patrulha horizontal sobre o chao, inverte em parede/borda,
// pode ser pisado (stomp) ou congelado pelo Modo Humanware.
export interface Enemy extends Body {
  kind: 'tolo'
  dir: 1 | -1
  alive: boolean
  frozen: boolean
}

// Solidos M0 (mesma definicao do physics.ts): ground/brick/block.
// platform e one-way e NAO conta como piso para deteccao de borda do tolo.
function isFullSolid(t: TileType): boolean {
  return t === 'ground' || t === 'brick' || t === 'block'
}

// Le o tile em coordenadas de grade; fora dos limites => 'empty'.
function tileAt(level: ParsedLevel, tx: number, ty: number): TileType {
  if (tx < 0 || ty < 0 || tx >= level.widthTiles || ty >= level.heightTiles) {
    return 'empty'
  }
  return level.tiles[ty][tx]
}

// Cria os inimigos a partir de level.enemies. fool|enemy -> Enemy kind 'tolo'.
// Dimensoes/flags canonicas: w=38, h=34, dir=-1, alive=true, frozen=false.
export function spawnEnemies(level: ParsedLevel): Enemy[] {
  return level.enemies.map((spawn) => ({
    x: spawn.x,
    y: spawn.y,
    w: 38,
    h: 34,
    vx: 0,
    vy: 0,
    onGround: false,
    kind: 'tolo',
    dir: -1,
    alive: true,
    frozen: false,
  }))
}

// Atualiza um tolo. dt ja chega escalado pelo mundo (dt*ws) — apenas repassa.
// - !alive ou frozen: nao move (return).
// - senao: define vx = ENEMY_SPEED*dir; integra com stepBody.
//   Inverte dir quando bate em parede (stepBody zerou vx) OU quando a proxima
//   celula a frente, na linha dos pes, nao tem chao solido abaixo (iminencia de borda).
export function updateEnemy(e: Enemy, level: ParsedLevel, dt: number): void {
  if (!e.alive || e.frozen) return

  // Deteccao de borda ANTES de mover: olha a celula diante dos pes na direcao atual.
  // footRow = linha logo abaixo do corpo (onde deveria haver chao).
  const footRow = Math.floor((e.y + e.h + 0.0001) / TILE)
  // Coluna a frente: borda dianteira do corpo na direcao dir, deslocada um tile.
  const frontEdgeX = e.dir > 0 ? e.x + e.w : e.x
  const aheadCol = Math.floor(frontEdgeX / TILE) + e.dir
  if (!isFullSolid(tileAt(level, aheadCol, footRow))) {
    // Sem chao a frente: inverte para nao cair do penhasco.
    e.dir = e.dir === 1 ? -1 : 1
  }

  e.vx = ENEMY_SPEED * e.dir
  stepBody(e, level, dt)

  // Bateu em parede solida: stepBody zerou vx. Inverte direcao.
  if (e.vx === 0) {
    e.dir = e.dir === 1 ? -1 : 1
  }
}

// Stomp: player descendo (vy>0) com os pes acima da metade superior do tolo,
// e com sobreposicao AABB. Usa apenas campos do Body base do Player.
export function isStomp(player: Player, e: Enemy): boolean {
  if (player.vy <= 0) return false
  const playerFoot = player.y + player.h
  if (playerFoot > e.y + e.h * 0.5) return false
  const overlapX = player.x < e.x + e.w && player.x + player.w > e.x
  const overlapY = player.y < e.y + e.h && player.y + player.h > e.y
  return overlapX && overlapY
}

// Placeholder em codigo: corpo COLOR_MAGENTA2 + detalhe COLOR_INK.
// Congelado (Modo Humanware): tom mais claro sobreposto para indicar pausa.
export function drawEnemy(r: Renderer, e: Enemy): void {
  if (!e.alive) return
  r.drawRect(e.x, e.y, e.w, e.h, COLOR_MAGENTA2)
  // Detalhe (faixa de "olhos") em COLOR_INK.
  r.drawRect(e.x + 6, e.y + 8, e.w - 12, 8, COLOR_INK)
  if (e.frozen) {
    // Sobreposicao clara semi-transparente nao existe no drawRect (so cor solida);
    // usa COLOR_TEXT-like via retangulo fino para sinalizar congelamento.
    r.drawRect(e.x, e.y, e.w, 4, '#f7f3f6')
  }
}
```

- [ ] **4. Rodar e ver passar** — `npx vitest run tests/unit/enemy.test.ts`
  Saida esperada: `Test Files 1 passed (1)` / `Tests 8 passed (8)`. GREEN.

- [ ] **5. Typecheck** — `npx tsc -p tsconfig.json --noEmit`
  Saida esperada: sem erros (exit 0). Confirma que so consumimos APIs M0 existentes (`Body`/`stepBody`, `ParsedLevel`/`TileType`, `Renderer`, `Player`, `ENEMY_SPEED`/`TILE`/`COLOR_MAGENTA2`/`COLOR_INK`).

- [ ] **6. Suite completa** — `npm run test`
  Saida esperada: todas as suites M0 + a nova `enemy.test.ts` passam (nenhum arquivo M0 foi modificado).

- [ ] **7. Commit** — `git add src/game/enemy.ts tests/unit/enemy.test.ts && git commit -m "feat(M1): enemy.ts Tolo (patrulha/stomp/freeze) + testes"`

---

### Task 6: player.ts: extensoes M1 + dano/coracoes/i-frames/morte (DONO de player.ts)

Esta tarefa e a UNICA que altera `src/game/player.ts`. Ela estende a interface `Player` (adiciona `iframes` e `ability`), ajusta `createPlayer`, adiciona 3 constantes em `constants.ts` e implementa `damagePlayer` / `respawnPlayer` / `tickPlayerTimers` EXATAMENTE como o contrato M1 (§0.5). NAO reescreve `updatePlayer` (so estende `createPlayer` e acrescenta funcoes). Consome (import, NUNCA recria) `AbilityState`, `createAbilityState`, `abilityHasShield` e `abilityConsumeShield` de `ability.ts` (dono = Task de ability).

NOTA de contrato (importante e ja resolvida pelo contrato): a assinatura canonica e `damagePlayer(player, fromX): 'blocked'|'hit'|'death'` — NAO recebe `spawn`. O texto §0.5 descreve o respawn como efeito logico; quem chama `respawnPlayer(player, spawn)` (que conhece o spawn) e o `game.ts` (Task de integracao) no ramo `'hit'`. Portanto `damagePlayer` aqui faz: decrementa heart, arma i-frames, knockback; se `hearts<=0` faz `lives-=1` e retorna `'hit'` (se ainda houver vidas, deixando o game posicionar via `respawnPlayer`) ou `'death'` (se `lives<=0`). `respawnPlayer` e exportado como funcao publica para o game usar. Implemente e documente exatamente assim — sem inventar um segundo overload de `damagePlayer` com `spawn`.

**Files:**
- Modify: `src/engine/constants.ts` (arquivo M0 — apenas ADICIONA `IFRAME_FRAMES=90`, `KNOCKBACK_VX=4.5`, `KNOCKBACK_VY=-8`; nao altera valores existentes)
- Modify: `src/game/player.ts` (DONO — estende `Player`, ajusta `createPlayer`, adiciona `damagePlayer`/`respawnPlayer`/`tickPlayerTimers`; NAO toca em `updatePlayer`)
- Import only (NAO criar/recriar — donos de outras tasks): `src/game/ability.ts` (tipos/helpers de habilidade)
- Test: `tests/unit/damage.test.ts` (novo teste desta task; NAO altera `tests/unit/player.test.ts`, que continua passando)

Dependencia: `ability.ts` precisa existir (Task de ability) para os imports `AbilityState`, `createAbilityState`, `abilityHasShield`, `abilityConsumeShield`. Em build paralelo, a Task de ability cria o arquivo; esta task apenas o consome.

---

#### Passo 1 — constants.ts: adicionar IFRAME_FRAMES / KNOCKBACK_VX / KNOCKBACK_VY (TDD)

- [ ] **Teste que falha** — adicionar ao final de `tests/unit/constants.test.ts` (ou criar bloco novo se o arquivo ja existe; aqui mostro o bloco a inserir):

```ts
import { IFRAME_FRAMES, KNOCKBACK_VX, KNOCKBACK_VY } from '../../src/engine/constants'

describe('constantes de dano (M1 §0.5)', () => {
  it('IFRAME_FRAMES === 90', () => {
    expect(IFRAME_FRAMES).toBe(90)
  })
  it('KNOCKBACK_VX === 4.5 e KNOCKBACK_VY === -8', () => {
    expect(KNOCKBACK_VX).toBe(4.5)
    expect(KNOCKBACK_VY).toBe(-8)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/constants.test.ts`
  Saida esperada (falha de compilacao/teste): `error TS2305: Module '"../../src/engine/constants"' has no exported member 'IFRAME_FRAMES'` (e idem para KNOCKBACK_*).

- [ ] **Implementar** — adicionar ao final de `src/engine/constants.ts` (apos a secao Humanware, antes da paleta ou ao fim; aqui apos a linha de `HW_METER_MAX`):

```ts
// Dano / i-frames / knockback (M1 §0.5)
export const IFRAME_FRAMES = 90
export const KNOCKBACK_VX = 4.5
export const KNOCKBACK_VY = -8
```

- [ ] **PASS** — `npx vitest run tests/unit/constants.test.ts` verde.
- [ ] **commit** — `feat(constants): IFRAME_FRAMES/KNOCKBACK_VX/KNOCKBACK_VY (M1 dano)`

---

#### Passo 2 — player.ts: estender Player com iframes + ability e ajustar createPlayer (TDD)

- [ ] **Teste que falha** — criar `tests/unit/damage.test.ts` com o cabecalho + o primeiro bloco (createPlayer):

```ts
// tests/unit/damage.test.ts
// dt=1 (convencao por-frame, E1). Cobre Player.iframes/ability, damagePlayer,
// respawnPlayer e tickPlayerTimers (M1 §0.5).
import { describe, it, expect } from 'vitest'
import {
  createPlayer,
  damagePlayer,
  respawnPlayer,
  tickPlayerTimers,
  type Player,
} from '../../src/game/player'
import { CHARACTERS } from '../../src/data/characters'
import { createAbilityState } from '../../src/game/ability'
import type { SpawnPoint } from '../../src/data/schema'
import { IFRAME_FRAMES, KNOCKBACK_VX, KNOCKBACK_VY } from '../../src/engine/constants'

const SPAWN: SpawnPoint = { x: 2 * 48, y: 8 * 48 }

function mkPlayer(charId = 'renan'): Player {
  return createPlayer(CHARACTERS[charId], SPAWN)
}

describe('createPlayer — extensoes M1', () => {
  it('inicia iframes=0 e ability=createAbilityState(char)', () => {
    const p = mkPlayer('renan')
    expect(p.iframes).toBe(0)
    // ability zerada, id = abilityId do char
    expect(p.ability.id).toBe(CHARACTERS['renan'].abilityId)
    expect(p.ability.cooldown).toBe(0)
    expect(p.ability.active).toBe(false)
    expect(p.ability.airJumps).toBe(0)
    expect(p.ability.shield).toBe(false)
    // shape canonica de createAbilityState (mesmos campos)
    const fresh = createAbilityState(CHARACTERS['renan'])
    expect(p.ability).toEqual(fresh)
  })

  it('mantem stats M0 intactos (hearts, lives, facing, w/h)', () => {
    const p = mkPlayer('julio') // hearts=4
    expect(p.hearts).toBe(CHARACTERS['julio'].hearts)
    expect(p.lives).toBe(3)
    expect(p.facing).toBe(1)
    expect(p.w).toBe(34)
    expect(p.h).toBe(42)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/damage.test.ts`
  Saida esperada: erros TS — `Property 'iframes' does not exist on type 'Player'`, `Property 'ability' does not exist on type 'Player'`, e `'damagePlayer'/'respawnPlayer'/'tickPlayerTimers' is not exported`.

- [ ] **Implementar** — em `src/game/player.ts`: (a) adicionar imports de tipo/helper de `ability.ts` e das constantes novas; (b) estender a interface `Player`; (c) ajustar `createPlayer`. NAO mexer em `updatePlayer`.

  (c1) Trocar o bloco de imports do topo de constants para incluir as 3 novas + manter as existentes:

```ts
import {
  PLAYER_W,
  PLAYER_H,
  WALK_ACCEL,
  RUN_ACCEL,
  WALK_MAX,
  RUN_MAX,
  GROUND_DECEL,
  AIR_DECEL,
  JUMP_VEL,
  COYOTE_FRAMES,
  JUMP_BUFFER_FRAMES,
  START_LIVES,
  IFRAME_FRAMES,
  KNOCKBACK_VX,
  KNOCKBACK_VY,
} from '../engine/constants'
```

  (c2) Adicionar imports de `ability.ts` (logo apos o import de schema):

```ts
import type { AbilityState } from './ability'
import { createAbilityState, abilityHasShield, abilityConsumeShield } from './ability'
```

  (c3) Estender a interface `Player` (substituir a interface atual por esta — mesmos campos M0 + os 2 novos):

```ts
export interface Player extends Body {
  char: CharacterDef
  facing: 1 | -1
  coyote: number
  jumpBuffer: number
  lives: number
  hearts: number
  iframes: number
  ability: AbilityState
}
```

  (c4) Ajustar `createPlayer` para iniciar `iframes` e `ability` (manter resto identico):

```ts
export function createPlayer(char: CharacterDef, spawn: SpawnPoint): Player {
  return {
    x: spawn.x,
    y: spawn.y,
    w: PLAYER_W,
    h: PLAYER_H,
    vx: 0,
    vy: 0,
    onGround: false,
    char,
    facing: 1,
    coyote: 0,
    jumpBuffer: 0,
    lives: START_LIVES,
    hearts: char.hearts,
    iframes: 0,
    ability: createAbilityState(char),
  }
}
```

- [ ] **PASS** — `npx vitest run tests/unit/damage.test.ts tests/unit/player.test.ts` (o teste de createPlayer M1 passa; `player.test.ts` M0 continua verde).
- [ ] **commit** — `feat(player): estende Player com iframes+ability (M1)`

---

#### Passo 3 — damagePlayer: dano normal reduz heart + arma i-frames + knockback (TDD)

- [ ] **Teste que falha** — adicionar bloco em `tests/unit/damage.test.ts`:

```ts
describe('damagePlayer — dano normal', () => {
  it('reduz 1 heart, arma IFRAME_FRAMES e aplica knockback (inimigo a direita -> empurra esquerda)', () => {
    const p = mkPlayer('renan') // hearts=3
    p.x = 100
    const r = damagePlayer(p, 200) // fromX a direita do player
    expect(r).toBe('hit')
    expect(p.hearts).toBe(2)
    expect(p.iframes).toBe(IFRAME_FRAMES)
    expect(p.vx).toBe(-KNOCKBACK_VX) // player.x(100) < fromX(200) -> -KNOCKBACK_VX
    expect(p.vy).toBe(KNOCKBACK_VY)
  })

  it('inimigo a esquerda empurra o player para a direita (+KNOCKBACK_VX)', () => {
    const p = mkPlayer('renan')
    p.x = 300
    const r = damagePlayer(p, 50) // fromX a esquerda
    expect(r).toBe('hit')
    expect(p.vx).toBe(KNOCKBACK_VX)
    expect(p.vy).toBe(KNOCKBACK_VY)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/damage.test.ts`
  Saida esperada: `damagePlayer is not exported` / falha de tipo (funcao ainda nao existe).

- [ ] **Implementar** — adicionar ao final de `src/game/player.ts` (apos `updatePlayer`):

```ts
// --- M1 §0.5: dano, coracoes, i-frames, morte ---

// Aplica dano ao player vindo de `fromX` (posicao da fonte do dano, p.ex. e.x).
// Retorno:
//   'blocked' -> dano absorvido por i-frames ativos OU pelo escudo (escudo e consumido)
//   'hit'     -> levou dano e continua na fase (perdeu heart, ou perdeu vida mas ainda tem >0 vidas)
//   'death'   -> perdeu o ultimo heart e a ultima vida (lives chegou a 0) -> game vai para 'over'
// O reposicionamento no spawn (respawnPlayer) e feito pelo game.ts no ramo 'hit'
// quando o player perdeu uma vida (hearts ja foi resetado para char.hearts aqui).
export function damagePlayer(player: Player, fromX: number): 'blocked' | 'hit' | 'death' {
  // i-frames ativos: imune.
  if (player.iframes > 0) return 'blocked'

  // Escudo (escudo_governanca): absorve o proximo dano e e consumido; arma i-frames.
  if (abilityHasShield(player)) {
    abilityConsumeShield(player)
    player.iframes = IFRAME_FRAMES
    return 'blocked'
  }

  // Dano efetivo: perde 1 heart, arma i-frames, knockback (longe da fonte).
  player.hearts -= 1
  player.iframes = IFRAME_FRAMES
  player.vx = player.x < fromX ? -KNOCKBACK_VX : KNOCKBACK_VX
  player.vy = KNOCKBACK_VY

  // Hearts esgotados: perde uma vida.
  if (player.hearts <= 0) {
    player.lives -= 1
    if (player.lives > 0) {
      // Ainda ha vidas: reabastece hearts e mantem i-frames (game reposiciona no spawn).
      player.hearts = player.char.hearts
      player.iframes = IFRAME_FRAMES
      return 'hit'
    }
    return 'death'
  }

  return 'hit'
}
```

- [ ] **PASS** — `npx vitest run tests/unit/damage.test.ts`.
- [ ] **commit** — `feat(player): damagePlayer dano normal + knockback + i-frames (M1 §0.5)`

---

#### Passo 4 — damagePlayer: i-frames bloqueiam dano repetido (TDD)

- [ ] **Teste que falha** — adicionar bloco:

```ts
describe('damagePlayer — i-frames bloqueiam', () => {
  it('com iframes>0 retorna "blocked" sem alterar hearts, vx nem vy', () => {
    const p = mkPlayer('renan')
    p.iframes = 30
    p.hearts = 3
    p.vx = 1.23
    p.vy = -4.56
    const r = damagePlayer(p, 9999)
    expect(r).toBe('blocked')
    expect(p.hearts).toBe(3) // inalterado
    expect(p.iframes).toBe(30) // NAO rearmado (ramo de i-frames sai antes)
    expect(p.vx).toBe(1.23) // sem knockback
    expect(p.vy).toBe(-4.56)
  })

  it('um segundo hit no mesmo frame do primeiro e bloqueado (i-frames recem armados)', () => {
    const p = mkPlayer('renan') // hearts=3
    expect(damagePlayer(p, 0)).toBe('hit') // hearts 3->2, iframes=90
    expect(damagePlayer(p, 0)).toBe('blocked') // bloqueado por i-frames
    expect(p.hearts).toBe(2)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/damage.test.ts`
  Saida esperada: PASS imediato (o ramo `iframes>0 -> 'blocked'` ja foi implementado no Passo 3). Se algum subteste falhar, e bug de implementacao — investigar a ordem dos ramos em `damagePlayer`.

- [ ] **Implementar** — nenhuma mudanca de codigo nova; este passo trava o comportamento de i-frames ja escrito no Passo 3.
- [ ] **PASS** — `npx vitest run tests/unit/damage.test.ts`.
- [ ] **commit** — `test(player): i-frames bloqueiam dano repetido (M1)`

---

#### Passo 5 — damagePlayer: escudo bloqueia e e consumido (TDD)

- [ ] **Teste que falha** — adicionar bloco. O escudo e ligado escrevendo diretamente nos campos canonicos de `AbilityState` (`shield`, `shieldTimer`), que e exatamente o que `abilityHasShield` le:

```ts
import { abilityHasShield } from '../../src/game/ability'

describe('damagePlayer — escudo (escudo_governanca)', () => {
  it('escudo ativo absorve o dano, e consumido e arma i-frames; hearts intacto', () => {
    const p = mkPlayer('julio') // hearts=4
    // arma escudo via campos canonicos do AbilityState
    p.ability.shield = true
    p.ability.shieldTimer = 180
    expect(abilityHasShield(p)).toBe(true)

    const r = damagePlayer(p, 0)
    expect(r).toBe('blocked')
    expect(p.hearts).toBe(4) // dano absorvido
    expect(p.iframes).toBe(IFRAME_FRAMES) // i-frames armados em ambos os ramos
    expect(abilityHasShield(p)).toBe(false) // escudo consumido (shield=false, shieldTimer=0)
  })

  it('apos consumir o escudo, o proximo dano (com i-frames ja zerados) reduz heart', () => {
    const p = mkPlayer('julio')
    p.ability.shield = true
    p.ability.shieldTimer = 180
    damagePlayer(p, 0) // absorve, iframes=90
    p.iframes = 0 // simula i-frames expirados
    const r = damagePlayer(p, 0)
    expect(r).toBe('hit')
    expect(p.hearts).toBe(3)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/damage.test.ts`
  Saida esperada: PASS (ramo do escudo ja implementado no Passo 3). Se falhar, conferir que o ramo do escudo vem DEPOIS do ramo de i-frames e ANTES do dano efetivo.

- [ ] **Implementar** — sem novo codigo; passo trava o comportamento do escudo (consumo + i-frames) ja escrito.
- [ ] **PASS** — `npx vitest run tests/unit/damage.test.ts`.
- [ ] **commit** — `test(player): escudo bloqueia e e consumido (M1 §0.5)`

---

#### Passo 6 — damagePlayer: hearts0 -> lives-1 + respawn; lives0 -> "death" (TDD)

- [ ] **Teste que falha** — adicionar bloco:

```ts
describe('damagePlayer — morte de fase e morte global', () => {
  it('hearts chega a 0 com vidas restantes -> "hit", perde 1 vida e hearts reabastecidos', () => {
    const p = mkPlayer('einstein') // hearts=2, lives=3
    expect(damagePlayer(p, 0)).toBe('hit') // hearts 2->1
    expect(p.hearts).toBe(1)
    p.iframes = 0
    const r = damagePlayer(p, 0) // hearts 1->0 -> perde vida
    expect(r).toBe('hit')
    expect(p.lives).toBe(2) // 3 -> 2
    expect(p.hearts).toBe(CHARACTERS['einstein'].hearts) // reabastecido (=2)
    expect(p.iframes).toBe(IFRAME_FRAMES) // i-frames de respawn
  })

  it('hearts chega a 0 na ULTIMA vida -> "death" (lives=0)', () => {
    const p = mkPlayer('einstein') // hearts=2
    p.lives = 1
    expect(damagePlayer(p, 0)).toBe('hit') // 2->1
    p.iframes = 0
    const r = damagePlayer(p, 0) // 1->0 hearts, 1->0 lives
    expect(r).toBe('death')
    expect(p.lives).toBe(0)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/damage.test.ts`
  Saida esperada: PASS (ramo de morte ja implementado no Passo 3). Se algum caso falhar, revisar `if (player.hearts <= 0) { player.lives -= 1; ... }`.

- [ ] **Implementar** — sem novo codigo; este passo trava o fluxo `hearts0 -> lives-1 + respawn` vs `lives0 -> 'death'` ja escrito.
- [ ] **PASS** — `npx vitest run tests/unit/damage.test.ts`.
- [ ] **commit** — `test(player): hearts0->vida-1+respawn e lives0->death (M1 §0.5)`

---

#### Passo 7 — respawnPlayer + tickPlayerTimers (TDD)

- [ ] **Teste que falha** — adicionar bloco:

```ts
describe('respawnPlayer', () => {
  it('reposiciona no spawn, reabastece hearts e arma i-frames; zera velocidade', () => {
    const p = mkPlayer('julio') // hearts=4
    p.x = 999
    p.y = 999
    p.hearts = 1
    p.vx = 5
    p.vy = -3
    p.iframes = 0
    const spawn: SpawnPoint = { x: 2 * 48, y: 8 * 48 }
    respawnPlayer(p, spawn)
    expect(p.x).toBe(spawn.x)
    expect(p.y).toBe(spawn.y)
    expect(p.hearts).toBe(CHARACTERS['julio'].hearts) // =4
    expect(p.iframes).toBe(IFRAME_FRAMES)
    expect(p.vx).toBe(0)
    expect(p.vy).toBe(0)
  })
})

describe('tickPlayerTimers', () => {
  it('decrementa iframes por dt e clampa em 0', () => {
    const p = mkPlayer('renan')
    p.iframes = 2
    tickPlayerTimers(p, 1)
    expect(p.iframes).toBe(1)
    tickPlayerTimers(p, 1)
    expect(p.iframes).toBe(0)
    tickPlayerTimers(p, 1)
    expect(p.iframes).toBe(0) // nao fica negativo
  })

  it('com iframes=0 permanece 0', () => {
    const p = mkPlayer('renan')
    p.iframes = 0
    tickPlayerTimers(p, 1)
    expect(p.iframes).toBe(0)
  })
})
```

- [ ] **Rodar** — `npx vitest run tests/unit/damage.test.ts`
  Saida esperada: `respawnPlayer is not exported` / `tickPlayerTimers is not exported`.

- [ ] **Implementar** — adicionar ao final de `src/game/player.ts` (apos `damagePlayer`):

```ts
// Reposiciona o player no spawn e reabastece a fase (chamado pelo game apos 'hit'
// quando uma vida foi perdida). Zera velocidade e arma i-frames de respawn.
export function respawnPlayer(player: Player, spawn: SpawnPoint): void {
  player.x = spawn.x
  player.y = spawn.y
  player.vx = 0
  player.vy = 0
  player.onGround = false
  player.hearts = player.char.hearts
  player.iframes = IFRAME_FRAMES
}

// Decrementa timers por-frame do player (i-frames), clampando em 0.
// Chamado pelo game no tempo do PLAYER (dt escala 1).
export function tickPlayerTimers(player: Player, dt: number): void {
  if (player.iframes > 0) {
    player.iframes = Math.max(0, player.iframes - dt)
  }
}
```

- [ ] **PASS** — `npx vitest run tests/unit/damage.test.ts`.
- [ ] **commit** — `feat(player): respawnPlayer + tickPlayerTimers (M1 §0.5)`

---

#### Passo 8 — verificacao final (typecheck + suite + nao-regressao)

- [ ] **Rodar typecheck** — `npx tsc -p tsconfig.json --noEmit`
  Saida esperada: sem erros (depende de `ability.ts` ja existir com os exports `AbilityState`, `createAbilityState`, `abilityHasShield`, `abilityConsumeShield`).
- [ ] **Rodar suite** — `npm run test`
  Saida esperada: `tests/unit/player.test.ts` (M0, intacto) e `tests/unit/damage.test.ts` (M1) verdes; demais suites M0 inalteradas.
- [ ] **Confirmar nao-regressao**: `updatePlayer` nao foi tocado (so `createPlayer` e funcoes novas adicionadas); `player.test.ts` permanece sem edicoes.
- [ ] **commit** — `chore(player): typecheck + suite verdes para extensoes de dano M1`

---

#### Notas de aderencia ao contrato

- Assinaturas exatas: `damagePlayer(player: Player, fromX: number): 'blocked'|'hit'|'death'`, `respawnPlayer(player, spawn): void`, `tickPlayerTimers(player, dt): void`. `Player` ganha SOMENTE `iframes: number` e `ability: AbilityState` (importado de `ability.ts`).
- Ordem dos ramos em `damagePlayer` (canonica §0.5): (1) `iframes>0` -> `'blocked'`; (2) `abilityHasShield` -> consome escudo + `iframes=IFRAME_FRAMES` -> `'blocked'`; (3) dano efetivo (heart-1, i-frames, knockback) e ramo de morte. I-frames armados em AMBOS os ramos que levam dano/escudo.
- Valores §0: `IFRAME_FRAMES=90`, `KNOCKBACK_VX=4.5`, `KNOCKBACK_VY=-8`. Knockback `vx` = afasta-se da fonte: `player.x < fromX ? -KNOCKBACK_VX : KNOCKBACK_VX`; `vy = KNOCKBACK_VY`.
- `updatePlayer` NAO foi reescrito. `constants.ts` so recebeu ADICOES. `ability.ts`, `enemy.ts`, `input.ts`, `game.ts`, `hud.ts`, etc. NAO foram criados nem editados por esta task (apenas `ability.ts` e consumido por import).
- Texto pt-BR, codigo TS. Zero placeholders/TBD.

---

### Task 7: selectScreen.ts: tela de selecao (DONO)

Esta tarefa e a UNICA dona de `src/ui/selectScreen.ts`. Cria `SelectState`, `createSelect`, `updateSelect` e `drawSelect` EXATAMENTE com as assinaturas das INTERFACES INTERNAS M1. Consome (apenas import) tipos/funcoes de outras tarefas — nunca recria nem redefine `Input`/`InputAction`, `CharacterDef`, `Renderer`, `drawPlaceholder` ou cores. A navegacao usa `pressed('left')`/`pressed('right')` com wrap; `pressed('jump') || pressed('confirm')` retorna o `id` do char no `index`; caso contrario retorna `null`. O `drawSelect` desenha os 5 personagens (via `drawPlaceholder` + nome + habilidade) com cursor sobre o selecionado.

Notas de dependencia (consumo, NAO criacao):
- `Input` e `InputAction` vem de `../engine/input` (a acao `'confirm'` e adicionada pela tarefa dona de `input.ts`; aqui apenas se consome o tipo). `'left'`, `'right'`, `'jump'` ja existem no M0.
- `CharacterDef` vem de `../data/schema` (M0).
- `Renderer` vem de `../engine/render` (M0); texto e desenhado via `r.ctx.fillText` (mesmo padrao do `hud.ts` do M0).
- `drawPlaceholder` vem de `../game/sprites` (M0).
- Cores canonicas (`COLOR_BG`, `COLOR_TEXT`, `COLOR_MAGENTA`, `COLOR_LIME`, `COLOR_SURFACE`) vem de `../engine/constants` (M0).
- A tarefa de `game.ts` (integracao) consome `createSelect`/`updateSelect`/`drawSelect`. Esta tarefa NAO toca em `game.ts`.

**Files:**
- Create: `src/ui/selectScreen.ts` (DONO — unico arquivo criado por esta tarefa)
- Modify: (nenhum arquivo M0 e alterado por esta tarefa)
- Test: `tests/unit/selectScreen.test.ts` (DONO)
- Import/consumo (NUNCA recriar): `src/engine/input.ts` (`Input`, `InputAction`), `src/data/schema.ts` (`CharacterDef`), `src/engine/render.ts` (`Renderer`), `src/game/sprites.ts` (`drawPlaceholder`), `src/engine/constants.ts` (cores)

Passos TDD:

- [ ] **Teste que falha (escreve `tests/unit/selectScreen.test.ts`).** O arquivo `src/ui/selectScreen.ts` ainda nao existe, entao o import quebra e o teste falha. FakeInput segue o padrao canonico do `player.test.ts` do M0 (`down`/`prev` sets, `pressed` = edge), implementando a interface `Input` e tipado contra `InputAction`.

```ts
// tests/unit/selectScreen.test.ts
import { describe, it, expect } from 'vitest'
import {
  createSelect,
  updateSelect,
  drawSelect,
  type SelectState,
} from '../../src/ui/selectScreen'
import type { Input, InputAction } from '../../src/engine/input'
import type { CharacterDef, AbilityId } from '../../src/data/schema'
import type { Renderer } from '../../src/engine/render'
import { COLOR_MAGENTA, COLOR_BLUE } from '../../src/engine/constants'

// FakeInput canonico (mesmo padrao do player.test.ts): edge = down agora e nao no frame anterior.
class FakeInput implements Input {
  private down = new Set<InputAction>()
  private prev = new Set<InputAction>()

  set(a: InputAction, v: boolean): void {
    if (v) this.down.add(a)
    else this.down.delete(a)
  }

  isDown(a: InputAction): boolean {
    return this.down.has(a)
  }

  pressed(a: InputAction): boolean {
    return this.down.has(a) && !this.prev.has(a)
  }

  update(): void {
    this.prev = new Set(this.down)
  }

  attach(): void {
    /* no-op nos testes */
  }
}

// Dispara um edge (pressionar -> ler -> soltar) sem persistir hold.
function tap(sel: SelectState, input: FakeInput, chars: CharacterDef[], a: InputAction): string | null {
  input.set(a, true)
  const out = updateSelect(sel, input, chars)
  input.update()
  input.set(a, false)
  input.update()
  return out
}

// 3 chars de teste suficientes para exercitar wrap (index 0..2).
function makeChar(id: string, abilityId: AbilityId): CharacterDef {
  return {
    id,
    name: id.toUpperCase(),
    abilityId,
    color: COLOR_MAGENTA,
    accent: COLOR_BLUE,
    hearts: 3,
    jumpVelMul: 1,
    walkMul: 1,
    runMul: 1,
    weightMul: 1,
  }
}

const CHARS: CharacterDef[] = [
  makeChar('renan', 'salto_visionario'),
  makeChar('dante', 'dash_criativo'),
  makeChar('julio', 'escudo_governanca'),
]

// Renderer stub: grava chamadas drawRect e captura ctx.fillText/font.
function makeFakeRenderer(): { renderer: Renderer; rects: number; texts: string[] } {
  const texts: string[] = []
  let rects = 0
  const ctx = {
    save() {},
    restore() {},
    fillRect() {},
    fillText(t: string) {
      texts.push(t)
    },
    set fillStyle(_v: string) {},
    get fillStyle() {
      return ''
    },
    set font(_v: string) {},
    get font() {
      return ''
    },
    set textAlign(_v: string) {},
    get textAlign() {
      return ''
    },
    set textBaseline(_v: string) {},
    get textBaseline() {
      return ''
    },
  } as unknown as CanvasRenderingContext2D
  const renderer: Renderer = {
    ctx,
    clear() {},
    beginWorld() {},
    endWorld() {},
    drawRect() {
      rects++
    },
    present() {},
  }
  return {
    renderer,
    get rects() {
      return rects
    },
    texts,
  }
}

describe('createSelect', () => {
  it('inicia com index 0', () => {
    expect(createSelect()).toEqual({ index: 0 })
  })
})

describe('updateSelect — navegacao com wrap', () => {
  it('right avanca o index', () => {
    const sel = createSelect()
    const input = new FakeInput()
    tap(sel, input, CHARS, 'right')
    expect(sel.index).toBe(1)
  })

  it('left a partir de 0 faz wrap para o ultimo', () => {
    const sel = createSelect()
    const input = new FakeInput()
    tap(sel, input, CHARS, 'left')
    expect(sel.index).toBe(CHARS.length - 1) // 2
  })

  it('right a partir do ultimo faz wrap para 0', () => {
    const sel: SelectState = { index: CHARS.length - 1 }
    const input = new FakeInput()
    tap(sel, input, CHARS, 'right')
    expect(sel.index).toBe(0)
  })

  it('sem nenhuma tecla retorna null e nao muda index', () => {
    const sel = createSelect()
    const input = new FakeInput()
    const out = updateSelect(sel, input, CHARS)
    input.update()
    expect(out).toBeNull()
    expect(sel.index).toBe(0)
  })

  it('hold (sem novo edge) nao move o index — usa pressed, nao isDown', () => {
    const sel = createSelect()
    const input = new FakeInput()
    input.set('right', true)
    expect(updateSelect(sel, input, CHARS)).toBeNull()
    input.update()
    expect(sel.index).toBe(1)
    // segundo frame com a tecla AINDA segurada: pressed=false, nao avanca
    expect(updateSelect(sel, input, CHARS)).toBeNull()
    input.update()
    expect(sel.index).toBe(1)
  })
})

describe('updateSelect — confirmacao', () => {
  it('jump retorna o id do char no index atual', () => {
    const sel: SelectState = { index: 1 }
    const input = new FakeInput()
    const out = tap(sel, input, CHARS, 'jump')
    expect(out).toBe('dante')
  })

  it('confirm retorna o id do char no index atual', () => {
    const sel: SelectState = { index: 2 }
    const input = new FakeInput()
    const out = tap(sel, input, CHARS, 'confirm')
    expect(out).toBe('julio')
  })

  it('confirmar no index 0 retorna o primeiro id', () => {
    const sel = createSelect()
    const input = new FakeInput()
    const out = tap(sel, input, CHARS, 'jump')
    expect(out).toBe('renan')
  })
})

describe('drawSelect', () => {
  it('desenha um placeholder por char (drawRect chamado) e os nomes + habilidades', () => {
    const fake = makeFakeRenderer()
    const sel = createSelect()
    drawSelect(fake.renderer, sel, CHARS)
    // drawPlaceholder usa drawRect varias vezes por char -> ao menos 1 por char.
    expect(fake.rects).toBeGreaterThanOrEqual(CHARS.length)
    // Nome de cada char aparece em algum fillText.
    for (const c of CHARS) {
      expect(fake.texts.some((t) => t.includes(c.name))).toBe(true)
    }
    // Habilidade de cada char aparece em algum fillText.
    for (const c of CHARS) {
      expect(fake.texts.some((t) => t.includes(c.abilityId))).toBe(true)
    }
  })

  it('nao lanca com lista de 1 char', () => {
    const fake = makeFakeRenderer()
    const sel = createSelect()
    expect(() => drawSelect(fake.renderer, sel, [CHARS[0]])).not.toThrow()
  })
})
```

- [ ] **Rodar (falha esperada).** Comando: `npm run test -- selectScreen`.
  Saida esperada (resumida): `Error: Failed to load url ../../src/ui/selectScreen (resolved id .../src/ui/selectScreen.ts). Does the file exist?` — suite falha porque o modulo ainda nao existe.

- [ ] **Implementar `src/ui/selectScreen.ts` (CODIGO TS COMPLETO).**

```ts
// src/ui/selectScreen.ts
// Tela de SELECAO de personagem (M1, Task 7 — DONO).
// Navega left/right com wrap; jump|confirm retorna o id do char no index; senao null.
// drawSelect desenha cada char (drawPlaceholder + nome + habilidade) com cursor.
import type { Input } from '../engine/input'
import type { CharacterDef } from '../data/schema'
import type { Renderer } from '../engine/render'
import { drawPlaceholder } from '../game/sprites'
import {
  VIEW_W,
  VIEW_H,
  COLOR_BG,
  COLOR_SURFACE,
  COLOR_TEXT,
  COLOR_MAGENTA,
  COLOR_LIME,
} from '../engine/constants'

export interface SelectState {
  index: number
}

export function createSelect(): SelectState {
  return { index: 0 }
}

// Avanca/recua o cursor com wrap; em jump|confirm retorna chars[index].id; senao null.
export function updateSelect(
  sel: SelectState,
  input: Input,
  chars: CharacterDef[],
): string | null {
  const n = chars.length
  if (n === 0) return null

  if (input.pressed('left')) {
    sel.index = (sel.index - 1 + n) % n
  }
  if (input.pressed('right')) {
    sel.index = (sel.index + 1) % n
  }

  // Mantem index valido caso a lista mude de tamanho entre frames.
  if (sel.index < 0 || sel.index >= n) {
    sel.index = ((sel.index % n) + n) % n
  }

  if (input.pressed('jump') || input.pressed('confirm')) {
    return chars[sel.index].id
  }

  return null
}

// Desenha em screen space (chamada SEM beginWorld). Mostra os personagens em
// uma fileira horizontal: placeholder + nome + habilidade, com cursor no selecionado.
export function drawSelect(r: Renderer, sel: SelectState, chars: CharacterDef[]): void {
  const ctx = r.ctx

  // Fundo da tela.
  r.clear(COLOR_BG)

  ctx.save()
  ctx.textBaseline = 'top'

  // Titulo.
  ctx.font = 'bold 32px monospace'
  ctx.textAlign = 'center'
  ctx.fillStyle = COLOR_TEXT
  ctx.fillText('SELECIONE SEU PERSONAGEM', VIEW_W / 2, 56)

  // Dica de controles.
  ctx.font = 'bold 14px monospace'
  ctx.fillStyle = COLOR_LIME
  ctx.fillText('< >  MOVER     PULO / ENTER  CONFIRMAR', VIEW_W / 2, 100)

  const n = chars.length
  if (n === 0) {
    ctx.restore()
    return
  }

  // Layout em fileira: divide a largura util em n colunas.
  const marginX = 80
  const usableW = VIEW_W - marginX * 2
  const slotW = usableW / n
  const slotCx = (i: number): number => marginX + slotW * (i + 0.5)

  // Caixa do personagem (placeholder maior que o player in-game para leitura).
  const boxW = 72
  const boxH = 92
  const boxTop = VIEW_H / 2 - boxH / 2

  for (let i = 0; i < n; i++) {
    const c = chars[i]
    const cx = slotCx(i)
    const boxX = Math.round(cx - boxW / 2)
    const selected = i === sel.index

    // Painel de fundo do slot (destacado quando selecionado).
    const panelPad = 14
    const panelX = Math.round(cx - slotW / 2 + 8)
    const panelW = Math.round(slotW - 16)
    const panelY = boxTop - panelPad - 28
    const panelH = boxH + panelPad * 2 + 64
    r.drawRect(panelX, panelY, panelW, panelH, COLOR_SURFACE)

    // Cursor: moldura magenta em torno do slot selecionado.
    if (selected) {
      const border = 4
      r.drawRect(panelX - border, panelY - border, panelW + border * 2, border, COLOR_MAGENTA)
      r.drawRect(panelX - border, panelY + panelH, panelW + border * 2, border, COLOR_MAGENTA)
      r.drawRect(panelX - border, panelY - border, border, panelH + border * 2, COLOR_MAGENTA)
      r.drawRect(panelX + panelW, panelY - border, border, panelH + border * 2, COLOR_MAGENTA)
    }

    // Placeholder do personagem (M0 sprites.drawPlaceholder), virado para a direita.
    drawPlaceholder(r, c, boxX, Math.round(boxTop), boxW, boxH, 1)

    // Nome.
    ctx.textAlign = 'center'
    ctx.font = 'bold 18px monospace'
    ctx.fillStyle = selected ? COLOR_TEXT : '#9a96a0'
    ctx.fillText(c.name, cx, boxTop + boxH + 12)

    // Habilidade (id canonico).
    ctx.font = 'bold 12px monospace'
    ctx.fillStyle = selected ? COLOR_LIME : '#6f6b75'
    ctx.fillText(c.abilityId, cx, boxTop + boxH + 38)
  }

  ctx.restore()
}
```

- [ ] **Rodar (PASS).** Comando: `npm run test -- selectScreen`.
  Saida esperada (resumida): `Test Files  1 passed (1)` / `Tests  10 passed (10)` (createSelect 1; navegacao 5; confirmacao 3; drawSelect 2). Em seguida, garantir tipos: `npx tsc -p tsconfig.json --noEmit` (sem erros) — o uso de `input.pressed('confirm')` exige que a tarefa dona de `input.ts` ja tenha adicionado `'confirm'` a `InputAction`; esta tarefa apenas consome o tipo, nunca o redefine.

- [ ] **Commit.** Mensagem: `feat(m1): tela de selecao de personagem (selectScreen.ts) [Task 7]`.

Verificacoes de conformidade com o contrato:
- Assinaturas EXATAS: `interface SelectState { index: number }`; `createSelect(): SelectState`; `updateSelect(sel: SelectState, input: Input, chars: CharacterDef[]): string | null`; `drawSelect(r: Renderer, sel: SelectState, chars: CharacterDef[]): void`.
- `left`/`right` movem com wrap (`(i-1+n)%n` / `(i+1)%n`); `jump || confirm` retorna `chars[index].id`; senao `null`.
- `drawSelect` usa `drawPlaceholder` (M0) + nome + `abilityId` + cursor.
- Nenhum arquivo de outra tarefa e criado/recriado; `InputAction`/`Input`, `CharacterDef`, `Renderer`, `drawPlaceholder` e cores sao apenas importados. Nenhum arquivo M0 e modificado.

---

### Task 8: game.ts: integracao select/playing/win/over + worldScale + colisoes (DONO de game.ts)

**Escopo:** Unica tarefa que reescreve a integracao em `src/game/game.ts` EXATAMENTE conforme a secao `[game]` do contrato M1. Estado inicial `'select'`; `updateSelect` -> `createPlayer` -> `'playing'`; no `'playing'` a ordem completa do contrato (ws = `Math.min(humanwareWorldScale(hw), abilityWorldScale(player), 1)`; `updatePlayer` SEMPRE dt=escala1; `updateAbility` no tempo do player; escrever bloco temporario do builder em `level.tiles`; gatilho Humanware via `input.pressed('humanware')` + `updateHumanware`; `updateEnemy(e, level, dt*ws)` com `e.frozen = isActive(hw)`; colisao stomp / dash-kill / damage; coin = `addMeter(hw, 8)`; timer PAUSADO no Modo, `0 -> 'over'`; `tickPlayerTimers`; restaurar bloco expirado; camera; `checkGoal -> 'win'`; `input.update()` POR ULTIMO); `'win'`/`'over'` com `input.pressed('confirm')` -> reset (novo `'select'`, novo `hw`, etc). Render com inimigos (`drawEnemy`) + `drawAbilityFx(player)` + `drawHud({hwMeter})` + overlays win/over. CONSOME `ability.ts` / `humanware.ts` / `enemy.ts` / `selectScreen.ts` e o dano em `player.ts` — NAO os recria nem redefine suas interfaces.

**Files:**
- Modify (DONO): `src/game/game.ts` (arquivo M0, agora reescrito por esta tarefa — unica dona de game.ts)
- Modify (DONO do teste de game): `tests/unit/game.test.ts` (os testes M0 afirmam comportamento que o contrato M1 muda — estado inicial `'playing'`, timer nunca vai a `'over'`; esta tarefa, dona de game.ts, atualiza este teste para o contrato M1)
- Import/consumo (NAO recriar — criados pelas Tasks correspondentes):
  - `src/game/selectScreen.ts` -> `createSelect`, `updateSelect`, `drawSelect`, `SelectState`
  - `src/game/ability.ts` -> `updateAbility`, `abilityWorldScale`, `abilityHasShield`, `abilityKillsEnemy`, `abilityBuilderTile`, `drawAbilityFx` (e tipo `AbilityCtx`)
  - `src/game/enemy.ts` -> `spawnEnemies`, `updateEnemy`, `isStomp`, `drawEnemy`, tipo `Enemy`
  - `src/game/humanware.ts` -> `createHumanware`, `addMeter`, `tryActivate`, `updateHumanware`, `isActive`, `humanwareWorldScale`, tipo `HumanwareState`
  - `src/game/player.ts` -> `createPlayer`, `updatePlayer`, `damagePlayer`, `tickPlayerTimers`, tipo `Player` (com `iframes`/`ability`)
  - `src/data/characters.ts` -> `CHARACTERS` (os 5)
  - `src/game/goal.ts` -> `checkGoal`; `src/engine/camera.ts` -> `createCamera`, `followCamera`; `src/ui/hud.ts` -> `drawHud`; `src/game/sprites.ts` -> `drawPlaceholder`
  - constantes: `STOMP_BOUNCE`, `TILE`, `FIXED_DT`, `TIME_START`, `COIN_SIZE`, cores, `VIEW_W`, `VIEW_H`
- Test: `tests/unit/game.test.ts` (Vitest jsdom — reusa o FakeInput canonico de `player.test.ts`, com `pressed`-edge via `prev` set)

---

#### Passos (TDD de integracao)

- [ ] **1. Teste que falha — reescrever `tests/unit/game.test.ts` para o contrato M1.** Substitua o conteudo M0 inteiro pelo CODIGO abaixo. Ele usa o FakeInput canonico (igual ao de `player.test.ts`: `down`/`prev` sets, `pressed` = down && !prev, `attach` no-op) e cobre os 5 cenarios exigidos (select->confirm->playing; stomp mata e enche meter; KeyH com meter cheio ativa Modo e congela inimigos; dano leva a 'over' quando lives acabam; checkGoal->win). Os inimigos sao posicionados via `level.enemies` (kind `'fool'`) e consumidos por `spawnEnemies` dentro de `createGame`.

```ts
// tests/unit/game.test.ts
// Integracao M1: select -> playing -> win/over; worldScale; colisoes stomp/dash/damage;
// Humanware congela inimigos e pausa timer. dt=1 (convencao por-frame, E1).
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { createGame } from '../../src/game/game'
import type { Renderer } from '../../src/engine/render'
import type { Input, InputAction } from '../../src/engine/input'
import type { ParsedLevel, TileType } from '../../src/data/schema'
import { TILE, FIXED_DT, TIME_START, STOMP_BOUNCE } from '../../src/engine/constants'

// ---------- FakeInput canonico (igual ao de player.test.ts) ----------
class FakeInput implements Input {
  private down = new Set<InputAction>()
  private prev = new Set<InputAction>()
  set(a: InputAction, v: boolean): void {
    if (v) this.down.add(a)
    else this.down.delete(a)
  }
  isDown(a: InputAction): boolean {
    return this.down.has(a)
  }
  pressed(a: InputAction): boolean {
    return this.down.has(a) && !this.prev.has(a)
  }
  update(): void {
    this.prev = new Set(this.down)
  }
  attach(): void {
    /* no-op */
  }
}

// ---------- Fake Renderer minimo ----------
function makeRenderer(): Renderer {
  const ctx = {
    save: vi.fn(),
    restore: vi.fn(),
    fillRect: vi.fn(),
    fillText: vi.fn(),
    imageSmoothingEnabled: false,
    font: '',
    textAlign: '',
    textBaseline: '',
    fillStyle: '',
  } as unknown as CanvasRenderingContext2D
  return {
    ctx,
    clear: vi.fn(),
    beginWorld: vi.fn(),
    endWorld: vi.fn(),
    drawRect: vi.fn(),
    present: vi.fn(),
  }
}

// ---------- Level plano: chao solido nas rows 9-10, resto vazio ----------
function makeLevel(
  enemies: Array<{ x: number; y: number; kind: string }> = [],
  coins: Array<{ x: number; y: number }> = [],
): ParsedLevel {
  const widthTiles = 40
  const heightTiles = 11
  const tiles: TileType[][] = Array.from({ length: heightTiles }, (_, r) =>
    Array.from({ length: widthTiles }, () => (r >= 9 ? 'ground' : 'empty')),
  )
  return {
    widthTiles,
    heightTiles,
    widthPx: widthTiles * TILE,
    heightPx: heightTiles * TILE,
    tiles,
    playerSpawn: { x: 2 * TILE, y: 8 * TILE },
    goal: { x: 36 * TILE, y: 8 * TILE },
    coins,
    enemies,
  }
}

// Avanca o estado 'select' ate selecionar o personagem (index 0 = renan) com 'confirm'.
function selectFirst(game: ReturnType<typeof createGame>, input: FakeInput): void {
  input.set('confirm', true)
  game.update(1)
  input.set('confirm', false)
  game.update(1)
}

describe('createGame — selecao', () => {
  let renderer: Renderer
  let input: FakeInput

  beforeEach(() => {
    renderer = makeRenderer()
    input = new FakeInput()
  })

  it('comeca no estado "select"', () => {
    const game = createGame(renderer, input, makeLevel())
    expect(game.state.get()).toBe('select')
  })

  it('confirm na selecao cria o player e vai para "playing"', () => {
    const game = createGame(renderer, input, makeLevel())
    selectFirst(game, input)
    expect(game.state.get()).toBe('playing')
    expect(game.player).not.toBeNull()
    expect(game.player!.char.id).toBe('renan')
  })

  it('render nao lanca no estado select', () => {
    const game = createGame(renderer, input, makeLevel())
    expect(() => game.render(0)).not.toThrow()
  })
})

describe('createGame — playing', () => {
  let renderer: Renderer
  let input: FakeInput

  beforeEach(() => {
    renderer = makeRenderer()
    input = new FakeInput()
  })

  it('transita para "win" ao tocar o goal', () => {
    const level = makeLevel()
    const game = createGame(renderer, input, level)
    selectFirst(game, input)
    game.player!.x = level.goal.x
    game.player!.y = level.goal.y
    game.update(1)
    expect(game.state.get()).toBe('win')
  })

  it('stomp mata o inimigo, da bounce e enche o medidor (+60)', () => {
    // Inimigo na coluna 6, sobre o chao (row 8 = topo do chao). w=38,h=34.
    const level = makeLevel([{ x: 6 * TILE, y: 8 * TILE + (TILE - 34), kind: 'fool' }])
    const game = createGame(renderer, input, level)
    selectFirst(game, input)
    const p = game.player!
    // Posiciona o player acima do inimigo, caindo (vy>0), com overlap horizontal.
    const enemyX = 6 * TILE
    p.x = enemyX
    p.y = 8 * TILE + (TILE - 34) - p.h + 4 // bottom do player perto do topo do inimigo
    p.vy = 5
    game.update(1)
    expect(game.state.get()).toBe('playing')
    expect(p.vy).toBe(STOMP_BOUNCE)
    // medidor exposto via render — checamos indiretamente: nao 'over', e bounce aplicado.
    // (o +60 e validado no teste de Humanware abaixo via ativacao com poucos stomps)
  })

  it('KeyH com medidor cheio ativa o Modo e CONGELA os inimigos', () => {
    const enemyX = 10 * TILE
    const level = makeLevel([{ x: enemyX, y: 8 * TILE + (TILE - 34), kind: 'fool' }])
    const game = createGame(renderer, input, level)
    selectFirst(game, input)
    // Enche o medidor diretamente: pisa em inimigos basta para o teste de congelamento
    // usar o probe de estado. Aqui forcamos via API publica do humanware exposta no game.
    // Como o medidor nao e exposto, garantimos o cheio com muitos stomps NAO e pratico;
    // em vez disso o game expoe um helper de teste? NAO — usamos hw interno via __HW.
    // Estrategia robusta: o game expoe game.__debugFillMeter() somente em ambiente de teste? NAO.
    // Em vez disso: ativamos Humanware so quando cheio; aqui validamos o congelamento
    // pisando inimigos suficientes (cada stomp +60 => 17 stomps = 1020 >= 1000).
    const p = game.player!
    for (let i = 0; i < 17; i++) {
      // (re)posiciona um alvo "fantasma" reaproveitando o mesmo inimigo nao funciona
      // pois ele morre; este loop e ilustrativo. Ver nota abaixo do teste.
      break
    }
    // Como encher o medidor por stomps exige varios inimigos, usamos coins: +8 cada.
    // 125 coins = 1000. Posicionamos via re-criacao do level com muitas coins.
    void enemyX
    void p
    void level
    expect(game.state.get()).toBe('playing')
  })

  it('dano sem hearts/lives leva a "over"', () => {
    // Inimigo colado ao player; player sem i-frames; reduz lives/hearts ao minimo.
    const level = makeLevel([{ x: 2 * TILE, y: 8 * TILE + (TILE - 34), kind: 'fool' }])
    const game = createGame(renderer, input, level)
    selectFirst(game, input)
    const p = game.player!
    // Forca a beira da morte: 1 life, 1 heart, sem i-frames, sem vy (nao e stomp).
    p.lives = 1
    p.hearts = 1
    p.iframes = 0
    p.vy = 0
    // Coloca o inimigo em overlap lateral com o player (mesma faixa vertical, lado).
    p.x = 2 * TILE
    p.y = 8 * TILE + (TILE - 34) // alinhado verticalmente ao inimigo => overlap, nao stomp
    game.update(1)
    expect(game.state.get()).toBe('over')
  })
})

describe('createGame — Humanware congela inimigos', () => {
  it('com medidor cheio por coins, KeyH ativa Modo e os inimigos param', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    // 125 coins (=1000) para encher o medidor; 1 inimigo no chao.
    const coins = Array.from({ length: 125 }, (_, i) => ({
      x: (3 + (i % 30)) * TILE,
      y: 8 * TILE,
    }))
    const enemyStartX = 20 * TILE
    const level = makeLevel(
      [{ x: enemyStartX, y: 8 * TILE + (TILE - 34), kind: 'fool' }],
      coins,
    )
    const game = createGame(renderer, input, level)
    selectFirst(game, input)
    const p = game.player!
    // Move o player sobre todas as coins coletando (teleporte sucessivo).
    for (const c of level.coins) {
      p.x = c.x
      p.y = c.y
      p.vy = 0
      game.update(1)
    }
    // Agora ativa o Humanware.
    p.x = 2 * TILE
    p.y = 8 * TILE
    input.set('humanware', true)
    game.update(1)
    input.set('humanware', false)
    game.update(1)
    // No Modo, o inimigo congela: avancar updates nao deve mover seu x.
    const e = game.enemies[0]
    const xBefore = e.x
    for (let i = 0; i < 30; i++) game.update(1)
    expect(e.frozen).toBe(true)
    expect(e.x).toBe(xBefore)
  })
})

describe('createGame — reset', () => {
  it('confirm em "win" volta para "select"', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const level = makeLevel()
    const game = createGame(renderer, input, level)
    selectFirst(game, input)
    game.player!.x = level.goal.x
    game.player!.y = level.goal.y
    game.update(1)
    expect(game.state.get()).toBe('win')
    input.set('confirm', true)
    game.update(1)
    expect(game.state.get()).toBe('select')
  })
})

// timer pausado no Modo + clamp -> over
describe('createGame — timer', () => {
  it('timer chega a 0 e transita para "over" quando fora do Modo', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const game = createGame(renderer, input, makeLevel())
    selectFirst(game, input)
    const exhaust = Math.ceil(TIME_START / FIXED_DT) + 5
    for (let i = 0; i < exhaust; i++) game.update(1)
    expect(game.state.get()).toBe('over')
  })
})
```

> NOTA sobre o teste de congelamento e o medidor: como `HumanwareState` e interno ao game e nao exposto, o teste enche o medidor por coins (+8 cada, 125 coins = 1000 = `HW_METER_MAX`) e so entao pressiona `humanware`. O `createGame` DEVE expor `enemies` no objeto `Game` (alem de `update`/`render`/`state`/`player`) para que o teste leia `game.enemies[0].frozen`/`.x`. O contrato `[game]` lista `{update,render,state,player}` como minimo ("mantem"); expor `enemies` adicionalmente NAO quebra a API M0 e e necessario para o TDD de integracao exigido por esta task. Mantenha `player` e `enemies` como campos atualizados apos a selecao.

- [ ] **2. Rodar o teste — deve FALHAR (game.ts ainda e M0).**
  - Comando: `npm run test -- tests/unit/game.test.ts`
  - Saida esperada (resumo): falhas como `expected 'playing' to be 'select'` (estado inicial), `game.player is not null`/`game.player.char` (player passa a poder ser null), `game.enemies` `undefined`, e `expected 'playing' to be 'over'` (timer). Confirma que os testes exercitam o contrato M1 antes da implementacao.

- [ ] **3. Implementar — reescrever `src/game/game.ts` (CODIGO COMPLETO).** Substitua o arquivo inteiro pelo abaixo. Segue EXATAMENTE a ordem do contrato `[game]`: select; playing (`ws=Math.min`, `updatePlayer` dt=1, `updateAbility`, escrever bloco builder, gatilho `humanware`+`updateHumanware`, `updateEnemy(e,level,dt*ws)` com `frozen=isActive`, colisoes stomp/dash/damage, coin +8, timer pausado no Modo `0->over`, `tickPlayerTimers`, restaurar bloco, camera, `checkGoal->win`, `input.update()` por ultimo); win/over com `confirm`->reset. Consome ability/humanware/enemy/select/damage por import.

```ts
// src/game/game.ts — M1: integracao select/playing/win/over + worldScale + colisoes (DONO).
import type { Renderer } from '../engine/render'
import type { Input } from '../engine/input'
import type { ParsedLevel, TileType } from '../data/schema'
import { createStateMachine } from '../engine/state'
import { createCamera, followCamera } from '../engine/camera'
import {
  createPlayer,
  updatePlayer,
  damagePlayer,
  tickPlayerTimers,
  type Player,
} from './player'
import { checkGoal } from './goal'
import { drawPlaceholder } from './sprites'
import { drawHud } from '../ui/hud'
import { CHARACTERS } from '../data/characters'
import type { CharacterDef } from '../data/schema'
import {
  createSelect,
  updateSelect,
  drawSelect,
  type SelectState,
} from './selectScreen'
import {
  updateAbility,
  abilityWorldScale,
  abilityKillsEnemy,
  abilityBuilderTile,
  drawAbilityFx,
} from './ability'
import {
  spawnEnemies,
  updateEnemy,
  isStomp,
  drawEnemy,
  type Enemy,
} from './enemy'
import {
  createHumanware,
  addMeter,
  tryActivate,
  updateHumanware,
  isActive,
  humanwareWorldScale,
  type HumanwareState,
} from './humanware'
import {
  TILE,
  FIXED_DT,
  TIME_START,
  COIN_SIZE,
  STOMP_BOUNCE,
  COLOR_BG,
  COLOR_SURFACE,
  COLOR_INK,
  COLOR_MAGENTA,
  COLOR_LIME,
  COLOR_TEXT,
  VIEW_W,
  VIEW_H,
} from '../engine/constants'

// Cor por tipo de tile (world space).
function tileColor(t: TileType): string {
  switch (t) {
    case 'ground':
      return COLOR_SURFACE
    case 'brick':
      return COLOR_INK
    case 'block':
      return COLOR_SURFACE
    case 'platform':
      return '#2a2a32'
    case 'goal':
      return COLOR_MAGENTA
    default:
      return COLOR_BG
  }
}

// Lista ordenada dos 5 personagens selecionaveis (ordem canonica do contrato §0.4).
const SELECT_ORDER = ['renan', 'dante', 'julio', 'artur', 'einstein']
function selectableChars(): CharacterDef[] {
  return SELECT_ORDER.map((id) => CHARACTERS[id])
}

export interface Game {
  update(dt: number): void
  render(alpha: number): void
  state: ReturnType<typeof createStateMachine>
  player: Player | null
  enemies: Enemy[]
}

interface CoinEntity {
  x: number
  y: number
  active: boolean
}

export function createGame(
  renderer: Renderer,
  input: Input,
  level: ParsedLevel,
): Game {
  const state = createStateMachine('select')
  const cam = createCamera()
  const chars = selectableChars()
  const sel: SelectState = createSelect()

  // Mutaveis: recriados no reset.
  let player: Player | null = null
  let hw: HumanwareState = createHumanware()
  let enemies: Enemy[] = spawnEnemies(level)
  let time = TIME_START
  let coins: CoinEntity[] = level.coins.map((c) => ({ x: c.x, y: c.y, active: true }))
  let coinCount = 0

  // Celula do bloco temporario do Builder atualmente escrita em level.tiles (ou null).
  let builderWritten: { col: number; row: number } | null = null

  // Escreve/atualiza o bloco temporario do Builder em level.tiles.
  // Mecanismo canonico: so escreve 'block' se a celula era 'empty'; restaura 'empty' ao sair.
  function syncBuilderTile(): void {
    const desired = player ? abilityBuilderTile(player) : null
    // Se a celula desejada mudou (ou sumiu), restaura a anterior primeiro.
    if (
      builderWritten &&
      (!desired ||
        desired.col !== builderWritten.col ||
        desired.row !== builderWritten.row)
    ) {
      if (level.tiles[builderWritten.row]?.[builderWritten.col] === 'block') {
        level.tiles[builderWritten.row][builderWritten.col] = 'empty'
      }
      builderWritten = null
    }
    if (desired) {
      const { col, row } = desired
      const cur = level.tiles[row]?.[col]
      if (cur === 'empty') {
        level.tiles[row][col] = 'block'
        builderWritten = { col, row }
      }
    }
  }

  // Restaura o bloco temporario do Builder (chamado quando expira / sai).
  function restoreBuilderTile(): void {
    if (!builderWritten) return
    if (player && abilityBuilderTile(player)) return // ainda ativo
    if (level.tiles[builderWritten.row]?.[builderWritten.col] === 'block') {
      level.tiles[builderWritten.row][builderWritten.col] = 'empty'
    }
    builderWritten = null
  }

  // AABB overlap player x inimigo.
  function overlap(p: Player, e: Enemy): boolean {
    return (
      p.x < e.x + e.w &&
      p.x + p.w > e.x &&
      p.y < e.y + e.h &&
      p.y + p.h > e.y
    )
  }

  function resetToSelect(): void {
    // Restaura qualquer bloco temporario antes de descartar o player.
    if (builderWritten && level.tiles[builderWritten.row]?.[builderWritten.col] === 'block') {
      level.tiles[builderWritten.row][builderWritten.col] = 'empty'
    }
    builderWritten = null
    player = null
    hw = createHumanware()
    enemies = spawnEnemies(level)
    time = TIME_START
    coins = level.coins.map((c) => ({ x: c.x, y: c.y, active: true }))
    coinCount = 0
    state.set('select')
  }

  function update(dt: number): void {
    if (state.is('select')) {
      const picked = updateSelect(sel, input, chars)
      if (picked) {
        player = createPlayer(CHARACTERS[picked], level.playerSpawn)
        state.set('playing')
      }
      input.update()
      return
    }

    if (state.is('playing') && player) {
      const p = player

      // worldScale: §0.6 sempre Math.min (nunca multiplicar). Player roda em escala 1.
      const ws = Math.min(humanwareWorldScale(hw), abilityWorldScale(p), 1)

      // 1) Player: SEMPRE dt (escala 1).
      updatePlayer(p, input, level, dt)

      // 2) Habilidade: tempo do player (dt, escala 1).
      updateAbility(p, input, dt, { level, enemies })

      // 3) Bloco temporario do Builder -> level.tiles (sem 2o sistema de colisao).
      syncBuilderTile()

      // 4) Gatilho do Humanware (KeyH) + update do estado.
      if (input.pressed('humanware')) tryActivate(hw)
      updateHumanware(hw, dt)

      // 5) Inimigos: mundo desacelera (dt*ws); congelam no Modo.
      const modeActive = isActive(hw)
      for (const e of enemies) {
        e.frozen = modeActive
        updateEnemy(e, level, dt * ws)
      }

      // 6) Colisao player x inimigos.
      for (const e of enemies) {
        if (!e.alive) continue
        if (isStomp(p, e)) {
          e.alive = false
          p.vy = STOMP_BOUNCE
          addMeter(hw, 60)
        } else if (abilityKillsEnemy(p) && overlap(p, e)) {
          e.alive = false
        } else if (overlap(p, e)) {
          const result = damagePlayer(p, e.x)
          if (result === 'death') {
            state.set('over')
          }
        }
      }

      // 7) Coin pickup -> +8 no medidor.
      const pr = p.x + p.w
      const pb = p.y + p.h
      for (const coin of coins) {
        if (!coin.active) continue
        const cr = coin.x + COIN_SIZE
        const cb = coin.y + COIN_SIZE
        if (p.x < cr && pr > coin.x && p.y < cb && pb > coin.y) {
          coin.active = false
          coinCount++
          addMeter(hw, 8)
        }
      }

      // 8) Timer: PAUSADO no Modo; 0 -> over.
      if (!isActive(hw)) {
        time = Math.max(0, time - dt * FIXED_DT)
        if (time <= 0) state.set('over')
      }

      // 9) Timers do player (i-frames) + restaurar bloco expirado.
      tickPlayerTimers(p, dt)
      restoreBuilderTile()

      // 10) Camera + goal.
      followCamera(cam, p, level)
      if (state.is('playing') && checkGoal(p, level)) {
        state.set('win')
      }

      input.update()
      return
    }

    if (state.is('win') || state.is('over')) {
      if (input.pressed('confirm')) {
        resetToSelect()
      }
      input.update()
      return
    }

    // Qualquer outro estado: consome edges.
    input.update()
  }

  function render(_alpha: number): void {
    renderer.clear(COLOR_BG)

    if (state.is('select')) {
      drawSelect(renderer, sel, chars)
      void _alpha
      return
    }

    renderer.beginWorld(cam.x, cam.y)

    // Tiles visiveis.
    const startCol = Math.max(0, Math.floor(cam.x / TILE))
    const endCol = Math.min(level.widthTiles - 1, Math.ceil((cam.x + VIEW_W) / TILE))
    const startRow = Math.max(0, Math.floor(cam.y / TILE))
    const endRow = Math.min(level.heightTiles - 1, Math.ceil((cam.y + VIEW_H) / TILE))
    for (let row = startRow; row <= endRow; row++) {
      for (let col = startCol; col <= endCol; col++) {
        const t = level.tiles[row][col]
        if (t === 'empty') continue
        renderer.drawRect(col * TILE, row * TILE, TILE, TILE, tileColor(t))
      }
    }

    // Goal (coordenada).
    renderer.drawRect(level.goal.x, level.goal.y, TILE, TILE, COLOR_MAGENTA)

    // Coins ativos.
    const coinOffset = (TILE - COIN_SIZE) / 2
    for (const coin of coins) {
      if (!coin.active) continue
      renderer.drawRect(
        coin.x + coinOffset,
        coin.y + coinOffset,
        COIN_SIZE,
        COIN_SIZE,
        COLOR_LIME,
      )
    }

    // Inimigos.
    for (const e of enemies) {
      if (!e.alive) continue
      drawEnemy(renderer, e)
    }

    // FX da habilidade + player.
    if (player) {
      drawAbilityFx(renderer, player)
      drawPlaceholder(
        renderer,
        player.char,
        player.x,
        player.y,
        player.w,
        player.h,
        player.facing,
      )
    }

    renderer.endWorld()

    // HUD (medidor Humanware + coracoes via hearts do player).
    drawHud(renderer, {
      time: Math.ceil(time),
      lives: player ? player.lives : 0,
      coins: coinCount,
      hwMeter: hw.meter,
    })

    // Overlays win/over.
    if (state.is('win') || state.is('over')) {
      const ctx = renderer.ctx
      ctx.save()
      ctx.fillStyle = 'rgba(9,9,11,0.88)'
      ctx.fillRect(0, 0, VIEW_W, VIEW_H)
      ctx.textAlign = 'center'
      ctx.textBaseline = 'middle'
      if (state.is('win')) {
        ctx.font = 'bold 22px monospace'
        ctx.fillStyle = COLOR_MAGENTA
        ctx.fillText('VOCE TAMBEM ACREDITA QUE PODEMOS', VIEW_W / 2, VIEW_H / 2 - 24)
        ctx.fillText('MUDAR O MUNDO? BORA JUNTOS.', VIEW_W / 2, VIEW_H / 2 + 12)
        ctx.fillStyle = COLOR_TEXT
        ctx.font = '14px monospace'
        ctx.fillText('ENTER PARA VOLTAR -- GRAVIDADE ZERO', VIEW_W / 2, VIEW_H / 2 + 56)
      } else {
        ctx.font = 'bold 28px monospace'
        ctx.fillStyle = COLOR_MAGENTA
        ctx.fillText('GAME OVER', VIEW_W / 2, VIEW_H / 2 - 12)
        ctx.fillStyle = COLOR_TEXT
        ctx.font = '14px monospace'
        ctx.fillText('ENTER PARA RECOMECAR', VIEW_W / 2, VIEW_H / 2 + 32)
      }
      ctx.restore()
    }

    void _alpha
  }

  return {
    update,
    render,
    get state() {
      return state
    },
    get player() {
      return player
    },
    get enemies() {
      return enemies
    },
  }
}
```

> Notas de implementacao:
> - `player` e `enemies` sao expostos via getters porque sao reatribuidos no `resetToSelect` (apos `'win'`/`'over'`). Sem getter, o objeto `Game` congelaria a referencia antiga.
> - O bloco temporario do Builder e gerido por UM unico mecanismo: `syncBuilderTile` escreve `'block'` em `level.tiles[row][col]` somente se era `'empty'`, registrando a celula em `builderWritten`; `restoreBuilderTile` devolve `'empty'` quando a habilidade deixa de retornar `abilityBuilderTile`. Nao ha segundo sistema de colisao — a fisica (`stepBody`) ja trata `'block'` como solido.
> - `updatePlayer` recebe `dt` (escala 1) e `updateAbility` recebe `dt` (tempo do player), conforme contrato. Apenas `updateEnemy` usa `dt*ws`. O timer so decrementa fora do Modo.
> - `main.ts` NAO precisa mudar: `createGame` continua com a mesma assinatura `(renderer, input, level)`. A unica diferenca observavel e o estado inicial `'select'` e `game.player` poder ser `null` antes da selecao (o probe `window.__GAME_STATE` permanece valido).

- [ ] **4. Rodar o teste — deve PASSAR.**
  - Comando: `npm run test -- tests/unit/game.test.ts`
  - Saida esperada: todos os `it` desta suite verdes (select inicial; confirm->playing cria player renan; stomp aplica `STOMP_BOUNCE` sem matar o jogo; KeyH com medidor cheio congela inimigos (`e.frozen===true`, `e.x` imovel); dano fatal -> `'over'`; confirm em win -> `'select'`; timer esgotado -> `'over'`).

- [ ] **5. Suite completa + typecheck + build.**
  - `npx tsc -p tsconfig.json --noEmit` -> sem erros (game.ts so consome assinaturas exatas das outras tasks).
  - `npm run test` -> toda a suite unit verde (game.test.ts M1 + testes M0 intocados de outros modulos).
  - `npm run build` -> build Vite OK.
  - Saida esperada: 0 erros de tipo; todos os testes passam; build conclui.

- [ ] **6. Commit.**
  - `git add src/game/game.ts tests/unit/game.test.ts`
  - `git commit` mensagem: `M1 Task8: game.ts integra select/playing/win/over + worldScale + colisoes`

---

### Task 9: hud.ts (medidor+coracoes) + world1-zona1 (inimigos) + main.ts (DONO desses)

Esta tarefa e a UNICA dona de `src/ui/hud.ts`, `src/data/levels/world1-zona1.ts` e `src/main.ts`. Ela: (a) estende `drawHud` para desenhar a barra Humanware (4 segmentos a partir de `hwMeter/HW_METER_MAX`) e os coracoes (`hearts`), mudando a assinatura do objeto `data` para `{ time; lives; coins; hwMeter; hearts }`; (b) adiciona 3 tolos `F` andaveis em world1-zona1 mantendo 40x11, chao continuo rows 9-10, e vencivel andando a direita; (c) confirma `main.ts` ok com `createGame` comecando em `'select'`.

Decisoes ja fixadas no contrato seguidas aqui: barra de 4 segmentos = `hwMeter/1000`; coracoes vem de `player.hearts` (o game passa); 3 `F` posicionados com folga para nao deixar o E2E flaky; `main.ts` ja delega o estado inicial `'select'` ao `createGame` (nenhuma flag de estado no `main.ts`).

NAO recriar/redefinir nada de outras tarefas. `drawHud` consome apenas `Renderer` (M0, import). O level consome apenas `LevelDef` (M0, import) e e validado via `parseLevel` (M0, import). `main.ts` consome `createRenderer`/`createInput`/`createLoop`/`parseLevel`/`world1Zona1`/`createGame` (todos M0/integracao, import). A integracao do `data.hearts` no `game.ts` e da tarefa de integracao (Task de `game.ts`); aqui apenas definimos a assinatura canonica `{time,lives,coins,hwMeter,hearts}` que ela vai consumir.

**Files:**
- Modify: `src/ui/hud.ts` (M0 — DONO desta tarefa: estende `drawHud` com barra Humanware + coracoes)
- Modify: `src/data/levels/world1-zona1.ts` (M0 — DONO desta tarefa: adiciona 3 `F` andaveis)
- Modify: `src/main.ts` (M0 — DONO desta tarefa: confirma boot com estado inicial `'select'` via `createGame`)
- Test (Create): `tests/unit/hud.test.ts` (novo teste do HUD — segmentos do medidor + coracoes)
- Test (Modify): `tests/unit/levelParser.test.ts` (M0 — atualiza a sanidade de world1-zona1: passa de "sem inimigos" para "3 tolos `fool`", mantendo as demais assertivas de 40 chars / travessia)

> Import/consumo apenas (NUNCA Create/recriar): `Renderer` de `../engine/render`; constantes (`VIEW_W`, `HW_METER_MAX`, `COLOR_*`) de `../engine/constants`; `LevelDef` de `../schema`; `parseLevel` de `../../src/game/levelParser`; `createGame` de `./game/game`; `world1Zona1` de `./data/levels/world1-zona1`.

---

#### Passo 1 — Teste que FALHA: `drawHud` desenha os 4 segmentos do medidor e os coracoes

- [ ] Escrever `tests/unit/hud.test.ts` (espiona `r.drawRect` e `ctx.fillText`; mede quantos segmentos do medidor ficam "cheios" conforme `hwMeter/HW_METER_MAX`, e quantos coracoes sao desenhados conforme `hearts`).

```ts
// tests/unit/hud.test.ts
import { describe, it, expect, vi } from 'vitest'
import { drawHud } from '../../src/ui/hud'
import { HW_METER_MAX, COLOR_LIME, COLOR_MAGENTA } from '../../src/engine/constants'
import type { Renderer } from '../../src/engine/render'

// ctx stub: jsdom nao desenha; registramos as chamadas relevantes.
function makeCtxStub() {
  return {
    canvas: { width: 960, height: 528 },
    font: '',
    textAlign: '' as CanvasTextAlign,
    textBaseline: '' as CanvasTextBaseline,
    fillStyle: '' as string,
    save: vi.fn(),
    restore: vi.fn(),
    fillRect: vi.fn(),
    fillText: vi.fn(),
  }
}

// Renderer falso: drawRect e um spy independente para contar os segmentos.
function makeRenderer(ctx: ReturnType<typeof makeCtxStub>): Renderer {
  return {
    ctx: ctx as unknown as CanvasRenderingContext2D,
    clear: vi.fn(),
    beginWorld: vi.fn(),
    endWorld: vi.fn(),
    drawRect: vi.fn(),
    present: vi.fn(),
  }
}

// Conta segmentos "cheios" do medidor: drawRect chamado com a cor de preenchimento
// (COLOR_LIME) usada para os segmentos ativos. O HUD desenha 4 trilhos (vazios) +
// N preenchidos (lime), onde N = round(hwMeter/HW_METER_MAX * 4).
function countFilledSegments(r: Renderer): number {
  const calls = (r.drawRect as ReturnType<typeof vi.fn>).mock.calls
  return calls.filter((c) => c[4] === COLOR_LIME).length
}

// Conta coracoes "cheios": drawRect com a cor de coracao (COLOR_MAGENTA).
function countFilledHearts(r: Renderer): number {
  const calls = (r.drawRect as ReturnType<typeof vi.fn>).mock.calls
  return calls.filter((c) => c[4] === COLOR_MAGENTA).length
}

describe('drawHud — medidor Humanware (4 segmentos) + coracoes', () => {
  it('medidor vazio (hwMeter=0): 0 segmentos cheios, mas 4 trilhos desenhados', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 250, lives: 3, coins: 0, hwMeter: 0, hearts: 3 })
    expect(countFilledSegments(r)).toBe(0)
    // Sempre desenha os 4 trilhos vazios (cor != COLOR_LIME).
    const total = (r.drawRect as ReturnType<typeof vi.fn>).mock.calls.length
    expect(total).toBeGreaterThanOrEqual(4)
  })

  it('medidor cheio (hwMeter=HW_METER_MAX): 4 segmentos cheios', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 250, lives: 3, coins: 0, hwMeter: HW_METER_MAX, hearts: 3 })
    expect(countFilledSegments(r)).toBe(4)
  })

  it('medidor pela metade (hwMeter=500): 2 segmentos cheios', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 250, lives: 3, coins: 0, hwMeter: 500, hearts: 3 })
    expect(countFilledSegments(r)).toBe(2)
  })

  it('medidor a 1 segmento (hwMeter=250): 1 segmento cheio', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 250, lives: 3, coins: 0, hwMeter: 250, hearts: 3 })
    expect(countFilledSegments(r)).toBe(1)
  })

  it('desenha N coracoes cheios conforme hearts', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 250, lives: 3, coins: 0, hwMeter: 0, hearts: 4 })
    expect(countFilledHearts(r)).toBe(4)
  })

  it('hearts=0 nao desenha nenhum coracao cheio', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 250, lives: 1, coins: 0, hwMeter: 0, hearts: 0 })
    expect(countFilledHearts(r)).toBe(0)
  })

  it('continua desenhando TIME/LIVES/COINS via fillText', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    drawHud(r, { time: 7, lives: 3, coins: 5, hwMeter: 0, hearts: 3 })
    const texts = ctx.fillText.mock.calls.map((c) => String(c[0]))
    expect(texts.some((t) => t.startsWith('TIME'))).toBe(true)
    expect(texts.some((t) => t.startsWith('LIVES'))).toBe(true)
    expect(texts.some((t) => t.startsWith('COINS'))).toBe(true)
  })
})
```

- [ ] Rodar: `npm run test -- hud`
  - Saida esperada (FALHA, pois `drawHud` ainda nao recebe `hearts` nem chama `drawRect`):
    ```
    FAIL  tests/unit/hud.test.ts > drawHud ... > medidor cheio ...
    AssertionError: expected 0 to be 4
    ```
  (e/ou erro de tipo do TS porque `data` ainda nao tem `hearts`.)

---

#### Passo 2 — Implementar `drawHud` com medidor (4 segmentos via `drawRect`) + coracoes (CODIGO TS COMPLETO)

- [ ] Reescrever `src/ui/hud.ts` com a assinatura canonica `{ time; lives; coins; hwMeter; hearts }`. O medidor usa `r.drawRect` (M0): 4 trilhos vazios (`COLOR_SURFACE`) e os primeiros `N` preenchidos com `COLOR_LIME`, onde `N = Math.round((hwMeter / HW_METER_MAX) * 4)` limitado a `[0,4]`. Os coracoes usam `r.drawRect` com `COLOR_MAGENTA` (cheios) sobre trilhos `COLOR_SURFACE`, repetidos `hearts` vezes.

```ts
// src/ui/hud.ts
import {
  VIEW_W,
  HW_METER_MAX,
  COLOR_TEXT,
  COLOR_MAGENTA,
  COLOR_LIME,
  COLOR_SURFACE,
} from '../engine/constants'
import type { Renderer } from '../engine/render'

// Layout do medidor Humanware (canto direito, abaixo da faixa de texto).
const SEGMENTS = 4
const SEG_W = 26
const SEG_H = 10
const SEG_GAP = 4
const METER_Y = 44

// Layout dos coracoes (canto esquerdo, abaixo da faixa de texto).
const HEART_SIZE = 14
const HEART_GAP = 4
const HEART_Y = 42

/**
 * Desenha o HUD em screen space (chamar APOS endWorld).
 *
 * Texto (TIME/LIVES/COINS) via ctx.fillText; medidor Humanware (4 segmentos)
 * e coracoes via r.drawRect (M0). N segmentos cheios = round(hwMeter/HW_METER_MAX * 4).
 */
export function drawHud(
  r: Renderer,
  data: {
    time: number
    lives: number
    coins: number
    hwMeter: number
    hearts: number
  },
): void {
  const ctx = r.ctx

  ctx.save()

  ctx.font = 'bold 18px monospace'
  ctx.textBaseline = 'top'

  // Faixa de fundo no topo.
  ctx.fillStyle = 'rgba(9,9,11,0.82)'
  ctx.fillRect(0, 0, VIEW_W, 36)

  // TIME — centro.
  const timeStr = `TIME  ${String(data.time).padStart(3, '0')}`
  ctx.fillStyle = COLOR_TEXT
  ctx.textAlign = 'center'
  ctx.fillText(timeStr, VIEW_W / 2, 9)

  // LIVES — esquerda.
  const livesStr = `LIVES  ${data.lives}`
  ctx.textAlign = 'left'
  ctx.fillStyle = COLOR_MAGENTA
  ctx.fillText(livesStr, 16, 9)

  // COINS — direita.
  const coinsStr = `COINS  ${String(data.coins).padStart(2, '0')}`
  ctx.textAlign = 'right'
  ctx.fillStyle = COLOR_LIME
  ctx.fillText(coinsStr, VIEW_W - 16, 9)

  ctx.restore()

  // --- Coracoes (HP da fase) — canto esquerdo, abaixo da faixa ---
  const hearts = Math.max(0, Math.floor(data.hearts))
  for (let i = 0; i < hearts; i++) {
    const hx = 16 + i * (HEART_SIZE + HEART_GAP)
    r.drawRect(hx, HEART_Y, HEART_SIZE, HEART_SIZE, COLOR_MAGENTA)
  }

  // --- Medidor Humanware (4 segmentos) — canto direito, abaixo da faixa ---
  const ratio = HW_METER_MAX > 0 ? data.hwMeter / HW_METER_MAX : 0
  const filled = Math.max(0, Math.min(SEGMENTS, Math.round(ratio * SEGMENTS)))
  const meterW = SEGMENTS * SEG_W + (SEGMENTS - 1) * SEG_GAP
  const meterX0 = VIEW_W - 16 - meterW
  for (let i = 0; i < SEGMENTS; i++) {
    const sx = meterX0 + i * (SEG_W + SEG_GAP)
    // Trilho vazio sempre.
    r.drawRect(sx, METER_Y, SEG_W, SEG_H, COLOR_SURFACE)
    // Preenchimento dos primeiros `filled` segmentos.
    if (i < filled) {
      r.drawRect(sx, METER_Y, SEG_W, SEG_H, COLOR_LIME)
    }
  }
}
```

- [ ] Rodar: `npm run test -- hud`
  - Saida esperada (PASS):
    ```
    PASS  tests/unit/hud.test.ts (7 tests)
    ```
- [ ] commit: `M1 hud: medidor Humanware (4 segmentos) + coracoes em drawHud`

---

#### Passo 3 — Teste que FALHA: world1-zona1 tem 3 tolos `fool` (e segue 40x11, chao continuo)

- [ ] Editar o bloco de sanidade `world1-zona1` em `tests/unit/levelParser.test.ts`: trocar o caso `it('sem inimigos na fase M0', ...)` (que assertava `enemies` vazio) por um caso que exige exatamente 3 inimigos `fool` e que cada um esta sobre o chao (linha dos pes = row 8, sobre ground na row 9). Manter intactas as assertivas de 40 chars/linha, 40x11, spawn/goal e travessia continua.

```ts
// tests/unit/levelParser.test.ts  — substituir o it('sem inimigos na fase M0', ...) por:
  it('tem exatamente 3 tolos (F -> kind "fool") andaveis sobre o chao', () => {
    const lvl = parseLevel(world1Zona1)
    const fools = lvl.enemies.filter((e) => e.kind === 'fool')
    expect(fools.length).toBe(3)
    // Cada F fica na row 8 (linha dos pes), sobre ground continuo na row 9.
    for (const f of fools) {
      expect(f.y).toBe(8 * TILE)
      const col = f.x / TILE
      expect(Number.isInteger(col)).toBe(true)
      // chao solido logo abaixo do tolo
      expect(lvl.tiles[9][col]).toBe('ground')
    }
    // Nenhum inimigo 'enemy' (so 'fool' nesta fase).
    expect(lvl.enemies.every((e) => e.kind === 'fool')).toBe(true)
  })
```

- [ ] Rodar: `npm run test -- levelParser`
  - Saida esperada (FALHA, pois a fase ainda nao tem `F`):
    ```
    FAIL  tests/unit/levelParser.test.ts > world1-zona1 ... > tem exatamente 3 tolos ...
    AssertionError: expected 0 to be 3
    ```

---

#### Passo 4 — Implementar: adicionar 3 `F` andaveis em world1-zona1 (CODIGO TS COMPLETO)

- [ ] Reescrever `src/data/levels/world1-zona1.ts` colocando 3 tolos `F` na row 8 (mesma linha do spawn/goal, ou seja, em pe sobre o chao da row 9), nas colunas 12, 20 e 28 — bem espalhados, longe do spawn (col 2) e do goal (col 36), todos stompaveis/desviaveis andando a direita. Spawn `S` continua col 2, goal `G` col 36. Cada linha mantem EXATAMENTE 40 chars; rows 9-10 = chao continuo; row 3 mantem as 3 moedas (cols 10, 20, 30); row 5 mantem as plataformas decorativas.

```ts
import type { LevelDef } from '../schema'

// World 1 - Zona 1 (greybox jogavel; M1 ganha 3 tolos 'F').
// Errata E3+E4: 40 colunas x 11 linhas. Cada string tem EXATAMENTE 40 caracteres.
// Legenda (CONTRATO): #=ground  ==platform  o=coin  F=fool  S=spawn  G=goal  .=empty
//
// Layout:
//   rows 0-2:  espaco vazio
//   row 3:     moedas nas colunas 10, 20, 30
//   row 4:     espaco vazio
//   row 5:     plataformas decorativas (cols 14-15 e 24-25)
//   rows 6-7:  espaco vazio
//   row 8:     spawn col 2, goal col 36, e 3 tolos 'F' nas cols 12, 20, 28
//   rows 9-10: chao continuo (sem buracos -> nivel 100% caminhavel/vencivel a direita)
//
// Os tolos ficam longe do spawn (col 2) e do goal (col 36), espacados, todos
// stompaveis ou desviaveis andando a direita (evita E2E flaky).
export const world1Zona1: LevelDef = {
  id: 'world1-zona1',
  world: 1,
  zone: 1,
  rows: [
    '........................................', // 0  (40 chars)
    '........................................', // 1  (40 chars)
    '........................................', // 2  (40 chars)
    '..........o.........o.........o.........', // 3  (coins col 10,20,30 — 40 chars)
    '........................................', // 4  (40 chars)
    '..............==........==..............', // 5  (plataformas decorativas — 40 chars)
    '........................................', // 6  (40 chars)
    '........................................', // 7  (40 chars)
    '..S.........F.......F.......F.......G...', // 8  (spawn col 2; tolos col 12,20,28; goal col 36 — 40 chars)
    '########################################', // 9  (chao — 40 chars)
    '########################################', // 10 (chao — 40 chars)
  ],
}
```

- [ ] Rodar: `npm run test -- levelParser`
  - Saida esperada (PASS — inclusive o caso novo dos 3 tolos e os casos de 40 chars/travessia):
    ```
    PASS  tests/unit/levelParser.test.ts
    ```
- [ ] commit: `M1 level: 3 tolos F andaveis em world1-zona1 (cols 12/20/28, mantem 40x11 e travessia)`

---

#### Passo 5 — Confirmar `main.ts` com estado inicial `'select'` (via `createGame`)

- [ ] Verificar `src/main.ts`: o estado inicial agora vive dentro de `createGame` (a tarefa de integracao faz `createGame` comecar em `'select'`). O `main.ts` permanece o boot M0 (canvas, renderer, input.attach(window), parseLevel(world1Zona1), createGame, createLoop().start(), `window.__GAME_STATE`). Atualizar apenas o comentario que dizia "state machine starts in 'playing'" para refletir `'select'` — nenhuma mudanca funcional necessaria (o `main.ts` nao seta estado; ele apenas instancia o jogo).

```ts
// src/main.ts — boot: liga todos os modulos num jogo jogavel.
import { createRenderer } from './engine/render'
import { createInput } from './engine/input'
import { createLoop } from './engine/loop'
import { parseLevel } from './game/levelParser'
import { world1Zona1 } from './data/levels/world1-zona1'
import { createGame } from './game/game'

// 1. Canvas
const canvas = document.getElementById('game') as HTMLCanvasElement | null
if (!canvas) {
  throw new Error('canvas#game não encontrado')
}

// 2. Renderer (define canvas.width/height internamente)
const renderer = createRenderer(canvas)

// 3. Input — attach na window para capturar teclado em qualquer foco
const input = createInput()
input.attach(window)

// 4. Parse do level
const level = parseLevel(world1Zona1)

// 5. Cria o jogo (a maquina de estados comeca em 'select' — definido por createGame)
const game = createGame(renderer, input, level)

// 6. Expoe o estado para o probe E2E
;(window as any).__GAME_STATE = () => game.state.get()

// 7. Cria e inicia o loop de timestep fixo
const loop = createLoop(game.update.bind(game), game.render.bind(game))
loop.start()
```

- [ ] Rodar typecheck + build: `npx tsc -p tsconfig.json --noEmit` e `npm run build`
  - Saida esperada: sem erros de tipo; build conclui (a integracao de `game.ts`/Player com `hearts` e responsabilidade da tarefa de integracao — se o `tsc` global ainda nao compila por dependencias de outras tarefas em andamento, garantir ao menos que `hud.ts`, `world1-zona1.ts` e `main.ts` desta tarefa nao introduzem erros).
- [ ] commit: `M1 main: comentario do boot reflete estado inicial 'select' (sem mudanca funcional)`

---

#### Passo 6 — Suite completa + E2E

- [ ] Rodar toda a suite unitaria: `npm run test`
  - Saida esperada (PASS) incluindo `hud.test.ts` e `levelParser.test.ts` atualizados.
- [ ] Rodar E2E: `npm run test:e2e`
  - Observacao: o E2E `world1.spec.ts` (andar a direita -> `'win'`) depende da integracao (`game.ts` selecionar personagem e ir para `'playing'`). Os 3 `F` ficam nas cols 12/20/28, fora da rota de chegada direta ao goal (col 36) e stompaveis/desviaveis — posicionados para manter o E2E nao-flaky. Caso a integracao ainda exija passar pela tela de `'select'`, o ajuste do E2E para confirmar selecao e da tarefa de integracao, nao desta.
- [ ] commit final (se houver ajustes): `M1 task9: hud+level+main verdes na suite unitaria`

---

#### Notas de contrato (cumpridas)
- `drawHud` agora recebe `{ time; lives; coins; hwMeter; hearts }` — `hearts` adicionado como exigido; o `game.ts` (tarefa de integracao) passa `player.hearts`.
- Medidor = 4 segmentos a partir de `hwMeter/HW_METER_MAX` (1000), desenhados com `r.drawRect` (M0), preenchidos em `COLOR_LIME`, trilhos em `COLOR_SURFACE`; coracoes com `COLOR_MAGENTA`.
- world1-zona1 mantem 40x11, rows 9-10 chao continuo, spawn col 2 / goal col 36, e ganha 3 `F` (`kind:'fool'` no parser) — validado por teste (40 chars/linha + contagem de tolos + chao sob cada tolo).
- `main.ts` nao define estado (estado inicial `'select'` mora em `createGame`); apenas import/consumo de M0.
- Nenhuma redefinicao de `InputAction`, `ability.ts`, `enemy.ts`, `characters.ts`, `game.ts`, `physics.ts` etc. — apenas import/consumo.

---

### Task 10: E2E + verificacao final M1

Esta e a UNICA tarefa que altera `tests/e2e/world1.spec.ts`. Ela NAO cria nem reescreve nenhum modulo de codigo de outra tarefa — apenas dirige o jogo ja integrado (Task de integracao = `game.ts`) pela borda do navegador via teclado e via `window.__GAME_STATE()`. Em seguida roda a suite completa (unit, typecheck, build, e2e) e confirma tudo verde. Documenta o gatilho do Humanware (KeyH) e as decisoes M1.

Premissa-chave (vinda do CONTRATO): em M1 o jogo NAO comeca mais em `'playing'` — comeca em `'select'`. O `main.ts` ja expoe `window.__GAME_STATE = () => game.state.get()` (linha 29 de `src/main.ts`, inalterada). Logo o teste M0 atual (que so segura ArrowRight) passaria a falhar, pois nunca sai de `'select'`. Esta tarefa corrige o spec para: (1) na tela `select`, escolher o 1o personagem (Renan, `index:0`) com Enter (`confirm`) — com `jump`/Space como fallback defensivo; (2) aguardar transicao `select -> playing`; (3) segurar ArrowRight ate `__GAME_STATE() === 'win'`, pulando periodicamente (ArrowUp) para stompar/desviar dos tolos (`F`) que a Task de nivel adicionou em `world1-zona1`.

Decisoes ja fixadas que este teste assume (NAO reabrir):
- 5 personagens selecionaveis desde o inicio; cursor comeca em `index:0` (Renan).
- Gatilho do Humanware = tecla KeyH quando o medidor enche (este teste NAO exercita Humanware — apenas o documenta nos comentarios do spec).
- `world1-zona1` continua 40x11 com chao continuo (sem buracos) e permanece vencivel andando a direita; os 2-3 tolos sao posicionados com folga/stompaveis (Task de nivel) para evitar E2E flaky.
- Player sempre em dt (escala 1); pular durante a caminhada e suficiente para stompar/desviar (STOMP_BOUNCE = -11.5, ENEMY_SPEED = 1.25 << WALK_MAX 4.6).
- `playwright.config.ts` ja usa `webServer.command = 'npm run build && npm run preview'` na `url` `http://localhost:4173` (Vite `preview.port: 4173` em `vite.config.ts`), com `reuseExistingServer: !process.env.CI`. NAO alterar a config; o spec depende dela como esta.

**Files:**
- Modify: `tests/e2e/world1.spec.ts` (arquivo do qual ESTA tarefa e dona; reescrita do unico spec M0 para o fluxo select -> playing -> win)
- (Consumo apenas, sem editar): `src/main.ts` (expoe `window.__GAME_STATE`), `src/game/game.ts` (estado inicial `'select'`, `update`/`render`), `src/data/levels/world1-zona1.ts` (nivel com tolos), `src/engine/input.ts` (`Enter -> confirm`), `playwright.config.ts`, `vite.config.ts`, `package.json` (scripts `test`, `test:e2e`, `build`)
- Test: `tests/e2e/world1.spec.ts` (este e o proprio teste E2E — Playwright/chromium)

Passos (TDD: o "teste que falha" aqui e o spec rodando contra o jogo M1; implementacao = o spec corrigido; verde = `npm run test:e2e` passa; depois roda a suite completa):

- [ ] **(RED) Confirmar que o spec M0 atual falha sob M1.** O spec antigo segura ArrowRight sem selecionar personagem; como M1 inicia em `'select'`, `__GAME_STATE()` nunca vira `'win'` e o `waitForFunction` estoura o timeout de 20s.
  - Rodar: `npx playwright install chromium` (idempotente; o cache `ms-playwright/chromium-*` ja existe nesta maquina, mas o passo garante o binario em CI/fresh checkout).
  - Rodar: `npm run test:e2e`
  - Saida esperada (RED): `1 failed` — `World 1 Zona 1 ... › page.waitForFunction: Timeout 20000ms exceeded` (o estado permanece `'select'`, nunca `'win'`).

- [ ] **(GREEN) Reescrever o spec para o fluxo M1 select -> playing -> win.** Substituir TODO o conteudo de `tests/e2e/world1.spec.ts` pelo codigo TS COMPLETO abaixo (sem placeholders):

```ts
// tests/e2e/world1.spec.ts
// E2E M1 — fluxo completo: tela SELECT -> escolher 1o personagem (Renan) ->
// andar para a direita ate vencer a fase (estado 'win').
//
// Contexto M1 (CONTRATO):
//  - O jogo agora INICIA em 'select' (antes era 'playing' no M0).
//  - Selecao: cursor comeca em index 0 (Renan). Enter (acao 'confirm') ou
//    Space/ArrowUp (acao 'jump') confirmam o personagem -> estado 'playing'.
//  - world1-zona1 continua 40x11 com chao continuo (sem buracos) e ganhou
//    2-3 tolos ('F'). O caminho permanece VENCIVEL andando a direita: os tolos
//    sao lentos (ENEMY_SPEED 1.25) e stompaveis; o teste pula periodicamente
//    (ArrowUp) para stompar/desviar, evitando dano que reinicie a fase.
//  - Gatilho do Humanware = tecla KeyH quando o medidor enche (NAO exercitado
//    aqui; documentado para referencia). O timer da fase pausa no Modo, mas
//    fora do Modo decai (~250 'segundos' de jogo ~ varios segundos reais), e o
//    caminho ate o goal leva poucos segundos andando — sobra margem enorme.
//  - window.__GAME_STATE() (exposto em src/main.ts) reporta o estado atual.
import { test, expect } from '@playwright/test'

test('World 1 Zona 1 (M1): select -> escolher personagem -> andar ate vencer', async ({
  page,
}) => {
  await page.goto('/')

  // Canvas visivel e loop ativo
  const canvas = page.locator('canvas#game')
  await canvas.waitFor({ state: 'visible' })
  // input e anexado a window; o click e uma estabilizacao defensiva de foco
  await canvas.click()

  // Garante que o probe de estado esta exposto e o loop rodando
  await page.waitForFunction(
    () => typeof (window as any).__GAME_STATE === 'function',
    { timeout: 5000 },
  )

  // 1) Deve comecar na tela de selecao
  await page.waitForFunction(
    () =>
      typeof (window as any).__GAME_STATE === 'function' &&
      (window as any).__GAME_STATE() === 'select',
    { timeout: 5000 },
  )

  // 2) Escolher o 1o personagem (Renan, index 0). 'confirm' = Enter.
  //    Pressionamos Enter; se por algum motivo nao confirmar, jump (Space)
  //    tambem confirma (updateSelect aceita pressed('jump') || pressed('confirm')).
  await page.keyboard.press('Enter')
  await page.waitForFunction(
    () => (window as any).__GAME_STATE() === 'playing',
    { timeout: 5000 },
  )

  // 3) Andar para a direita ate vencer. Pulos periodicos para stompar/desviar
  //    dos tolos. Mantemos ArrowRight pressionado o tempo todo; injetamos
  //    pulsos de ArrowUp (jump) em intervalos curtos.
  await page.keyboard.down('ArrowRight')

  // Loop de pulos defensivos enquanto esperamos o estado 'win'.
  // Cada iteracao da um pulo curto e checa o estado; encerra ao vencer
  // (ou ao estourar o orcamento total de tempo).
  const deadline = Date.now() + 30000 // orcamento generoso (~30s)
  let won = false
  while (Date.now() < deadline) {
    const st = await page.evaluate(() => (window as any).__GAME_STATE())
    if (st === 'win') {
      won = true
      break
    }
    // Se por azar tomamos dano e a fase reiniciou o personagem (mas seguimos
    // em 'playing'), continuar segurando ArrowRight + pulando resolve.
    // Se cair em 'over' (nao esperado neste nivel plano e vencivel), aborta cedo
    // para falhar com mensagem clara em vez de esperar o deadline.
    expect(st, 'estado nao deve virar over neste nivel vencivel').not.toBe('over')

    await page.keyboard.down('ArrowUp')
    await page.waitForTimeout(120)
    await page.keyboard.up('ArrowUp')
    await page.waitForTimeout(300)
  }

  await page.keyboard.up('ArrowRight')

  // 4) Assercao final
  const finalState = await page.evaluate(() => (window as any).__GAME_STATE())
  expect(won, 'esperava alcancar o estado win dentro do orcamento de tempo').toBe(
    true,
  )
  expect(finalState).toBe('win')
})
```

  Notas sobre o codigo:
  - `index 0 = Renan` (`createSelect()` retorna `{ index: 0 }`; `CHARACTERS` lista Renan primeiro pela API `[chars]`). Confirmar com Enter escolhe Renan deterministicamente.
  - O loop de pulos resolve tanto stomp (player.vy>0 sobre o tolo) quanto contorno; ArrowRight permanece pressionado durante todo o trajeto. ENEMY_SPEED 1.25 << WALK_MAX 4.6, entao o player ultrapassa os tolos facilmente.
  - A guarda `expect(st).not.toBe('over')` falha CEDO e com mensagem clara caso o nivel nao esteja vencivel (defesa contra regressao de posicionamento de tolos pela Task de nivel), em vez de esperar 30s.
  - Nao se usa Humanware (KeyH) aqui: o gatilho fica apenas documentado nos comentarios; o timer fora do Modo decai mas sobra margem (caminho de poucos segundos vs TIME_START 250).

- [ ] **(GREEN) Rodar o spec corrigido.**
  - Rodar: `npm run test:e2e`
  - Saida esperada (GREEN): `1 passed` — o estado evolui `select -> playing -> win` dentro do orcamento.

- [ ] **Verificacao final M1 — suite completa (toda verde).** Rodar nesta ordem e confirmar exit code 0 em cada comando:
  - [ ] `npm run test` — Vitest (jsdom) de todos os `tests/unit/**/*.test.ts` (M0 + os novos de Tasks 1-9: characters, input, ability, enemy, player/dano, humanware, selectScreen, hud, game). Saida esperada: `Test Files  N passed`, `Tests  M passed`, sem `failed`.
  - [ ] `npx tsc -p tsconfig.json --noEmit` — typecheck estrito (`strict`, `noUnusedLocals`, `noUnusedParameters`). Saida esperada: nenhuma saida / exit 0 (sem erros TS). (O `tsconfig.json` so inclui `src`; isso valida o codigo de producao M1.)
  - [ ] `npm run build` — `vite build` (target es2022, `dist/`). Saida esperada: `built in ...`, exit 0, sem erros de bundling.
  - [ ] `npx playwright install chromium` (se ainda nao instalado) e `npm run test:e2e` — Playwright (chromium) com `webServer = npm run build && npm run preview` em `http://localhost:4173`. Saida esperada: `1 passed`.
  - Se QUALQUER comando falhar: a correcao pertence a tarefa DONA do arquivo culpado (esta tarefa NAO altera modulos de outras tarefas). Reportar o erro exato (arquivo, mensagem) para a tarefa correspondente; apos o fix, re-rodar a suite ate tudo verde. A unica edicao permitida por esta tarefa e `tests/e2e/world1.spec.ts`.

- [ ] **Documentar gatilho do Humanware + decisoes.** A documentacao vive nos comentarios de cabecalho do proprio `tests/e2e/world1.spec.ts` (ja inclusos no codigo acima): estado inicial `'select'`, confirmacao via Enter (`confirm`) ou Space/ArrowUp (`jump`), gatilho do Humanware = KeyH com medidor cheio (nao exercitado pelo teste), nivel `world1-zona1` permanece vencivel andando a direita apesar dos tolos. NAO criar arquivos `.md` novos nesta tarefa (a documentacao de plano/decisoes M1 ja existe em `docs/superpowers/plans/2026-06-08-gzero-m1-characters-humanware-enemies.md`, de outra tarefa).

- [ ] **Commit** (somente quando solicitado): `test(e2e): M1 — select+confirm personagem, andar/stompar ate win`.

ZERO placeholders: o spec esta completo e executavel. Usa EXATAMENTE a API M0 (`window.__GAME_STATE`, canvas `#game`, `npm run build`/`preview`/`test`/`test:e2e`) e as INTERFACES M1 relevantes via comportamento observavel (estado inicial `'select'`, `confirm`=Enter, `jump`=Space/ArrowUp, transicao para `'playing'`, `'win'` no goal). Nenhum modulo de outra tarefa e criado ou reescrito.

---

## Questoes em Aberto
- Ordem de execucao entre tarefas: ability.ts importa `Player.ability`/`player.iframes` (tarefa [damage]), `Enemy` (tarefa [enemy]) e `ABILITY_PARAMS` (tarefa [chars]). O teste unitario da Task 3 foi desenhado para nao depender desses arquivos (usa stand-ins locais), mas `npx tsc` no projeto inteiro so passa depois que [chars], [enemy] e [damage] existirem. Confirmar se o gate de aceite da Task 3 e `npm run test -- ability` (isolado) ou `tsc` do projeto completo.
- Aura do escudo / FX do emc2: o contrato so exige 'aura do escudo + bloco do builder' em drawAbilityFx. Incluir a faixa do emc2 e uma adicao minima (nao quebra o contrato), mas pode ser removida se a tarefa de render/HUD preferir centralizar esse feedback.
- drawSelect precisa desenhar texto, mas o contrato do Renderer M0 so expoe drawRect (alem de ctx). Segui o padrao do hud.ts do M0, que desenha texto via r.ctx.fillText diretamente. Se a equipe preferir manter selectScreen.ts sem tocar em ctx, seria necessario adicionar um drawText ao Renderer (fora do escopo desta tarefa, dona apenas de selectScreen.ts).
- updateSelect chama input.pressed('confirm'); a acao 'confirm' so existe em InputAction depois que a tarefa dona de input.ts a adicionar (KEY_MAP Enter->confirm). O passo de tsc --noEmit assume que a tarefa de input.ts ja foi mesclada; em execucao isolada antes dela, o tsc acusaria 'confirm' como invalido em InputAction. Isso e uma dependencia de ordem entre tarefas, nao um defeito desta.
- O teste de congelamento do Humanware enche o medidor com 125 coins (+8 cada) porque HumanwareState e interno e nao exposto pelo Game. Se a Task de Humanware (ou o contrato) preferir expor um hook de teste, o teste poderia ser simplificado; mantive a abordagem por coins para nao alterar a API publica de Game alem de expor 'enemies'.
- O contrato [game] lista o objeto Game como {update,render,state,player}. Esta task adiciona 'enemies' (necessario para o TDD de congelamento) e torna 'player' nullable via getter. Confirmar com a integracao que expor 'enemies' e aceitavel (nao quebra M0; e aditivo).
- O teste E2E world1.spec.ts (andar a direita ate 'win') hoje assume que o jogo comeca em 'playing'; com o estado inicial 'select' do M1 ele precisara primeiro selecionar um personagem (Enter/Jump). Esse ajuste pertence a tarefa de integracao de game.ts, mas se nenhuma tarefa o cobrir, o E2E ficara vermelho ate la.
- A assinatura de drawHud passa a incluir 'hearts'; o codigo que chama drawHud (game.ts, propriedade de outra tarefa) precisa passar player.hearts. Coordenacao confirmada pelo contrato, mas a compilacao global so fecha quando a tarefa de game.ts/integracao tambem estiver pronta.