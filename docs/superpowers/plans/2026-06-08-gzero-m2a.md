# Gravidade Zero — O Jogo · Plano M2a (Motor de Animacao + Fatia Vertical Artur)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Passos `- [ ]`.

**Goal:** Trocar os sprites placeholder por arte real animada (procedural, abordagem D) para o Artur, com background parallax e particulas, provando o pipeline de arte in-game.

**Architecture:** Novos modulos de DONO UNICO: engine/assets.ts, engine/animator.ts (puro), engine/parallax.ts, engine/particles.ts, engine/spriteDraw.ts, data/assets.ts; integracao em game.ts/main.ts. Interfaces FIXADAS no contrato.

**Tech Stack:** Vite, TypeScript strict, Canvas 2D, Vitest, Playwright. Arte em public/assets (servida pelo Vite).

> Plano M2a (1o de M2a-d). Spec: docs/superpowers/specs/2026-06-08-m2-art-animation-design.md.

---

## ⚠️ Errata M2a — Correções Canônicas (LER PRIMEIRO; prevalece sobre as tarefas)

A revisão confirmou **dono único OK** e **sem placeholders**; restaram correções pontuais. **Onde uma Task divergir, a errata vence.**

### E1 — (HIGH) Aspas em nomes de teste (Task 6) quebram a suite
Em `tests/unit/game.test.ts`, os nomes de teste com aspas simples internas (ex.: `it('render('playing') ...')`) causam **erro de parse** e derrubam a suite inteira. Use **aspas duplas externas**:
```ts
it("render no estado playing desenha o parallax e o sprite animado", () => { /* ... */ })
it("update no estado playing nao lanca com store presente", () => { /* ... */ })
```
Confira que NENHUM nome de teste M2a tenha aspas simples internas não escapadas.

### E2 — (MED) `ParticleSystem` deve casar com o contrato (Task 4)
Se a implementação precisar de um acumulador de rate-limit, declare-o **opcional** para não estender a interface fixada: `interface ParticleSystem { particles: Particle[]; ambientAcc?: number }`. `createParticles()` inicializa `ambientAcc: 0`. Assim qualquer literal `{ particles: [] }` tipado como `ParticleSystem` ainda compila.

### E3 — (LOW) `public/` ainda NÃO existe (Task 5)
A nota do contrato de que "public/ existe" está incorreta — **não há `public/` no disco**. A Task 5 cria a árvore: `mkdir -p public/assets/chars public/assets/bg` (Git Bash) e então copia os 3 PNGs de `.m2-samples/`. Confirme que os arquivos-fonte existem antes do `cp`.

### E4 — (LOW) `spriteDraw` parâmetros não usados (Task 5)
`drawAnimatedSprite` mantém a assinatura de 9 args do contrato; `w`/`h` não escalam (altura fixa `SPRITE_DRAW_H=90`). Prefira renomear os não usados para `_w`/`_h` (como o animator faz com `_body`) em vez de `void w; void h`.

> LOW aceitos (sem ação): o burst do Humanware dispara 1 frame após a ativação (borda de subida) — imperceptível; o pisca de i-frames usa `(player.iframes>>2)&1` — verificado correto.

---

## Contrato Tecnico do M2a (API M0/M1 + interfaces internas fixadas)

CONTRATO M2a (estende M0/M1). Cada arquivo tem UM DONO. Modulos novos CRIADOS por uma unica tarefa com a API COMPLETA abaixo; outras tarefas so CONSOMEM (import). NUNCA recriar arquivo de outra tarefa. Use os nomes/assinaturas EXATOS.

ALVO M2a: trocar os sprites placeholder por ARTE REAL ANIMADA para 1 personagem (Artur), com background parallax e particulas. Constroi o motor de animacao (abordagem D = animacao PROCEDURAL sobre arte-base de IA) e prova in-game. Outros personagens/inimigos continuam placeholder por enquanto (fallback). Vite+TS, Canvas 2D 960x528, Vitest+Playwright.

=== API EXISTENTE (M0/M1 — NAO QUEBRAR) ===
- src/engine/constants.ts: TILE48 VIEW_W960 VIEW_H528 FIXED_DT=1/60 WALK_MAX4.6 RUN_MAX7.3 GRAVITY0.8 STOMP_BOUNCE-11.5 PLAYER_W34 PLAYER_H42 HW_TIME_SCALE0.35 IFRAME_FRAMES90 + COLOR_BG#09090b COLOR_SURFACE#121216 COLOR_INK#050505 COLOR_MAGENTA#ff0055 COLOR_MAGENTA2#e802cf COLOR_BLUE#0099ff COLOR_LIME#cdf140 COLOR_VIOLET#7112ff COLOR_TEXT#f7f3f6.
- src/engine/render.ts: Renderer{ctx:CanvasRenderingContext2D, clear(color), beginWorld(camX,camY), endWorld(), drawRect(x,y,w,h,color), present()}; createRenderer(canvas). (NAO alterar render.ts.)
- src/engine/camera.ts: Camera{x,y}; createCamera(); followCamera(cam,target,level).
- src/game/player.ts: Player extends Body{x,y,w,h,vx,vy,onGround,char,facing:1|-1,coyote,jumpBuffer,lives,hearts,iframes,ability}; createPlayer(char,spawn); updatePlayer(player,input,level,dt).
- src/game/sprites.ts: drawPlaceholder(r,char,x,y,w,h,facing) (FALLBACK quando nao ha arte).
- src/game/game.ts (M1): createGame(renderer,input,level):Game{update(dt),render(alpha),state,player}; estado inicial 'select'; no 'playing' a ordem: updatePlayer->updateAbility->builder->humanware(KeyH)+updateHumanware->enemies(updateEnemy dt*ws)->colisoes->coin(+8)->timer(pausado no Modo)->tickPlayerTimers->camera->checkGoal->input.update() por ultimo. render: clear COLOR_BG, beginWorld(cam), tiles+goal+coins+enemies(drawEnemy)+drawAbilityFx(player)+player(drawPlaceholder), endWorld, drawHud({time,lives,coins,hwMeter,hearts}), overlays select/win/over. Humanware: isActive(hw); ws=Math.min(...). Importa de ./humanware ./ability ./enemy ../ui/selectScreen ../ui/hud.
- src/main.ts: cria canvas#game, createRenderer, createInput().attach(window), parseLevel(world1Zona1), createGame, createLoop(game.update,game.render).start(); window.__GAME_STATE.
- Vite serve a pasta public/ na raiz: um arquivo em public/assets/x.png e servido em /assets/x.png (dev e build/preview). data/levels/world1-zona1.ts existe (40x11). Testes: tests/unit/*.test.ts (Vitest jsdom), tests/e2e/world1.spec.ts (Playwright build+preview:4173). Comandos: npm run test | npm run test:e2e | npx tsc -p tsconfig.json --noEmit | npm run build.

=== INTERFACES INTERNAS M2a (CANONICAS — use EXATAMENTE) ===
[assets] src/engine/assets.ts (UM DONO):
  interface ImageAsset { src: CanvasImageSource; w: number; h: number }
  interface AssetEntry { url: string; chromaKey?: boolean }
  interface AssetStore { get(key: string): ImageAsset | null; readonly ready: boolean }
  loadImage(entry: AssetEntry): Promise<ImageAsset>   // carrega via new Image(); se chromaKey, desenha num canvas offscreen e torna pixels quase-off-white (luma>=235 e baixa saturacao) transparentes (alpha 0); ImageAsset.src = o canvas. Senao src = a Image.
  loadAssets(manifest: Record<string, AssetEntry>): Promise<AssetStore>  // carrega todos em paralelo, cacheia por chave; AssetStore.get retorna null se faltar; ready=true apos carregar.
  export function chromaKeyPixels(data: Uint8ClampedArray): void  // funcao PURA testavel: percorre RGBA in-place, zera alpha onde off-white (luma>=235, max-min<=18). (Testar com um buffer pequeno em jsdom.)
[animator] src/engine/animator.ts (UM DONO; LOGICA PURA, sem render):
  type AnimState = 'idle' | 'walk' | 'run' | 'jump' | 'fall' | 'hurt'
  interface Animator { state: AnimState; t: number }   // t = frames no estado atual
  createAnimator(): Animator   // { state:'idle', t:0 }
  function classifyAnim(body: { vx:number; vy:number; onGround:boolean }, iframes: number): AnimState
    // iframes>0 -> 'hurt'; !onGround && vy<0 -> 'jump'; !onGround && vy>=0 -> 'fall'; |vx|>WALK_MAX+0.3 -> 'run'; |vx|>0.3 -> 'walk'; senao 'idle'.
  function updateAnimator(an: Animator, body, iframes: number, dt: number): void  // s=classifyAnim(...); se s!==an.state {an.state=s; an.t=0} senao an.t+=dt
  interface SpriteTransform { scaleX: number; scaleY: number; rotation: number; offsetY: number }
  function getTransform(an: Animator, body): SpriteTransform
    // deterministico por estado/t (a "fluidez"): idle = respiracao (scaleY 1+sin(t*0.06)*0.03, scaleX inverso, offsetY=sin(t*0.06)*2); walk = bob (offsetY=-abs(sin(t*0.18))*3, scaleX 1+sin*0.04, rotation sin*0.04); run = bob/lean mais fortes + rotation sin(t*0.28)*0.06; jump = stretch (scaleY1.15, scaleX0.9); fall = scaleY1.08/scaleX0.96; hurt = flat (1,1,0,0) (o pisca dos i-frames fica no draw). Todos os valores finitos.
[parallax] src/engine/parallax.ts (UM DONO):
  interface ParallaxLayer { key: string; factor: number }   // factor 0=fixo(ceu distante), 1=acompanha o mundo
  function drawParallax(r: Renderer, layers: ParallaxLayer[], store: AssetStore, cam: Camera): void
    // SCREEN SPACE (antes de beginWorld). Para cada layer: img=store.get(key); se null pula; desenha a imagem cobrindo 960x528, deslocada por -(cam.x*factor) horizontalmente, repetindo (tile) p/ cobrir a largura.
[particles] src/engine/particles.ts (UM DONO):
  interface Particle { x:number; y:number; vx:number; vy:number; life:number; maxLife:number; color:string; size:number }
  interface ParticleSystem { particles: Particle[] }
  function createParticles(): ParticleSystem
  function emitAmbient(ps: ParticleSystem, w: number, h: number, dt: number): void  // poeira de gravidade-zero subindo devagar; rate-limited (ex.: ~1 a cada poucos frames; cap ~60 particulas)
  function emitBurst(ps: ParticleSystem, x: number, y: number, n: number, colors: string[]): void  // explosao radial (Humanware/FX)
  function updateParticles(ps: ParticleSystem, dt: number): void  // integra (x+=vx*dt etc.), life-=dt, remove mortas
  function drawParticles(r: Renderer, ps: ParticleSystem): void   // desenha cada particula via r.drawRect (alpha via r.ctx.globalAlpha = life/maxLife; restaurar para 1 no fim)
[spritedraw] src/engine/spriteDraw.ts (UM DONO):
  function drawAnimatedSprite(r: Renderer, asset: ImageAsset | null, an: Animator, x: number, y: number, w: number, h: number, facing: 1|-1, body): void
    // ANCORA = pes (x = centro horizontal do corpo, y = base/pe do corpo). Aplica getTransform(an,body): r.ctx.save(); translate(x, y+tf.offsetY); scale(facing*tf.scaleX, tf.scaleY); rotate(tf.rotation); desenha asset.src centrado no eixo X e ancorado nos pes, escalado para caber numa altura-alvo (use a altura do sprite-alvo = h*~2.1 para o personagem parecer maior que a hitbox 34x42 — definir SPRITE_DRAW_H = 90 e largura proporcional ao aspecto do asset). Se asset===null, NAO desenhar (o game chama drawPlaceholder como fallback). restore().
[manifest] src/data/assets.ts (UM DONO):
  export const ASSET_MANIFEST: Record<string, AssetEntry> = {
    'char.artur': { url: '/assets/chars/artur.png', chromaKey: true },
    'bg.sky':     { url: '/assets/bg/sky.png' },
    'bg.cosmic':  { url: '/assets/bg/cosmic.png' },
  }
  export const SKY_LAYERS: ParallaxLayer[] = [{ key: 'bg.sky', factor: 0.3 }]   // M2a: 1 camada (mundo 1 = ceu claro)
[arte] arquivos reais (a tarefa dona do manifest tambem copia): copiar (cp) .m2-samples/new-artur-v1.png -> public/assets/chars/artur.png ; .m2-samples/new-bg.png -> public/assets/bg/sky.png ; .m2-samples/background.png -> public/assets/bg/cosmic.png. (.m2-samples existe no disco; e gitignored, mas os arquivos estao la.)
[game integration] src/game/game.ts + src/main.ts (UM DONO = tarefa de integracao):
  - main.ts: ANTES de createGame, await loadAssets(ASSET_MANIFEST) -> store; passar store para createGame(renderer, input, level, store). (Enquanto carrega, pode pintar COLOR_BG.) createGame assinatura passa a aceitar um 4o arg opcional store?: AssetStore.
  - createGame: guarda um Animator do player (createAnimator()) e um ParticleSystem (createParticles()). 
  - update('playing'): apos updatePlayer/updateAbility, chamar updateAnimator(playerAnim, player, player.iframes, dt); emitAmbient(ps, VIEW_W, VIEW_H, dt); se isActive(hw) e acabou de ativar, emitBurst(ps, player.x, player.y-30, 24, [COLOR_MAGENTA, COLOR_LIME]); updateParticles(ps, dt). (Manter toda a ordem M1 existente.)
  - render('playing'/etc.): drawParallax(renderer, SKY_LAYERS, store, cam) em SCREEN SPACE antes de clear/beginWorld? -> primeiro renderer.clear(COLOR_BG), depois drawParallax(...) (cobre o fundo), depois beginWorld(cam): tiles+goal+coins+enemies+drawAbilityFx; o PLAYER agora: const a=store?store.get('char.artur'):null; se a, drawAnimatedSprite(renderer, a, playerAnim, player.x+player.w/2, player.y+player.h, player.w, player.h, player.facing, player); senao drawPlaceholder(...) (fallback). endWorld(); drawParticles(renderer, ps); drawHud(...); overlays. (store pode ser undefined em testes -> fallback placeholder, sem quebrar.)
  - i-frame piscar: se player.iframes>0 e (player.iframes>>2)&1, pular o desenho do sprite (pisca).
[verify] tests/e2e + suite completa (UM DONO).

=== DECISOES M2a ===
- Sprite ancora nos pes; SPRITE_DRAW_H=90 (personagem maior que a hitbox 34x42, como num platformer). 
- store opcional em createGame: testes unit passam sem store (fallback placeholder); o jogo real preload via main.
- chroma-key no load (off-white->transparente); funcao chromaKeyPixels pura e testavel.
- M2a = mundo 1 com bg.sky (ceu claro). bg.cosmic fica no manifest p/ uso futuro.
- Manter TODA a logica M1 (abilities/humanware/enemies/dano) intacta — M2a so adiciona render/animacao.

---

### Task 1: engine/assets.ts: loader + chroma-key (DONO)

Cria o carregador de imagens e o chroma-key do M2a. Este arquivo e o UNICO dono de `src/engine/assets.ts`. Define `ImageAsset`/`AssetEntry`/`AssetStore`, `loadImage` (chroma-key via canvas offscreen), `loadAssets` (paralelo + cache por chave) e a funcao PURA `chromaKeyPixels(data)`. Nenhuma outra tarefa recria este arquivo; demais tarefas (parallax, spriteDraw, manifest, game) so importam destes nomes.

**Files:**
- Create: `src/engine/assets.ts` (DONO — eu crio)
- Create: `tests/unit/assets.test.ts` (DONO — teste deste arquivo)
- Modify: nenhum (Task 1 nao toca M0/M1; `main.ts`/`game.ts`/`manifest` sao de outras tarefas)
- Consume (import): nenhum modulo do projeto (assets.ts e folha; usa so APIs do DOM: `Image`, `document.createElement('canvas')`)

Contexto verificado no repo (para casar 100% com as convencoes existentes):
- Vitest com `globals: true` + `environment: 'jsdom'` (`vitest.config.ts`); imports de teste seguem o padrao de `tests/unit/render.test.ts` (`import { describe, it, expect, vi, beforeEach } from 'vitest'`).
- jsdom NAO tem canvas real: `getContext('2d')` retorna `null` e `new Image()` nunca dispara `onload` sozinho. Por isso os testes mockam `ctx`/`Image` com spies (`vi.fn()`), exatamente como `render.test.ts` faz com `makeCtxStub`/`makeCanvasStub`.
- `chromaKeyPixels` e PURA (opera sobre um `Uint8ClampedArray`), entao roda em jsdom sem canvas — e o caso de teste mais forte do gate.
- Regra de chroma do contrato: luma>=235 E (max-min)<=18 -> alpha 0. Uso luma = `0.299*R + 0.587*G + 0.114*B`.

Gate da tarefa: `npm run test` (Vitest). O `tsc`/`build`/e2e do projeto inteiro sao da Task 7.

---

#### Passo 1 — Teste que FALHA (RED)

Crie `tests/unit/assets.test.ts` com o conteudo COMPLETO abaixo. Ele cobre: (a) `chromaKeyPixels` zera off-white e preserva cores saturadas/escuras (buffer pequeno, sem canvas); (b) `loadAssets` com `Image` global mockada resolve um `AssetStore` cujo `get(key)` retorna o asset e `ready===true`, e `get` de chave ausente retorna `null`; (c) `loadImage` sem `chromaKey` usa a propria `Image` como `src`.

```ts
// tests/unit/assets.test.ts
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'
import {
  chromaKeyPixels,
  loadImage,
  loadAssets,
  type AssetEntry,
} from '../../src/engine/assets'

// ---- chromaKeyPixels: funcao PURA, sem canvas (roda em jsdom) ----
describe('chromaKeyPixels', () => {
  it('zera o alpha de pixels off-white (luma>=235 e max-min<=18)', () => {
    // 1 pixel quase-branco neutro: 245,245,245 -> luma>=235, spread=0
    const data = new Uint8ClampedArray([245, 245, 245, 255])
    chromaKeyPixels(data)
    expect(data[3]).toBe(0)
    // RGB nao precisa mudar; o que importa e o alpha
  })

  it('preserva pixels coloridos saturados mesmo se claros', () => {
    // magenta forte: max-min grande -> NAO e off-white -> alpha mantido
    const data = new Uint8ClampedArray([255, 0, 85, 255])
    chromaKeyPixels(data)
    expect(data[3]).toBe(255)
  })

  it('preserva pixels escuros (luma baixa)', () => {
    const data = new Uint8ClampedArray([10, 10, 12, 255])
    chromaKeyPixels(data)
    expect(data[3]).toBe(255)
  })

  it('preserva cinza claro com spread maior que 18 (nao off-white)', () => {
    // 240,240,210 -> luma alta mas spread=30 (>18) -> mantem
    const data = new Uint8ClampedArray([240, 240, 210, 255])
    chromaKeyPixels(data)
    expect(data[3]).toBe(255)
  })

  it('processa multiplos pixels in-place', () => {
    const data = new Uint8ClampedArray([
      245, 245, 245, 255, // off-white -> 0
      255, 0, 85, 255, // magenta -> 255
    ])
    chromaKeyPixels(data)
    expect(data[3]).toBe(0)
    expect(data[7]).toBe(255)
  })
})

// ---- Mock de Image e canvas para loadImage/loadAssets em jsdom ----
// Em jsdom, new Image() nunca dispara onload sozinho e canvas.getContext('2d')
// retorna null. Mockamos ambos com spies (padrao de render.test.ts).

class FakeImage {
  onload: (() => void) | null = null
  onerror: ((e?: unknown) => void) | null = null
  width = 4
  height = 2
  naturalWidth = 4
  naturalHeight = 2
  decoding = 'async'
  private _src = ''
  set src(v: string) {
    this._src = v
    // resolve de forma assincrona, como o browser
    queueMicrotask(() => this.onload && this.onload())
  }
  get src(): string {
    return this._src
  }
}

// ctx 2d falso: registra drawImage e devolve um ImageData controlado.
function makeFakeCtx(pixels: number[]) {
  return {
    drawImage: vi.fn(),
    getImageData: vi.fn(() => ({
      data: new Uint8ClampedArray(pixels),
      width: 4,
      height: 2,
    })),
    putImageData: vi.fn(),
  }
}

describe('loadImage / loadAssets (Image mockada)', () => {
  let createdCanvasCtx: ReturnType<typeof makeFakeCtx>

  beforeEach(() => {
    // 8 px (4x2) todos off-white -> chroma key deve zerar todos os alphas
    const offWhite: number[] = []
    for (let i = 0; i < 8; i++) offWhite.push(245, 245, 245, 255)
    createdCanvasCtx = makeFakeCtx(offWhite)

    // @ts-expect-error: substituimos o Image global por um fake controlavel
    globalThis.Image = FakeImage as unknown as typeof Image

    // canvas offscreen falso (document.createElement('canvas'))
    vi.spyOn(document, 'createElement').mockImplementation(((tag: string) => {
      if (tag === 'canvas') {
        return {
          width: 0,
          height: 0,
          getContext: (k: string) => (k === '2d' ? createdCanvasCtx : null),
        } as unknown as HTMLCanvasElement
      }
      // fallback p/ qualquer outro elemento (nao usado aqui)
      return {} as unknown as HTMLElement
    }) as typeof document.createElement)
  })

  afterEach(() => {
    vi.restoreAllMocks()
  })

  it('loadImage sem chromaKey usa a propria Image como src', async () => {
    const entry: AssetEntry = { url: '/assets/x.png' }
    const asset = await loadImage(entry)
    expect(asset.w).toBe(4)
    expect(asset.h).toBe(2)
    // src e a Image (FakeImage), nao um canvas
    expect((asset.src as FakeImage) instanceof FakeImage).toBe(true)
    expect(createdCanvasCtx.drawImage).not.toHaveBeenCalled()
  })

  it('loadImage com chromaKey desenha num canvas e devolve o canvas como src', async () => {
    const entry: AssetEntry = { url: '/assets/x.png', chromaKey: true }
    const asset = await loadImage(entry)
    expect(createdCanvasCtx.drawImage).toHaveBeenCalled()
    expect(createdCanvasCtx.getImageData).toHaveBeenCalled()
    expect(createdCanvasCtx.putImageData).toHaveBeenCalled()
    expect(asset.w).toBe(4)
    expect(asset.h).toBe(2)
    // src NAO e a FakeImage (e o canvas offscreen)
    expect((asset.src as unknown) instanceof FakeImage).toBe(false)
  })

  it('loadAssets carrega tudo, cacheia por chave, get retorna asset e ready=true', async () => {
    const store = await loadAssets({
      a: { url: '/assets/a.png' },
      b: { url: '/assets/b.png', chromaKey: true },
    })
    expect(store.ready).toBe(true)
    expect(store.get('a')).not.toBeNull()
    expect(store.get('b')).not.toBeNull()
    expect(store.get('a')!.w).toBe(4)
  })

  it('store.get retorna null para chave ausente', async () => {
    const store = await loadAssets({ a: { url: '/assets/a.png' } })
    expect(store.get('zzz')).toBeNull()
  })
})
```

#### Passo 2 — Rode e confirme o FAIL

```bash
npm run test
```

Saida esperada (o modulo ainda nao existe -> falha de import/resolucao):

```
FAIL  tests/unit/assets.test.ts
Failed to resolve import "../../src/engine/assets" ...
```

(Os demais testes do projeto continuam PASS; so o novo arquivo falha.)

#### Passo 3 — Implemente (GREEN)

Crie `src/engine/assets.ts` com o conteudo COMPLETO abaixo. Assinaturas EXATAS conforme `[assets]` do contrato.

```ts
// src/engine/assets.ts
// DONO Task 1 (M2a): loader de imagens + chroma-key.
// Folha do grafo: depende apenas das APIs do DOM (Image, canvas).

export interface ImageAsset {
  src: CanvasImageSource
  w: number
  h: number
}

export interface AssetEntry {
  url: string
  chromaKey?: boolean
}

export interface AssetStore {
  get(key: string): ImageAsset | null
  readonly ready: boolean
}

// Limiares do contrato: off-white = luma>=235 E (max-min)<=18.
const LUMA_MIN = 235
const SPREAD_MAX = 18

// Funcao PURA e testavel: percorre RGBA in-place, zera o alpha onde off-white.
export function chromaKeyPixels(data: Uint8ClampedArray): void {
  for (let i = 0; i < data.length; i += 4) {
    const r = data[i]
    const g = data[i + 1]
    const b = data[i + 2]
    // luma perceptual (Rec. 601)
    const luma = 0.299 * r + 0.587 * g + 0.114 * b
    const max = r > g ? (r > b ? r : b) : g > b ? g : b
    const min = r < g ? (r < b ? r : b) : g < b ? g : b
    if (luma >= LUMA_MIN && max - min <= SPREAD_MAX) {
      data[i + 3] = 0 // transparente
    }
  }
}

// Carrega UMA entrada. Se chromaKey, desenha num canvas offscreen e torna os
// pixels off-white transparentes; ImageAsset.src = esse canvas. Senao, src = a Image.
export function loadImage(entry: AssetEntry): Promise<ImageAsset> {
  return new Promise<ImageAsset>((resolve, reject) => {
    const img = new Image()
    img.onload = () => {
      const w = img.naturalWidth || img.width
      const h = img.naturalHeight || img.height

      if (!entry.chromaKey) {
        resolve({ src: img, w, h })
        return
      }

      const canvas = document.createElement('canvas')
      canvas.width = w
      canvas.height = h
      const ctx = canvas.getContext('2d')
      if (!ctx) {
        // Sem canvas 2d (ambiente sem suporte): usa a Image crua como fallback.
        resolve({ src: img, w, h })
        return
      }
      ctx.drawImage(img, 0, 0, w, h)
      const imageData = ctx.getImageData(0, 0, w, h)
      chromaKeyPixels(imageData.data)
      ctx.putImageData(imageData, 0, 0)
      resolve({ src: canvas, w, h })
    }
    img.onerror = () => reject(new Error(`loadImage: falha ao carregar ${entry.url}`))
    img.src = entry.url
  })
}

// Carrega todas as entradas em paralelo, cacheia por chave. get retorna null se
// faltar; ready=true apos todas resolverem.
export async function loadAssets(
  manifest: Record<string, AssetEntry>,
): Promise<AssetStore> {
  const cache = new Map<string, ImageAsset>()
  let ready = false

  const keys = Object.keys(manifest)
  await Promise.all(
    keys.map(async (key) => {
      const asset = await loadImage(manifest[key])
      cache.set(key, asset)
    }),
  )
  ready = true

  return {
    get(key: string): ImageAsset | null {
      return cache.get(key) ?? null
    },
    get ready(): boolean {
      return ready
    },
  }
}
```

#### Passo 4 — Rode e confirme o PASS

```bash
npm run test
```

Saida esperada: `tests/unit/assets.test.ts` PASS (todos os casos), e a suite inteira continua verde.

```
 ✓ tests/unit/assets.test.ts (9)
 Test Files  N passed (N)
```

#### Passo 5 — Commit

```bash
git add src/engine/assets.ts tests/unit/assets.test.ts
git commit -m "M2a Task 1: engine/assets.ts loader + chroma-key (DONO)"
```

---

**Notas de fronteira (para nao colidir com outras tarefas):**
- Esta tarefa NAO cria o manifest (`src/data/assets.ts`), NAO copia arte de `.m2-samples/`, e NAO toca `game.ts`/`main.ts` — tudo isso e de outras tarefas (manifest/arte/integracao). Confirmei no disco: `.m2-samples/new-artur-v1.png`, `.m2-samples/new-bg.png` e `.m2-samples/background.png` existem; `public/` existe mas ainda sem `assets/` (a tarefa do manifest cria).
- A assinatura PURA `chromaKeyPixels(data: Uint8ClampedArray): void` e o ponto de teste mais robusto (independe de canvas, roda 100% em jsdom).
- Pequeno reforco fora do contrato literal, mas necessario para jsdom/robustez: se `getContext('2d')` retornar `null` no caminho chromaKey, caímos no fallback (`src = img`) em vez de lançar — mantem o loader resiliente sem alterar a API publica.

---

### Task 2: engine/animator.ts: animacao procedural (DONO, logica pura)

Logica pura de animacao: classifica o estado a partir do `Body` + i-frames e produz transforms deterministicas (a "fluidez") por estado/tempo. ZERO render aqui — `spriteDraw.ts` (Task 5) consome `getTransform`; o `game.ts` (Task 6) chama `updateAnimator`. Voce e DONO de `src/engine/animator.ts`; so CONSOME `WALK_MAX` de `constants.ts` (M0) e o shape de `Body` de `physics.ts` (M0) via `import type`.

**Files:**
- Create: `src/engine/animator.ts` (DONO — arquivo desta task)
- Test: `tests/unit/animator.test.ts` (DONO — testes desta task)
- (consumir via import, NAO modificar) `src/engine/constants.ts` (`WALK_MAX`), `src/engine/physics.ts` (`type Body`)

Notas de ambiente (validadas no repo):
- `WALK_MAX = 4.6` em `src/engine/constants.ts` (o "4.6" do contrato e o valor da constante; importar a constante, nunca hardcodar).
- `Body` de `src/engine/physics.ts` tem `{ x, y, w, h, vx, vy, onGround }`. `classifyAnim`/`getTransform` recebem um subconjunto estrutural (`{ vx, vy, onGround }`), entao um `Player` (que `extends Body`) e aceito direto.
- tsconfig e `strict` + `noUnusedLocals` + `noUnusedParameters`: TODO parametro declarado tem de ser usado. Em `getTransform`, o param `body` e usado (ler `body.onGround`/`body.vy` nao e necessario para os transforms do contrato — entao NAO declarar params nao usados; a assinatura do contrato para `getTransform(an, body)` exige `body`, mas o contrato so usa `an.state`/`an.t`). Para satisfazer `noUnusedParameters` SEM mudar a assinatura publica, prefixar o param nao usado com `_`: `getTransform(an: Animator, _body)`. Idem qualquer outro param nao usado.
- `import type` para tipos (`isolatedModules`/`verbatim`-friendly), `import` normal so para `WALK_MAX`.
- Sem `import` de canvas/render — esta task e 100% logica pura, roda em jsdom sem ctx.
- Gate desta task = `npm run test` (script = `vitest run`). tsc/build/e2e do projeto inteiro sao a Task 7.

Passos TDD (checkbox):

- [ ] **Escrever teste que FALHA** — `tests/unit/animator.test.ts` (cobre os 6 casos de `classifyAnim` com fronteiras via `WALK_MAX`; `updateAnimator` reseta `t` na troca e incrementa senao; `getTransform` finito e idle oscila com `t`):

```ts
// tests/unit/animator.test.ts
import { describe, it, expect } from 'vitest'
import { WALK_MAX } from '../../src/engine/constants'
import {
  createAnimator,
  classifyAnim,
  updateAnimator,
  getTransform,
  type AnimState,
} from '../../src/engine/animator'

// Body minimo aceito por classifyAnim/getTransform (subconjunto estrutural de physics.Body)
function body(over: { vx?: number; vy?: number; onGround?: boolean } = {}) {
  return { vx: 0, vy: 0, onGround: true, ...over }
}

describe('createAnimator', () => {
  it('comeca em idle com t=0', () => {
    const an = createAnimator()
    expect(an.state).toBe('idle')
    expect(an.t).toBe(0)
  })
})

describe('classifyAnim (6 casos + fronteiras)', () => {
  it('hurt: iframes>0 tem prioridade sobre tudo', () => {
    // Mesmo correndo no chao, iframes>0 => hurt
    const s: AnimState = classifyAnim(body({ vx: 99, vy: 0, onGround: true }), 1)
    expect(s).toBe('hurt')
    // E mesmo no ar
    expect(classifyAnim(body({ vy: -5, onGround: false }), 90)).toBe('hurt')
  })

  it('jump: no ar subindo (vy<0)', () => {
    expect(classifyAnim(body({ vy: -0.01, onGround: false }), 0)).toBe('jump')
  })

  it('fall: no ar caindo/parado verticalmente (vy>=0)', () => {
    expect(classifyAnim(body({ vy: 0, onGround: false }), 0)).toBe('fall')
    expect(classifyAnim(body({ vy: 5, onGround: false }), 0)).toBe('fall')
  })

  it('run: no chao com |vx| > WALK_MAX+0.3', () => {
    expect(classifyAnim(body({ vx: WALK_MAX + 0.31, onGround: true }), 0)).toBe('run')
    expect(classifyAnim(body({ vx: -(WALK_MAX + 0.31), onGround: true }), 0)).toBe('run')
  })

  it('fronteira run/walk: exatamente WALK_MAX+0.3 NAO e run (e walk)', () => {
    // |vx| > WALK_MAX+0.3 e estrito; no ponto exato cai em walk
    expect(classifyAnim(body({ vx: WALK_MAX + 0.3, onGround: true }), 0)).toBe('walk')
  })

  it('walk: no chao com |vx| > 0.3 e ate WALK_MAX+0.3', () => {
    expect(classifyAnim(body({ vx: 0.31, onGround: true }), 0)).toBe('walk')
    expect(classifyAnim(body({ vx: WALK_MAX, onGround: true }), 0)).toBe('walk')
  })

  it('fronteira walk/idle: exatamente 0.3 NAO e walk (e idle)', () => {
    expect(classifyAnim(body({ vx: 0.3, onGround: true }), 0)).toBe('idle')
    expect(classifyAnim(body({ vx: -0.3, onGround: true }), 0)).toBe('idle')
  })

  it('idle: no chao parado (|vx| <= 0.3) e iframes=0', () => {
    expect(classifyAnim(body({ vx: 0, onGround: true }), 0)).toBe('idle')
  })
})

describe('updateAnimator', () => {
  it('reseta t para 0 ao TROCAR de estado', () => {
    const an = createAnimator() // idle, t=0
    // acumula tempo em idle
    updateAnimator(an, body({ vx: 0, onGround: true }), 0, 5)
    expect(an.state).toBe('idle')
    expect(an.t).toBe(5)
    // troca para walk => t reinicia em 0
    updateAnimator(an, body({ vx: 1, onGround: true }), 0, 1)
    expect(an.state).toBe('walk')
    expect(an.t).toBe(0)
  })

  it('incrementa t por dt quando o estado NAO muda', () => {
    const an = createAnimator()
    updateAnimator(an, body({ vx: 0, onGround: true }), 0, 1)
    updateAnimator(an, body({ vx: 0, onGround: true }), 0, 1)
    updateAnimator(an, body({ vx: 0, onGround: true }), 0, 2)
    expect(an.state).toBe('idle')
    expect(an.t).toBe(4)
  })
})

describe('getTransform', () => {
  const STATES: AnimState[] = ['idle', 'walk', 'run', 'jump', 'fall', 'hurt']

  it('retorna sempre valores finitos para todos os estados em varios t', () => {
    const an = createAnimator()
    for (const st of STATES) {
      for (const t of [0, 1, 7, 33, 120, 999]) {
        an.state = st
        an.t = t
        const tf = getTransform(an, body())
        for (const v of [tf.scaleX, tf.scaleY, tf.rotation, tf.offsetY]) {
          expect(Number.isFinite(v)).toBe(true)
        }
      }
    }
  })

  it('idle: offsetY oscila com t (respiracao) — nao e constante', () => {
    const an = createAnimator()
    an.state = 'idle'
    // pega offsetY ao longo de uma volta do seno (t*0.06)
    const samples: number[] = []
    for (let t = 0; t < 120; t++) {
      an.t = t
      samples.push(getTransform(an, body()).offsetY)
    }
    const min = Math.min(...samples)
    const max = Math.max(...samples)
    expect(max - min).toBeGreaterThan(0.5) // oscila de fato (amplitude ~2)
  })

  it('hurt: transform "flat" (1,1,0,0)', () => {
    const an = createAnimator()
    an.state = 'hurt'
    an.t = 42
    const tf = getTransform(an, body())
    expect(tf.scaleX).toBe(1)
    expect(tf.scaleY).toBe(1)
    expect(tf.rotation).toBe(0)
    expect(tf.offsetY).toBe(0)
  })

  it('jump: stretch (scaleY>1, scaleX<1)', () => {
    const an = createAnimator()
    an.state = 'jump'
    an.t = 3
    const tf = getTransform(an, body())
    expect(tf.scaleY).toBeGreaterThan(1)
    expect(tf.scaleX).toBeLessThan(1)
  })
})
```

- [ ] **Rodar e ver FALHAR** — `npm run test`. Saida esperada (modulo ainda nao existe):

```
FAIL  tests/unit/animator.test.ts [ tests/unit/animator.test.ts ]
Error: Failed to load url ../../src/engine/animator (resolved id: .../src/engine/animator) ... Does the file exist?
```

- [ ] **Implementar** — `src/engine/animator.ts` (CODIGO COMPLETO, transforms do contrato, deterministico, sem render):

```ts
// src/engine/animator.ts
// Logica PURA de animacao procedural (abordagem D do M2a): classifica o estado a
// partir do corpo + i-frames e produz transforms deterministicas por estado/tempo.
// SEM render aqui — spriteDraw.ts consome getTransform; game.ts chama updateAnimator.
import { WALK_MAX } from './constants'
import type { Body } from './physics'

export type AnimState = 'idle' | 'walk' | 'run' | 'jump' | 'fall' | 'hurt'

export interface Animator {
  state: AnimState
  // t = frames acumulados no estado atual (a base temporal da "fluidez")
  t: number
}

export function createAnimator(): Animator {
  return { state: 'idle', t: 0 }
}

// Subconjunto estrutural de Body suficiente para classificar/transformar.
// Aceita qualquer Body (e portanto Player, que extends Body).
type AnimBody = Pick<Body, 'vx' | 'vy' | 'onGround'>

// Regras do contrato (ordem de prioridade importa):
//  iframes>0 -> 'hurt'
//  !onGround && vy<0 -> 'jump'
//  !onGround && vy>=0 -> 'fall'
//  |vx| > WALK_MAX+0.3 -> 'run'
//  |vx| > 0.3 -> 'walk'
//  senao -> 'idle'
export function classifyAnim(body: AnimBody, iframes: number): AnimState {
  if (iframes > 0) return 'hurt'
  if (!body.onGround) return body.vy < 0 ? 'jump' : 'fall'
  const speed = Math.abs(body.vx)
  if (speed > WALK_MAX + 0.3) return 'run'
  if (speed > 0.3) return 'walk'
  return 'idle'
}

// s=classifyAnim(...); se mudou o estado, zera t; senao acumula dt.
export function updateAnimator(
  an: Animator,
  body: AnimBody,
  iframes: number,
  dt: number,
): void {
  const s = classifyAnim(body, iframes)
  if (s !== an.state) {
    an.state = s
    an.t = 0
  } else {
    an.t += dt
  }
}

export interface SpriteTransform {
  scaleX: number
  scaleY: number
  rotation: number
  offsetY: number
}

// Transforms deterministicos por estado/t (a "fluidez").
// O param body nao e usado pelos transforms do contrato (eles dependem so de
// state/t); prefixado com _ para satisfazer noUnusedParameters mantendo a
// assinatura publica getTransform(an, body) do contrato.
export function getTransform(an: Animator, _body: AnimBody): SpriteTransform {
  const t = an.t
  switch (an.state) {
    case 'idle': {
      // respiracao: scaleY 1+sin(t*0.06)*0.03, scaleX inverso, offsetY=sin(t*0.06)*2
      const b = Math.sin(t * 0.06)
      return {
        scaleX: 1 - b * 0.03,
        scaleY: 1 + b * 0.03,
        rotation: 0,
        offsetY: b * 2,
      }
    }
    case 'walk': {
      // bob: offsetY=-abs(sin(t*0.18))*3, scaleX 1+sin*0.04, rotation sin*0.04
      const p = t * 0.18
      const s = Math.sin(p)
      return {
        scaleX: 1 + s * 0.04,
        scaleY: 1,
        rotation: s * 0.04,
        offsetY: -Math.abs(s) * 3,
      }
    }
    case 'run': {
      // bob/lean mais fortes + rotation sin(t*0.28)*0.06
      const s = Math.sin(t * 0.18)
      return {
        scaleX: 1 + s * 0.06,
        scaleY: 1,
        rotation: Math.sin(t * 0.28) * 0.06,
        offsetY: -Math.abs(s) * 5,
      }
    }
    case 'jump':
      // stretch: scaleY 1.15, scaleX 0.9
      return { scaleX: 0.9, scaleY: 1.15, rotation: 0, offsetY: 0 }
    case 'fall':
      // squash leve: scaleY 1.08, scaleX 0.96
      return { scaleX: 0.96, scaleY: 1.08, rotation: 0, offsetY: 0 }
    case 'hurt':
      // flat: (1,1,0,0) — o pisca dos i-frames fica no draw, nao aqui
      return { scaleX: 1, scaleY: 1, rotation: 0, offsetY: 0 }
  }
}
```

- [ ] **Rodar e ver PASS** — `npm run test`. Esperado: a suite `animator.test.ts` verde junto com as suites M0/M1 existentes (nenhuma regrediu, pois esta task so ADICIONA arquivo novo). Saida esperada:

```
✓ tests/unit/animator.test.ts (N tests)
Test Files  ... passed
```

- [ ] **Commit** — `git add src/engine/animator.ts tests/unit/animator.test.ts && git commit -m "M2a Task 2: engine/animator.ts animacao procedural (logica pura + testes)"`

---

### Task 3: engine/parallax.ts: camadas de background (DONO)

**Escopo:** Criar `src/engine/parallax.ts` EXATAMENTE conforme a interface `[parallax]` do contrato M2a: `ParallaxLayer` e `drawParallax`. Desenho em SCREEN SPACE (antes de `beginWorld`), deslocamento horizontal `-(cam.x * factor)`, e tiling para cobrir os 960px de largura mesmo com o deslocamento. Layer cujo `store.get(key)` retorna `null` e pulado sem erro. Consome `Renderer` (de `./render`), `Camera` (de `./camera`), `AssetStore`/`ImageAsset` (de `./assets`, DONO = Task 1) e `VIEW_W`/`VIEW_H` (de `./constants`) — somente via `import type` / `import`, sem recriar nada. Gate: `npm run test`.

**Files:**
- Create: `src/engine/parallax.ts` (DONO desta task)
- Test: `tests/unit/parallax.test.ts` (DONO desta task)
- Modify: nenhum (M0/M1 intactos; nada de `render.ts`)
- Consome via import: `src/engine/render.ts` (`Renderer`), `src/engine/camera.ts` (`Camera`), `src/engine/assets.ts` (`AssetStore`, `ImageAsset` — DONO Task 1), `src/engine/constants.ts` (`VIEW_W`, `VIEW_H`)

**Contexto verificado no repo (M0/M1):**
- `Renderer` expoe `ctx: CanvasRenderingContext2D` publico (`src/engine/render.ts` linhas 3-10) — usamos `r.ctx.drawImage(...)` diretamente, pois `Renderer` nao tem `drawImage`.
- `Camera` = `{ x: number; y: number }` (`src/engine/camera.ts` linhas 4-7).
- Constantes reais: `VIEW_W = 960`, `VIEW_H = 528` (`src/engine/constants.ts` linhas 3-4).
- `AssetStore.get(key)` retorna `ImageAsset | null`; `ImageAsset = { src: CanvasImageSource; w: number; h: number }` (contrato `[assets]`, arquivo `src/engine/assets.ts` da Task 1).
- Estilo de teste do repo: Vitest + jsdom, `vi.fn()` spies em stub de ctx, imports relativos `../../src/...` (ver `tests/unit/render.test.ts`).

**Decisao de tiling (deterministica):** Como `factor` pode ser fracionario, o offset `ox = -(cam.x * factor)` cai num intervalo arbitrario. Normalizamos para `[-imgW, 0]` com `startX = ((ox % imgW) + imgW) % imgW - imgW` e desenhamos copias `imgW`-a-`imgW` ate cobrir `VIEW_W`. A imagem e desenhada cobrindo a altura toda (`VIEW_H`); a largura de cada tile usa a largura natural escalada para a altura da viewport (`imgW = img.w * (VIEW_H / img.h)`), garantindo aspecto correto e cobertura sem buracos. Cada `drawImage` usa a forma de 9 argumentos `(src, 0,0, img.w,img.h, dx,0, imgW,VIEW_H)`.

**Passos TDD:**

- [ ] **(1) Escrever o teste que falha** — criar `tests/unit/parallax.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { VIEW_W, VIEW_H } from '../../src/engine/constants'
import { drawParallax, type ParallaxLayer } from '../../src/engine/parallax'
import type { Renderer } from '../../src/engine/render'
import type { Camera } from '../../src/engine/camera'
import type { AssetStore, ImageAsset } from '../../src/engine/assets'

// ctx stub: jsdom nao desenha; registramos as chamadas de drawImage.
function makeCtxStub() {
  return {
    drawImage: vi.fn(),
    save: vi.fn(),
    restore: vi.fn(),
    translate: vi.fn(),
    fillRect: vi.fn(),
    fillStyle: '' as string,
  }
}

// Renderer minimo: so precisamos do ctx (drawParallax usa r.ctx.drawImage).
function makeRenderer(ctx: ReturnType<typeof makeCtxStub>): Renderer {
  return { ctx } as unknown as Renderer
}

// ImageAsset fake: src e um objeto qualquer (CanvasImageSource nao e validado em jsdom).
function makeAsset(w: number, h: number): ImageAsset {
  return { src: { tag: 'img' } as unknown as CanvasImageSource, w, h }
}

// AssetStore fake controlado por um Map.
function makeStore(entries: Record<string, ImageAsset | null>): AssetStore {
  return {
    get: (key: string) => (key in entries ? entries[key] : null),
    ready: true,
  }
}

function makeCam(x: number, y = 0): Camera {
  return { x, y }
}

describe('drawParallax', () => {
  let ctx: ReturnType<typeof makeCtxStub>
  let r: Renderer

  beforeEach(() => {
    ctx = makeCtxStub()
    r = makeRenderer(ctx)
  })

  it('desenha a imagem com deslocamento -(cam.x*factor) (factor 0.5, cam.x=100 -> -50)', () => {
    // img quadrada 528x528 -> escalada p/ altura 528 mantem largura 528 (imgW = VIEW_H).
    const store = makeStore({ 'bg.sky': makeAsset(VIEW_H, VIEW_H) })
    const layers: ParallaxLayer[] = [{ key: 'bg.sky', factor: 0.5 }]
    drawParallax(r, layers, store, makeCam(100))

    expect(ctx.drawImage).toHaveBeenCalled()
    // startX = normalizado p/ [-imgW,0]: ox=-50, imgW=528 -> startX=-50.
    // A primeira copia desenhada deve comecar em dx=-50.
    const calls = ctx.drawImage.mock.calls
    const firstDx = calls[0][5] // dx e o 6o argumento (src,sx,sy,sw,sh,dx,dy,dw,dh)
    expect(firstDx).toBe(-50)
    // Cada copia cobre a altura toda do canvas (dh = VIEW_H), ancorada em dy=0.
    expect(calls[0][6]).toBe(0) // dy
    expect(calls[0][8]).toBe(VIEW_H) // dh
  })

  it('faz tiling para cobrir os 960px de largura (>=2 copias quando imgW < VIEW_W)', () => {
    const store = makeStore({ 'bg.sky': makeAsset(VIEW_H, VIEW_H) }) // imgW=528 < 960
    drawParallax(r, [{ key: 'bg.sky', factor: 0 }], store, makeCam(0))
    // ox=0 -> startX = -imgW = -528; copias em -528, 0, 528, ... ate passar 960.
    // Precisa de pelo menos 3 copias p/ cobrir [0,960] partindo de -528.
    expect(ctx.drawImage.mock.calls.length).toBeGreaterThanOrEqual(3)
    const dxs = ctx.drawImage.mock.calls.map((c) => c[5] as number)
    expect(dxs).toContain(-528)
    expect(dxs).toContain(0)
    // a ultima copia comeca antes de VIEW_W e cobre ate >= VIEW_W
    const last = dxs[dxs.length - 1]
    expect(last).toBeLessThan(VIEW_W)
    expect(last + VIEW_H).toBeGreaterThanOrEqual(VIEW_W)
  })

  it('layer ausente (store.get null) e pulado sem desenhar e sem lancar', () => {
    const store = makeStore({}) // get sempre null
    expect(() =>
      drawParallax(r, [{ key: 'bg.sky', factor: 0.3 }], store, makeCam(200))
    ).not.toThrow()
    expect(ctx.drawImage).not.toHaveBeenCalled()
  })

  it('lista de layers vazia nao desenha nada e nao lanca', () => {
    const store = makeStore({ 'bg.sky': makeAsset(VIEW_H, VIEW_H) })
    expect(() => drawParallax(r, [], store, makeCam(0))).not.toThrow()
    expect(ctx.drawImage).not.toHaveBeenCalled()
  })

  it('factor 0 ignora cam.x (fundo distante fixo): startX nao muda com cam.x', () => {
    const store = makeStore({ 'bg.sky': makeAsset(VIEW_H, VIEW_H) })
    drawParallax(r, [{ key: 'bg.sky', factor: 0 }], store, makeCam(0))
    const a = (ctx.drawImage.mock.calls[0][5] as number)
    ctx.drawImage.mockClear()
    drawParallax(r, [{ key: 'bg.sky', factor: 0 }], store, makeCam(9999))
    const b = (ctx.drawImage.mock.calls[0][5] as number)
    expect(a).toBe(b) // factor 0 -> deslocamento independe de cam.x
  })

  it('desenha multiplas camadas na ordem da lista (back-to-front)', () => {
    const store = makeStore({
      'bg.far': makeAsset(VIEW_H, VIEW_H),
      'bg.near': makeAsset(VIEW_H, VIEW_H),
    })
    drawParallax(
      r,
      [
        { key: 'bg.far', factor: 0.1 },
        { key: 'bg.near', factor: 0.6 },
      ],
      store,
      makeCam(0)
    )
    // ambas as camadas desenharam (>= 2 chamadas no total)
    expect(ctx.drawImage.mock.calls.length).toBeGreaterThanOrEqual(2)
  })

  it('protege contra asset degenerado (h<=0): nao lanca e nao desenha', () => {
    const store = makeStore({ 'bg.sky': makeAsset(0, 0) })
    expect(() =>
      drawParallax(r, [{ key: 'bg.sky', factor: 0.3 }], store, makeCam(10))
    ).not.toThrow()
    expect(ctx.drawImage).not.toHaveBeenCalled()
  })
})
```

- [ ] **(2) Rodar e ver FALHAR** — `npm run test`. Saida esperada (resumida):

```
FAIL  tests/unit/parallax.test.ts
  Failed to resolve import "../../src/engine/parallax" from "tests/unit/parallax.test.ts". Does the file exist?
```

(Falha de import porque `src/engine/parallax.ts` ainda nao existe — exatamente o esperado no passo vermelho do TDD. Os outros testes do repo continuam passando.)

- [ ] **(3) Implementar** — criar `src/engine/parallax.ts` com o CODIGO COMPLETO:

```ts
import { VIEW_W, VIEW_H } from './constants'
import type { Renderer } from './render'
import type { Camera } from './camera'
import type { AssetStore } from './assets'

export interface ParallaxLayer {
  key: string
  factor: number // 0 = fixo (ceu distante); 1 = acompanha o mundo
}

/**
 * Desenha as camadas de parallax em SCREEN SPACE (chamar ANTES de beginWorld).
 * Para cada layer: img = store.get(key). Se null, pula sem erro.
 * A imagem cobre a altura toda da viewport (VIEW_H), ancorada no topo (dy=0),
 * e e deslocada horizontalmente por -(cam.x * factor), repetindo (tiling)
 * para cobrir os VIEW_W (960) px de largura mesmo com offsets fracionarios.
 * Ordem da lista = back-to-front (primeira camada e a mais ao fundo).
 */
export function drawParallax(
  r: Renderer,
  layers: ParallaxLayer[],
  store: AssetStore,
  cam: Camera
): void {
  const ctx = r.ctx

  for (const layer of layers) {
    const img = store.get(layer.key)
    if (!img) continue // layer ausente -> pula sem desenhar

    // Asset degenerado (sem dimensoes validas) -> ignora com seguranca.
    if (!(img.w > 0) || !(img.h > 0)) continue

    // Largura de cada tile escalada para preencher a altura da viewport,
    // preservando o aspecto natural da imagem.
    const imgW = img.w * (VIEW_H / img.h)
    if (!(imgW > 0) || !Number.isFinite(imgW)) continue

    // Deslocamento horizontal; factor 0 => fixo, factor 1 => acompanha o mundo.
    const ox = -(cam.x * layer.factor)

    // Normaliza o ponto de partida para o intervalo [-imgW, 0],
    // assim a primeira copia comeca imediatamente a esquerda da borda esquerda.
    const startX = (((ox % imgW) + imgW) % imgW) - imgW

    // Desenha copias imgW-a-imgW ate cobrir toda a largura do canvas.
    for (let dx = startX; dx < VIEW_W; dx += imgW) {
      ctx.drawImage(
        img.src,
        0,
        0,
        img.w,
        img.h, // recorte de origem (imagem inteira)
        dx,
        0,
        imgW,
        VIEW_H // destino: cobre a altura toda, ancorado no topo
      )
    }
  }
}
```

- [ ] **(4) Rodar e ver PASSAR** — `npm run test`. Saida esperada (resumida):

```
PASS  tests/unit/parallax.test.ts  (7 tests)
Test Files  <N> passed (<N>)
     Tests  <M> passed (<M>)
```

Verificacoes cobertas pelo teste:
- factor 0.5 + cam.x=100 -> primeira copia em `dx = -50` (deslocamento `-(cam.x*factor)` correto).
- tiling cobre os 960px (>=3 copias quando `imgW=528 < VIEW_W`), incluindo `dx = -528` e `dx = 0`, ultima copia comecando antes de 960 e cobrindo ate >= 960.
- layer ausente (`store.get` null) e pulado: zero `drawImage`, sem throw.
- lista vazia: zero `drawImage`, sem throw.
- factor 0 -> deslocamento independe de `cam.x` (fundo fixo).
- multiplas camadas desenham back-to-front.
- asset degenerado (`h<=0`) e ignorado com seguranca.

- [ ] **(5) Sanidade de tipos local (opcional, dentro do gate da task)** — `npm run test` ja roda o typecheck do Vitest (`typecheck.tsconfig` em `vitest.config.ts`). O `tsc -p tsconfig.json --noEmit` e o `build`/`e2e` do projeto inteiro ficam para a **Task 7 (verify)**, nao para esta task.

- [ ] **(6) Commit** — `git add src/engine/parallax.ts tests/unit/parallax.test.ts && git commit -m "M2a Task 3: engine/parallax.ts (drawParallax screen-space + tiling)"`

```
Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
```

**Notas de contrato / nao-quebra:**
- Esta task NAO toca `render.ts` nem nenhum arquivo M0/M1; so cria `parallax.ts` e seu teste.
- `AssetStore`/`ImageAsset` sao apenas IMPORTADOS de `src/engine/assets.ts` (DONO = Task 1). Se a Task 1 ainda nao tiver landado, o teste de `parallax` falha no import de `assets` — o que e esperado na execucao paralela; o gate `npm run test` desta task assume `assets.ts` ja disponivel (dependencia de import declarada acima).
- `drawParallax` usa `r.ctx.drawImage` porque o `Renderer` de M0 nao expoe `drawImage` (so `drawRect`); o `ctx` e publico e isso esta dentro do contrato (so `render.ts` e imutavel, e nao o estamos alterando).
- A integracao no `game.ts`/`main.ts` (chamar `drawParallax(renderer, SKY_LAYERS, store, cam)` depois de `clear` e antes de `beginWorld`) e responsabilidade da **task de integracao** (DONO de `game.ts`/`main.ts`), nao desta.

---

### Task 4: engine/particles.ts: sistema de particulas (DONO)

Sistema de particulas para a camada de FX do M2a: poeira ambiente de gravidade-zero (sobe devagar, rate-limited + cap) e rajadas radiais (`emitBurst`) para o Humanware/FX. Logica de integracao pura + um draw que escreve apenas via `r.drawRect` e `r.ctx.globalAlpha` (o unico campo de ctx que o desenho toca). Este arquivo e CONSUMIDO depois pela tarefa de integracao (game.ts/main.ts), que NAO faz parte desta tarefa.

DONO unico deste arquivo. Consome apenas tipos/API ja existentes (`Renderer` de `../engine/render`). Nao recriar nem editar nenhum arquivo de outra tarefa (assets/animator/parallax/spriteDraw/manifest/game/main).

**Files:**
- Create: `src/engine/particles.ts` (DONO — unico arquivo que esta tarefa cria)
- Test: `tests/unit/particles.test.ts` (DONO)
- Modify: nenhum (M0/M1 intactos; nada a alterar em render.ts/constants.ts)

Notas de ambiente:
- jsdom nao tem canvas real. O teste de `drawParticles` usa um `Renderer` fake com `drawRect` espionado (`vi.fn()`) e um `ctx` stub com `globalAlpha` setavel (mesmo padrao de `tests/unit/ability.test.ts` e `tests/unit/render.test.ts`).
- `drawParticles` so pode tocar `r.drawRect(...)` e `r.ctx.globalAlpha` (nada de `fillStyle`/`fillRect` diretos — cor vai pelo 5o arg de `drawRect`).
- Gate desta tarefa: `npm run test`. (tsc/build/e2e do projeto inteiro = Task 7.)

---

#### Passo 1 — Teste que FALHA: estrutura + `createParticles` + `emitBurst` (adiciona n)

- [ ] Escrever `tests/unit/particles.test.ts` com o bloco abaixo (apenas a parte 1 por enquanto; o arquivo final do Passo 5 ja contem tudo):

```ts
// tests/unit/particles.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import {
  createParticles,
  emitAmbient,
  emitBurst,
  updateParticles,
  drawParticles,
  type ParticleSystem,
  type Particle,
} from '../../src/engine/particles'
import type { Renderer } from '../../src/engine/render'
import { COLOR_MAGENTA, COLOR_LIME } from '../../src/engine/constants'

describe('createParticles', () => {
  it('comeca com lista de particulas vazia', () => {
    const ps = createParticles()
    expect(ps.particles).toEqual([])
  })
})

describe('emitBurst', () => {
  it('adiciona exatamente n particulas', () => {
    const ps = createParticles()
    emitBurst(ps, 100, 50, 24, [COLOR_MAGENTA, COLOR_LIME])
    expect(ps.particles.length).toBe(24)
  })

  it('todas nascem na origem (x,y) dada', () => {
    const ps = createParticles()
    emitBurst(ps, 100, 50, 8, [COLOR_MAGENTA])
    for (const p of ps.particles) {
      expect(p.x).toBe(100)
      expect(p.y).toBe(50)
    }
  })

  it('usa apenas cores da paleta passada e produz velocidades radiais finitas', () => {
    const ps = createParticles()
    const colors = [COLOR_MAGENTA, COLOR_LIME]
    emitBurst(ps, 0, 0, 16, colors)
    for (const p of ps.particles) {
      expect(colors).toContain(p.color)
      expect(Number.isFinite(p.vx)).toBe(true)
      expect(Number.isFinite(p.vy)).toBe(true)
      expect(p.maxLife).toBeGreaterThan(0)
      expect(p.life).toBe(p.maxLife)
      expect(p.size).toBeGreaterThan(0)
    }
    // radial: pelo menos uma com vx>0 e uma com vx<0 (n=16 cobre o circulo)
    expect(ps.particles.some((p) => p.vx > 0)).toBe(true)
    expect(ps.particles.some((p) => p.vx < 0)).toBe(true)
  })

  it('emitBurst com cores vazias e seguro (fallback de cor), ainda adiciona n', () => {
    const ps = createParticles()
    emitBurst(ps, 0, 0, 5, [])
    expect(ps.particles.length).toBe(5)
    for (const p of ps.particles) expect(typeof p.color).toBe('string')
  })
})
```

#### Passo 2 — Teste que FALHA: `updateParticles` (integra + remove mortas)

- [ ] Acrescentar ao mesmo arquivo de teste:

```ts
describe('updateParticles', () => {
  it('integra posicao: x+=vx*dt, y+=vy*dt', () => {
    const ps = createParticles()
    ps.particles.push({ x: 10, y: 20, vx: 4, vy: -2, life: 60, maxLife: 60, color: '#fff', size: 3 })
    updateParticles(ps, 1)
    expect(ps.particles[0].x).toBeCloseTo(14, 5)
    expect(ps.particles[0].y).toBeCloseTo(18, 5)
  })

  it('escala o passo por dt (dt=0.5 move metade)', () => {
    const ps = createParticles()
    ps.particles.push({ x: 0, y: 0, vx: 10, vy: 10, life: 60, maxLife: 60, color: '#fff', size: 3 })
    updateParticles(ps, 0.5)
    expect(ps.particles[0].x).toBeCloseTo(5, 5)
    expect(ps.particles[0].y).toBeCloseTo(5, 5)
  })

  it('decrementa life em dt', () => {
    const ps = createParticles()
    ps.particles.push({ x: 0, y: 0, vx: 0, vy: 0, life: 10, maxLife: 10, color: '#fff', size: 3 })
    updateParticles(ps, 3)
    expect(ps.particles[0].life).toBeCloseTo(7, 5)
  })

  it('remove particulas com life<=0', () => {
    const ps = createParticles()
    ps.particles.push({ x: 0, y: 0, vx: 0, vy: 0, life: 2, maxLife: 10, color: '#fff', size: 3 })
    ps.particles.push({ x: 0, y: 0, vx: 0, vy: 0, life: 9, maxLife: 10, color: '#fff', size: 3 })
    updateParticles(ps, 2) // primeira chega a 0 -> removida; segunda fica em 7
    expect(ps.particles.length).toBe(1)
    expect(ps.particles[0].life).toBeCloseTo(7, 5)
  })

  it('com lista vazia nao lanca', () => {
    const ps = createParticles()
    expect(() => updateParticles(ps, 1)).not.toThrow()
    expect(ps.particles.length).toBe(0)
  })
})
```

#### Passo 3 — Teste que FALHA: `emitAmbient` (rate-limited + cap)

- [ ] Acrescentar ao mesmo arquivo de teste:

```ts
describe('emitAmbient', () => {
  it('e rate-limited: 1 chamada nao adiciona mais de 1 particula', () => {
    const ps = createParticles()
    emitAmbient(ps, 960, 528, 1)
    expect(ps.particles.length).toBeLessThanOrEqual(1)
  })

  it('ao longo de muitas chamadas chega a emitir poeira (lista cresce)', () => {
    const ps = createParticles()
    for (let i = 0; i < 40; i++) emitAmbient(ps, 960, 528, 1)
    expect(ps.particles.length).toBeGreaterThan(0)
  })

  it('respeita o cap (~60): nunca passa de 60 mesmo apos muitas chamadas', () => {
    const ps = createParticles()
    for (let i = 0; i < 5000; i++) emitAmbient(ps, 960, 528, 1)
    expect(ps.particles.length).toBeLessThanOrEqual(60)
  })

  it('poeira nasce dentro da area e sobe (vy<=0)', () => {
    const ps = createParticles()
    for (let i = 0; i < 5000; i++) emitAmbient(ps, 960, 528, 1)
    expect(ps.particles.length).toBeGreaterThan(0)
    for (const p of ps.particles) {
      expect(p.x).toBeGreaterThanOrEqual(0)
      expect(p.x).toBeLessThanOrEqual(960)
      expect(p.y).toBeGreaterThanOrEqual(0)
      expect(p.y).toBeLessThanOrEqual(528)
      expect(p.vy).toBeLessThanOrEqual(0) // gravidade-zero: sobe devagar
      expect(p.maxLife).toBeGreaterThan(0)
    }
  })
})
```

#### Passo 4 — Teste que FALHA: `drawParticles` (via `r.drawRect` + `globalAlpha`)

- [ ] Acrescentar ao mesmo arquivo de teste (Renderer fake com `drawRect` espiao e ctx com `globalAlpha`):

```ts
function makeCtxStub() {
  return { globalAlpha: 1 as number }
}
function makeRenderer(ctx: ReturnType<typeof makeCtxStub>): Renderer {
  return {
    ctx: ctx as unknown as CanvasRenderingContext2D,
    clear: vi.fn(),
    beginWorld: vi.fn(),
    endWorld: vi.fn(),
    drawRect: vi.fn(),
    present: vi.fn(),
  }
}

describe('drawParticles', () => {
  let ctx: ReturnType<typeof makeCtxStub>
  let r: Renderer
  beforeEach(() => {
    ctx = makeCtxStub()
    r = makeRenderer(ctx)
  })

  it('desenha um drawRect por particula', () => {
    const ps = createParticles()
    emitBurst(ps, 50, 50, 7, [COLOR_MAGENTA])
    drawParticles(r, ps)
    expect((r.drawRect as ReturnType<typeof vi.fn>).mock.calls.length).toBe(7)
  })

  it('lista vazia: nenhum drawRect', () => {
    const ps = createParticles()
    drawParticles(r, ps)
    expect((r.drawRect as ReturnType<typeof vi.fn>)).not.toHaveBeenCalled()
  })

  it('restaura globalAlpha para 1 ao terminar', () => {
    const ps = createParticles()
    ps.particles.push({ x: 0, y: 0, vx: 0, vy: 0, life: 5, maxLife: 10, color: COLOR_LIME, size: 3 })
    drawParticles(r, ps)
    expect(ctx.globalAlpha).toBe(1)
  })

  it('passa a cor da particula como 5o argumento de drawRect', () => {
    const ps = createParticles()
    ps.particles.push({ x: 12, y: 34, vx: 0, vy: 0, life: 5, maxLife: 10, color: COLOR_MAGENTA, size: 4 })
    drawParticles(r, ps)
    const call = (r.drawRect as ReturnType<typeof vi.fn>).mock.calls[0]
    expect(call[4]).toBe(COLOR_MAGENTA)
  })

  it('ajusta globalAlpha = life/maxLife durante o desenho de cada particula', () => {
    const ps = createParticles()
    ps.particles.push({ x: 0, y: 0, vx: 0, vy: 0, life: 5, maxLife: 10, color: COLOR_LIME, size: 3 })
    const seen: number[] = []
    ;(r.drawRect as ReturnType<typeof vi.fn>).mockImplementation(() => {
      seen.push(ctx.globalAlpha)
    })
    drawParticles(r, ps)
    expect(seen[0]).toBeCloseTo(0.5, 5) // 5/10
    expect(ctx.globalAlpha).toBe(1) // restaurado no fim
  })
})

// Suprime aviso de import de tipo nao usado diretamente.
void (null as unknown as ParticleSystem)
void (null as unknown as Particle)
```

#### Passo 5 — RODAR e ver FALHAR (modulo ainda nao existe)

- [ ] Comando: `npm run test`
- [ ] Saida esperada (resumo): a suite `tests/unit/particles.test.ts` falha no import — `Failed to resolve import "../../src/engine/particles"` / `Cannot find module`. Demais suites M0/M1 continuam passando. (RED.)

#### Passo 6 — IMPLEMENTAR `src/engine/particles.ts` (codigo TS completo)

- [ ] Criar `src/engine/particles.ts`:

```ts
// src/engine/particles.ts
// Sistema de particulas do M2a (DONO).
// - emitAmbient: poeira de gravidade-zero, sobe devagar, rate-limited + cap.
// - emitBurst: explosao radial (Humanware / FX de habilidade).
// - updateParticles: integracao pura (x+=vx*dt, y+=vy*dt), life-=dt, remove mortas.
// - drawParticles: desenha cada particula via r.drawRect; alpha = life/maxLife
//   atraves de r.ctx.globalAlpha (restaurado para 1 no fim).
import type { Renderer } from './render'

export interface Particle {
  x: number
  y: number
  vx: number
  vy: number
  life: number
  maxLife: number
  color: string
  size: number
}

export interface ParticleSystem {
  particles: Particle[]
  // Contador interno de frames para rate-limit da poeira ambiente.
  // Nao faz parte da API publica, mas vive no estado do sistema.
  ambientAcc: number
}

// Limites/ritmo da poeira ambiente.
const AMBIENT_CAP = 60 // maximo de particulas de poeira simultaneas
const AMBIENT_EVERY = 6 // emite ~1 poeira a cada 6 unidades de dt acumuladas
const AMBIENT_COLOR = '#f7f3f6' // COLOR_TEXT-ish: poeira clara discreta

export function createParticles(): ParticleSystem {
  return { particles: [], ambientAcc: 0 }
}

export function emitAmbient(ps: ParticleSystem, w: number, h: number, dt: number): void {
  // Rate-limit por acumulo de dt: so emite ao cruzar o limiar.
  ps.ambientAcc += dt
  if (ps.ambientAcc < AMBIENT_EVERY) return
  ps.ambientAcc -= AMBIENT_EVERY

  // Respeita o cap: conta apenas a poeira ambiente (size pequeno + vy<=0).
  if (ps.particles.length >= AMBIENT_CAP) return

  const maxLife = 180 + Math.random() * 120 // ~3-5s a 60fps
  ps.particles.push({
    x: Math.random() * w,
    y: Math.random() * h,
    vx: (Math.random() - 0.5) * 0.15, // deriva horizontal minima
    vy: -(0.12 + Math.random() * 0.25), // sobe devagar (gravidade-zero)
    life: maxLife,
    maxLife,
    color: AMBIENT_COLOR,
    size: 1 + Math.floor(Math.random() * 2), // 1-2 px
  })
}

export function emitBurst(
  ps: ParticleSystem,
  x: number,
  y: number,
  n: number,
  colors: string[]
): void {
  const palette = colors.length > 0 ? colors : ['#ffffff']
  for (let i = 0; i < n; i++) {
    // Distribui radialmente ao redor do circulo, com leve jitter.
    const angle = (i / n) * Math.PI * 2 + Math.random() * 0.3
    const speed = 1.8 + Math.random() * 2.2
    const maxLife = 22 + Math.random() * 16
    ps.particles.push({
      x,
      y,
      vx: Math.cos(angle) * speed,
      vy: Math.sin(angle) * speed,
      life: maxLife,
      maxLife,
      color: palette[i % palette.length],
      size: 2 + Math.floor(Math.random() * 3), // 2-4 px
    })
  }
}

export function updateParticles(ps: ParticleSystem, dt: number): void {
  const out: Particle[] = []
  for (const p of ps.particles) {
    p.x += p.vx * dt
    p.y += p.vy * dt
    p.life -= dt
    if (p.life > 0) out.push(p)
  }
  ps.particles = out
}

export function drawParticles(r: Renderer, ps: ParticleSystem): void {
  const ctx = r.ctx
  for (const p of ps.particles) {
    const a = p.maxLife > 0 ? p.life / p.maxLife : 0
    ctx.globalAlpha = a < 0 ? 0 : a > 1 ? 1 : a
    // Centra o quadradinho na posicao da particula.
    const half = p.size / 2
    r.drawRect(p.x - half, p.y - half, p.size, p.size, p.color)
  }
  // Restaura para nao vazar alpha para o resto do frame.
  ctx.globalAlpha = 1
}
```

Notas de conformidade com a interface canonica `[particles]`:
- Tipos `Particle`/`ParticleSystem` e funcoes com as assinaturas EXATAS do contrato. `ParticleSystem.particles: Particle[]` permanece o campo publico; `ambientAcc` e estado interno do rate-limit (criado por `createParticles`, nao exposto na assinatura). Os testes so dependem de `.particles`.
- `emitAmbient` rate-limited (acumulo de dt) + cap (`AMBIENT_CAP = 60`); poeira nasce dentro de `w x h` e sobe (`vy <= 0`).
- `emitBurst` adiciona exatamente `n` (radial), com fallback de cor se `colors` vier vazio.
- `updateParticles` integra `x+=vx*dt`, `y+=vy*dt`, `life-=dt` e remove `life<=0`.
- `drawParticles` desenha so via `r.drawRect`; alpha por `r.ctx.globalAlpha = life/maxLife`, restaurado para `1` no fim.

#### Passo 7 — RODAR e ver PASSAR

- [ ] Comando: `npm run test`
- [ ] Saida esperada (resumo): `tests/unit/particles.test.ts` passa (todos os `describe`: createParticles, emitBurst, updateParticles, emitAmbient, drawParticles). Nenhuma suite M0/M1 regrediu — total verde. (GREEN.)

#### Passo 8 — Commit

- [ ] `git add src/engine/particles.ts tests/unit/particles.test.ts`
- [ ] `git commit -m "M2a Task 4: engine/particles.ts (sistema de particulas: ambient+burst+update+draw) + testes"`

---

**Gate da tarefa:** `npm run test` verde (a suite nova + M0/M1 intactas). O `tsc -p tsconfig.json --noEmit` / `npm run build` / e2e do projeto inteiro pertencem a Task 7 (verificacao).

**Decisoes de implementacao (registradas):**
- O contador de rate-limit da poeira (`ambientAcc`) vive no `ParticleSystem` como estado interno, criado por `createParticles`. Alternativa (variavel de modulo) foi descartada por nao ser reentrante/testavel com multiplos sistemas.
- Cores default `['#ffffff']` em `emitBurst` quando `colors` vazio — evita `undefined` em `p.color` (mantem `getTransform`/draw deterministicos e finitos). A chamada real no game usa `[COLOR_MAGENTA, COLOR_LIME]` conforme a tarefa de integracao.
- `drawParticles` toca exclusivamente `r.drawRect` e `r.ctx.globalAlpha` (compativel com jsdom: o ctx stub do teste so precisa de um `globalAlpha` setavel), respeitando a regra de nao alterar `render.ts`.

---

### Task 5: spriteDraw.ts + data/assets.ts + arte em public/assets (DONO)

Constrói o desenho do sprite animado real do Artur (âncora nos pés, `SPRITE_DRAW_H=90`, aplicando o `SpriteTransform` do animator via `ctx.save/translate/scale/rotate/drawImage`), o manifesto de assets (`ASSET_MANIFEST` + `SKY_LAYERS`) e copia a arte real para `public/assets`. Esta tarefa é DONA de `src/engine/spriteDraw.ts` e `src/data/assets.ts` e da arte em `public/assets/**`. Ela apenas CONSOME (import) os módulos donos de outras tarefas — `Animator`/`getTransform`/`SpriteTransform` de `src/engine/animator.ts`, `ImageAsset` de `src/engine/assets.ts`, `AssetEntry` de `src/engine/assets.ts`, `ParallaxLayer` de `src/engine/parallax.ts`, `Renderer` de `src/engine/render.ts` — e NUNCA recria nenhum deles.

> Pré-requisito de build: esta tarefa importa tipos de `src/engine/animator.ts`, `src/engine/assets.ts` e `src/engine/parallax.ts`. O **gate desta tarefa é `npm run test`** (Vitest roda arquivo a arquivo; os testes de `spriteDraw` e `assets`-manifest só importam os tipos que já existem quando as tarefas donas tiverem entrado). O `tsc -p` / `build` / `e2e` do projeto inteiro é a Task 7.

**Files:**
- Create: `src/engine/spriteDraw.ts` (DONO)
- Create: `src/data/assets.ts` (DONO)
- Create (arte, DONO): `public/assets/chars/artur.png`, `public/assets/bg/sky.png`, `public/assets/bg/cosmic.png` (copiados de `.m2-samples/`)
- Test: `tests/unit/spriteDraw.test.ts` (DONO)
- Test: `tests/unit/assetsManifest.test.ts` (DONO)
- Modify: nenhum arquivo M0/M1 (Task 5 não modifica `game.ts`/`main.ts` — isso é a tarefa de integração)
- Consume (import only, NÃO criar/editar): `src/engine/render.ts`, `src/engine/animator.ts`, `src/engine/assets.ts`, `src/engine/parallax.ts`, `src/engine/constants.ts`

---

#### Passo 1 — Teste que falha: `drawAnimatedSprite`

- [ ] Escrever `tests/unit/spriteDraw.test.ts`. Em jsdom não há canvas real, então usamos um `ctx` espião (spies) que registra as chamadas e um `ImageAsset` falso (`src` é qualquer objeto — não desenhamos de verdade). Verificamos: (a) com asset != null, chama `save → translate → scale → rotate → drawImage(asset.src, ...)` exatamente uma vez cada e fecha com `restore`; (b) com asset === null, NÃO chama `drawImage` (o game faz fallback para `drawPlaceholder`); (c) a âncora horizontal é o centro e o `scaleX` recebe o sinal de `facing` (facing=-1 espelha).

```ts
// tests/unit/spriteDraw.test.ts
import { describe, it, expect, vi } from 'vitest'
import type { Renderer } from '../../src/engine/render'
import type { ImageAsset } from '../../src/engine/assets'
import { createAnimator } from '../../src/engine/animator'
import { drawAnimatedSprite } from '../../src/engine/spriteDraw'

// ctx espiao: jsdom nao desenha; registramos as chamadas.
function makeCtxStub() {
  return {
    canvas: { width: 960, height: 528 },
    globalAlpha: 1,
    save: vi.fn(),
    restore: vi.fn(),
    translate: vi.fn(),
    scale: vi.fn(),
    rotate: vi.fn(),
    drawImage: vi.fn(),
  }
}

// Renderer falso que so expoe o ctx (drawAnimatedSprite usa r.ctx.*).
function makeRenderer(ctx: ReturnType<typeof makeCtxStub>): Renderer {
  return {
    ctx: ctx as unknown as CanvasRenderingContext2D,
    clear: vi.fn(),
    beginWorld: vi.fn(),
    endWorld: vi.fn(),
    drawRect: vi.fn(),
    present: vi.fn(),
  }
}

// ImageAsset falso: src e so um marcador; drawImage nunca toca o pixel real.
const FAKE_SRC = { tag: 'fake-image' } as unknown as CanvasImageSource
const asset: ImageAsset = { src: FAKE_SRC, w: 64, h: 96 }

const body = { vx: 0, vy: 0, onGround: true }

describe('drawAnimatedSprite', () => {
  it('com asset desenha 1x via save/translate/scale/rotate/drawImage e fecha com restore', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    const an = createAnimator()
    drawAnimatedSprite(r, asset, an, 100, 200, 34, 42, 1, body)

    expect(ctx.save).toHaveBeenCalledTimes(1)
    expect(ctx.translate).toHaveBeenCalledTimes(1)
    expect(ctx.scale).toHaveBeenCalledTimes(1)
    expect(ctx.rotate).toHaveBeenCalledTimes(1)
    expect(ctx.drawImage).toHaveBeenCalledTimes(1)
    expect(ctx.restore).toHaveBeenCalledTimes(1)
    // o primeiro argumento de drawImage e a IMAGEM do asset
    expect(ctx.drawImage.mock.calls[0][0]).toBe(FAKE_SRC)
  })

  it('asset null NAO desenha (fallback fica no game via drawPlaceholder)', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    const an = createAnimator()
    drawAnimatedSprite(r, null, an, 100, 200, 34, 42, 1, body)

    expect(ctx.drawImage).not.toHaveBeenCalled()
    expect(ctx.save).not.toHaveBeenCalled()
    expect(ctx.restore).not.toHaveBeenCalled()
  })

  it('ancora no centro horizontal: translate recebe x exato passado (centro do corpo)', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    const an = createAnimator()
    drawAnimatedSprite(r, asset, an, 100, 200, 34, 42, 1, body)
    // translate(x, y + offsetY): primeiro arg e o centro horizontal recebido
    expect(ctx.translate.mock.calls[0][0]).toBe(100)
  })

  it('facing=-1 espelha: scaleX fica negativo', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    const an = createAnimator()
    drawAnimatedSprite(r, asset, an, 100, 200, 34, 42, -1, body)
    // scale(facing*scaleX, scaleY) -> primeiro arg negativo
    expect(ctx.scale.mock.calls[0][0]).toBeLessThan(0)
  })

  it('todos os argumentos de translate/scale/rotate sao finitos', () => {
    const ctx = makeCtxStub()
    const r = makeRenderer(ctx)
    const an = createAnimator()
    drawAnimatedSprite(r, asset, an, 100, 200, 34, 42, 1, body)
    for (const args of [
      ...ctx.translate.mock.calls,
      ...ctx.scale.mock.calls,
      ...ctx.rotate.mock.calls,
    ]) {
      for (const v of args) expect(Number.isFinite(v as number)).toBe(true)
    }
  })
})
```

- [ ] Rodar e confirmar que FALHA por módulo inexistente:

```bash
npm run test -- tests/unit/spriteDraw.test.ts
# Saida esperada (falha de import — spriteDraw.ts ainda nao existe):
#  FAIL  tests/unit/spriteDraw.test.ts [ tests/unit/spriteDraw.test.ts ]
#  Error: Failed to load url ../../src/engine/spriteDraw (resolved id: .../src/engine/spriteDraw)
```

---

#### Passo 2 — Implementar `src/engine/spriteDraw.ts`

- [ ] Criar o arquivo. Âncora nos pés (`x` = centro horizontal, `y` = base/pé do corpo). Escala o asset para caber em `SPRITE_DRAW_H = 90` (maior que a hitbox 34x42), com largura proporcional ao aspecto do asset. Aplica o `SpriteTransform` de `getTransform(an, body)` exatamente na ordem do contrato: `save → translate(x, y+offsetY) → scale(facing*scaleX, scaleY) → rotate(rotation) → drawImage(...) → restore`. Asset `null` → não desenha nada (o `game` chama `drawPlaceholder` como fallback).

```ts
// src/engine/spriteDraw.ts
import type { Renderer } from './render'
import type { ImageAsset } from './assets'
import type { Animator } from './animator'
import { getTransform } from './animator'

/**
 * Altura-alvo do sprite desenhado (px). Maior que a hitbox 34x42 para o
 * personagem parecer maior que a colisao, como num platformer classico.
 */
export const SPRITE_DRAW_H = 90

/**
 * Desenha o sprite animado do personagem ancorado nos PES.
 *
 * @param r      Renderer (usa r.ctx.* diretamente).
 * @param asset  ImageAsset ja carregado (ou null -> nao desenha; fallback no game).
 * @param an     Animator (estado/t) -> getTransform produz a "fluidez".
 * @param x      Centro HORIZONTAL do corpo (x do corpo + w/2).
 * @param y      Base/PE do corpo (y do corpo + h).
 * @param w      Largura da hitbox (referencia; nao usada para escalar a altura).
 * @param h      Altura da hitbox (referencia; nao usada para escalar a altura).
 * @param facing 1 (direita) | -1 (esquerda) -> espelha em X.
 * @param body   { vx, vy, onGround } -> entrada de getTransform.
 */
export function drawAnimatedSprite(
  r: Renderer,
  asset: ImageAsset | null,
  an: Animator,
  x: number,
  y: number,
  w: number,
  h: number,
  facing: 1 | -1,
  body: { vx: number; vy: number; onGround: boolean },
): void {
  // Sem arte -> nao desenha; o game faz fallback para drawPlaceholder.
  if (asset === null) return

  const ctx = r.ctx
  const tf = getTransform(an, body)

  // Altura desenhada fixa em SPRITE_DRAW_H; largura proporcional ao aspecto.
  const aspect = asset.w > 0 && asset.h > 0 ? asset.w / asset.h : 1
  const drawH = SPRITE_DRAW_H
  const drawW = drawH * aspect

  ctx.save()
  // Origem nos pes (x = centro horizontal, y = base) + respiracao/bob vertical.
  ctx.translate(x, y + tf.offsetY)
  // facing espelha em X; scaleX/scaleY do transform dao a vida (stretch/bob).
  ctx.scale(facing * tf.scaleX, tf.scaleY)
  ctx.rotate(tf.rotation)
  // Centrado no eixo X (-drawW/2) e ancorado nos pes (-drawH na vertical).
  ctx.drawImage(asset.src, -drawW / 2, -drawH, drawW, drawH)
  ctx.restore()

  // referencias de hitbox aceitas pela assinatura do contrato (nao escalam o sprite)
  void w
  void h
}
```

- [ ] Rodar e confirmar PASS:

```bash
npm run test -- tests/unit/spriteDraw.test.ts
# Saida esperada:
#  ✓ tests/unit/spriteDraw.test.ts (5)
#  Test Files  1 passed (1)
```

- [ ] `git add src/engine/spriteDraw.ts tests/unit/spriteDraw.test.ts && git commit -m "M2a Task5: drawAnimatedSprite (ancora nos pes, SPRITE_DRAW_H=90)"`

---

#### Passo 3 — Teste que falha: `ASSET_MANIFEST` + `SKY_LAYERS`

- [ ] Escrever `tests/unit/assetsManifest.test.ts`. Verifica as 3 chaves do manifesto, as URLs exatas, o `chromaKey: true` só no Artur, e que `SKY_LAYERS` tem 1 camada `{ key: 'bg.sky', factor: 0.3 }`.

```ts
// tests/unit/assetsManifest.test.ts
import { describe, it, expect } from 'vitest'
import { ASSET_MANIFEST, SKY_LAYERS } from '../../src/data/assets'

describe('ASSET_MANIFEST', () => {
  it('tem exatamente as 3 chaves do M2a', () => {
    expect(Object.keys(ASSET_MANIFEST).sort()).toEqual(
      ['bg.cosmic', 'bg.sky', 'char.artur'].sort(),
    )
  })

  it('aponta para as URLs servidas a partir de /assets', () => {
    expect(ASSET_MANIFEST['char.artur'].url).toBe('/assets/chars/artur.png')
    expect(ASSET_MANIFEST['bg.sky'].url).toBe('/assets/bg/sky.png')
    expect(ASSET_MANIFEST['bg.cosmic'].url).toBe('/assets/bg/cosmic.png')
  })

  it('chroma-key so no personagem (off-white -> transparente)', () => {
    expect(ASSET_MANIFEST['char.artur'].chromaKey).toBe(true)
    // backgrounds nao recortam (cobrem 960x528)
    expect(ASSET_MANIFEST['bg.sky'].chromaKey).toBeUndefined()
    expect(ASSET_MANIFEST['bg.cosmic'].chromaKey).toBeUndefined()
  })
})

describe('SKY_LAYERS', () => {
  it('M2a = mundo 1 com 1 camada de ceu (factor 0.3)', () => {
    expect(SKY_LAYERS).toEqual([{ key: 'bg.sky', factor: 0.3 }])
  })
})
```

- [ ] Rodar e confirmar que FALHA por módulo inexistente:

```bash
npm run test -- tests/unit/assetsManifest.test.ts
# Saida esperada:
#  FAIL  tests/unit/assetsManifest.test.ts
#  Error: Failed to load url ../../src/data/assets (resolved id: .../src/data/assets)
```

---

#### Passo 4 — Implementar `src/data/assets.ts`

- [ ] Criar o manifesto importando `AssetEntry` de `../engine/assets` (DONO: tarefa de assets) e `ParallaxLayer` de `../engine/parallax` (DONO: tarefa de parallax). NÃO redeclarar esses tipos aqui.

```ts
// src/data/assets.ts
import type { AssetEntry } from '../engine/assets'
import type { ParallaxLayer } from '../engine/parallax'

/**
 * Manifesto de assets do M2a. Vite serve public/ na raiz, entao
 * public/assets/x.png -> /assets/x.png (dev e build/preview).
 * Apenas o personagem usa chromaKey (off-white -> transparente);
 * os backgrounds cobrem 960x528 inteiros e nao recortam.
 */
export const ASSET_MANIFEST: Record<string, AssetEntry> = {
  'char.artur': { url: '/assets/chars/artur.png', chromaKey: true },
  'bg.sky': { url: '/assets/bg/sky.png' },
  'bg.cosmic': { url: '/assets/bg/cosmic.png' },
}

/**
 * Camadas de parallax do mundo 1 (ceu claro). 1 camada no M2a;
 * bg.cosmic fica no manifesto para uso futuro.
 */
export const SKY_LAYERS: ParallaxLayer[] = [{ key: 'bg.sky', factor: 0.3 }]
```

- [ ] Rodar e confirmar PASS:

```bash
npm run test -- tests/unit/assetsManifest.test.ts
# Saida esperada:
#  ✓ tests/unit/assetsManifest.test.ts (4)
#  Test Files  1 passed (1)
```

- [ ] `git add src/data/assets.ts tests/unit/assetsManifest.test.ts && git commit -m "M2a Task5: ASSET_MANIFEST + SKY_LAYERS"`

---

#### Passo 5 — Copiar a arte real para `public/assets`

- [ ] Confirmar que os 3 PNGs-fonte existem em `.m2-samples/` (existem: `new-artur-v1.png`, `new-bg.png`, `background.png`), criar as pastas e copiar com os nomes finais. `.m2-samples` é gitignored, mas `public/assets/**` deve entrar no commit (a arte real é a entrega).

```bash
# 1) confirmar fontes (devem listar os 3 arquivos)
ls -l .m2-samples/new-artur-v1.png .m2-samples/new-bg.png .m2-samples/background.png

# 2) criar pastas e copiar para os nomes finais
mkdir -p public/assets/chars public/assets/bg
cp .m2-samples/new-artur-v1.png public/assets/chars/artur.png
cp .m2-samples/new-bg.png       public/assets/bg/sky.png
cp .m2-samples/background.png   public/assets/bg/cosmic.png

# 3) confirmar destino (3 png presentes)
ls -l public/assets/chars/artur.png public/assets/bg/sky.png public/assets/bg/cosmic.png
# Saida esperada: as 3 linhas existem, cada uma com tamanho > 0.
```

> Nota Windows/PowerShell: se `cp`/`mkdir -p` não estiverem disponíveis no shell em uso, equivalentes:
> `New-Item -ItemType Directory -Force public/assets/chars, public/assets/bg` e
> `Copy-Item .m2-samples/new-artur-v1.png public/assets/chars/artur.png` (idem para `new-bg.png`→`sky.png`, `background.png`→`cosmic.png`).

- [ ] Garantir que `public/assets/**` não está sendo ignorado pelo `.gitignore` (só `.m2-samples` deve estar). Se necessário, conferir com `git check-ignore -v public/assets/chars/artur.png` (sem saída = não ignorado).
- [ ] `git add public/assets/chars/artur.png public/assets/bg/sky.png public/assets/bg/cosmic.png && git commit -m "M2a Task5: arte real (artur/sky/cosmic) em public/assets"`

---

#### Passo 6 — Gate da tarefa

- [ ] Rodar a suíte unitária completa (gate desta tarefa):

```bash
npm run test
# Saida esperada: todos os arquivos passam, incluindo
#  ✓ tests/unit/spriteDraw.test.ts
#  ✓ tests/unit/assetsManifest.test.ts
#  Test Files  N passed (N)
```

- [ ] Verificar que os 3 PNG existem em `public/assets` (gate de arte):

```bash
ls public/assets/chars/artur.png public/assets/bg/sky.png public/assets/bg/cosmic.png
# Saida esperada: as 3 linhas, sem "No such file".
```

- [ ] (Apenas se os módulos donos `animator.ts`/`assets.ts`/`parallax.ts` já estiverem no branch) confirmar tipos cruzados: `npx tsc -p tsconfig.json --noEmit` sem erros nos imports de Task 5. (O `tsc`/`build`/`e2e` do projeto inteiro é responsabilidade da Task 7.)
- [ ] Commit final (se restou algo não commitado): `git commit -am "M2a Task5: gate verde (spriteDraw + manifest + arte)"`.

---

**Resumo do contrato cumprido nesta tarefa**
- `drawAnimatedSprite(r, asset, an, x, y, w, h, facing, body)` — âncora nos pés, `SPRITE_DRAW_H=90`, largura proporcional ao aspecto do asset, ordem `save/translate/scale/rotate/drawImage/restore`, `facing` espelha em X, `asset===null` não desenha. **(O piscar de i-frames fica no `game.ts` — tarefa de integração — não aqui.)**
- `ASSET_MANIFEST` com as 3 chaves (`char.artur` com `chromaKey:true`; `bg.sky`/`bg.cosmic` sem) e `SKY_LAYERS = [{ key:'bg.sky', factor:0.3 }]`, importando `AssetEntry`/`ParallaxLayer` dos módulos donos.
- Arte real copiada: `public/assets/chars/artur.png` (← `new-artur-v1.png`), `public/assets/bg/sky.png` (← `new-bg.png`), `public/assets/bg/cosmic.png` (← `background.png`).

---

### Task 6: game.ts + main.ts: integracao da animacao (DONO)

Integra o motor de animacao M2a (assets, animator, parallax, particles, spriteDraw, manifest) no loop existente, **sem tocar em nenhuma logica M1** (abilities/humanware/enemies/dano/colisoes/timer/builder/camera/goal/overlays permanecem byte-a-byte iguais na ordem). Esta tarefa e DONA de `src/game/game.ts` e `src/main.ts` (arquivos M1 que ela MODIFICA). Ela apenas **consome via import** os modulos das Tasks 1–5; NUNCA os recria.

**Files:**
- **Modify** `src/game/game.ts` (M1 — esta tarefa e a dona da integracao): novo 4o arg `store?: AssetStore`; `playerAnim`/`ps`; chamadas de animator/particles no `update('playing')`; render com `drawParallax` + `drawAnimatedSprite` (fallback `drawPlaceholder`) + `drawParticles`.
- **Modify** `src/main.ts` (M0 — esta tarefa e a dona da integracao): `await loadAssets(ASSET_MANIFEST)` antes de `createGame`, passa `store` como 4o arg; pinta `COLOR_BG` enquanto carrega.
- **Test** `tests/unit/game.test.ts` (M1 — JA EXISTE; esta tarefa ACRESCENTA um `describe` no fim, nao reescreve os blocos M1).
- Consome (import, NAO recria): `src/engine/assets.ts` (Task 1: `loadAssets`, `AssetStore`), `src/engine/animator.ts` (Task 2: `createAnimator`, `updateAnimator`, `Animator`), `src/engine/parallax.ts` (Task 3: `drawParallax`), `src/engine/particles.ts` (Task 4: `createParticles`, `emitAmbient`, `emitBurst`, `updateParticles`, `drawParticles`, `ParticleSystem`), `src/engine/spriteDraw.ts` (Task 5: `drawAnimatedSprite`), `src/data/assets.ts` (Task manifest: `ASSET_MANIFEST`, `SKY_LAYERS`). Tambem mantem todos os imports M1 ja existentes (`./humanware ./ability ./enemy ../ui/selectScreen ../ui/hud` etc.).

> Nota TDD: o gate desta tarefa e `npm run test` (Vitest/jsdom). Em jsdom nao ha canvas real, entao o `store` de teste e um **mock** cujo `get()` devolve um `ImageAsset` falso (com `src` mockado) e o renderer e o mesmo fake `vi.fn()` do `game.test.ts` M1. A asercao "usou animacao" e feita espionando que `drawAnimatedSprite` foi chamado — fazemos isso via `vi.mock('../../src/engine/spriteDraw', ...)` para que o spy substitua a implementacao real (que precisaria de canvas). `drawParallax`/`drawParticles` tambem sao mockados para nao tocar canvas. Os blocos M1 existentes continuam **sem** store (4o arg `undefined`) e seguem verdes pelo fallback `drawPlaceholder`.

---

#### Passo 6.1 — Teste que FALHA: store opcional + animacao no render

- [ ] **Escrever o teste que falha.** Acrescentar ao FINAL de `tests/unit/game.test.ts` (apos o ultimo `describe`), reaproveitando `makeRenderer`/`FakeInput`/`makeLevel`/`selectFirst`/`TILE` ja definidos no arquivo. Adicionar no topo do arquivo, junto aos imports M1 existentes:

```ts
// (M2a Task 6) — imports adicionais no topo de tests/unit/game.test.ts
import type { AssetStore, ImageAsset } from '../../src/engine/assets'
import * as spriteDraw from '../../src/engine/spriteDraw'
import * as parallax from '../../src/engine/parallax'
import * as particles from '../../src/engine/particles'

// Mocka os modulos que tocam canvas real (jsdom nao tem 2d de verdade aqui).
// As fns viram spies; preservamos as fns puras de `particles` que o game usa no update.
vi.mock('../../src/engine/spriteDraw', () => ({
  drawAnimatedSprite: vi.fn(),
}))
vi.mock('../../src/engine/parallax', () => ({
  drawParallax: vi.fn(),
}))
vi.mock('../../src/engine/particles', async (importOriginal) => {
  const actual = await importOriginal<typeof import('../../src/engine/particles')>()
  return { ...actual, drawParticles: vi.fn() }
})
```

```ts
// (M2a Task 6) — novo bloco no FINAL de tests/unit/game.test.ts

// Mock de AssetStore: get() devolve um ImageAsset falso para 'char.artur'.
function makeStore(): AssetStore {
  const fakeAsset: ImageAsset = {
    src: {} as unknown as CanvasImageSource,
    w: 64,
    h: 96,
  }
  return {
    get: (key: string) => (key === 'char.artur' ? fakeAsset : null),
    ready: true,
  }
}

describe('createGame — integracao M2a (animacao)', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('SEM store: render usa drawPlaceholder (fallback), nunca drawAnimatedSprite', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const game = createGame(renderer, input, makeLevel()) // 3 args = sem store
    selectFirst(game, input)
    game.render(0)
    expect(spriteDraw.drawAnimatedSprite).not.toHaveBeenCalled()
  })

  it('COM store: render desenha o player via drawAnimatedSprite', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const game = createGame(renderer, input, makeLevel(), makeStore())
    selectFirst(game, input)
    game.render(0)
    expect(spriteDraw.drawAnimatedSprite).toHaveBeenCalledTimes(1)
    const call = (spriteDraw.drawAnimatedSprite as ReturnType<typeof vi.fn>).mock.calls[0]
    const p = game.player!
    // ancora nos pes: x = centro horizontal, y = base do corpo
    expect(call[2]).toBe(p.x + p.w / 2) // x
    expect(call[3]).toBe(p.y + p.h)     // y
    expect(call[6]).toBe(p.facing)      // facing
  })

  it('render('playing') desenha o parallax (SKY_LAYERS) em screen space', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const game = createGame(renderer, input, makeLevel(), makeStore())
    selectFirst(game, input)
    game.render(0)
    expect(parallax.drawParallax).toHaveBeenCalledTimes(1)
    // clear acontece antes do parallax (fundo coberto por cima do COLOR_BG)
    expect(renderer.clear).toHaveBeenCalled()
    expect(particles.drawParticles).toHaveBeenCalledTimes(1)
  })

  it('i-frames: piscar pula o desenho do sprite em frames alternados', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const game = createGame(renderer, input, makeLevel(), makeStore())
    selectFirst(game, input)
    const p = game.player!
    // iframes com bit (iframes>>2)&1 == 1 -> NAO desenha
    p.iframes = 4 // (4>>2)&1 = 1
    game.render(0)
    expect(spriteDraw.drawAnimatedSprite).not.toHaveBeenCalled()
    // iframes com bit 0 -> desenha
    vi.clearAllMocks()
    p.iframes = 8 // (8>>2)&1 = 0
    game.render(0)
    expect(spriteDraw.drawAnimatedSprite).toHaveBeenCalledTimes(1)
  })

  it('update('playing') NAO lanca com store (animator/particles integrados)', () => {
    const renderer = makeRenderer()
    const input = new FakeInput()
    const game = createGame(renderer, input, makeLevel(), makeStore())
    selectFirst(game, input)
    expect(() => game.update(1)).not.toThrow()
  })
})
```

- [ ] **Rodar e ver FALHAR.** `npm run test`
  - Saida esperada (falha de tipo/runtime): `createGame` so aceita 3 args, entao `createGame(renderer, input, makeLevel(), makeStore())` falha a compilacao TS (`Expected 3 arguments, but got 4`); e em runtime `expect(spriteDraw.drawAnimatedSprite).toHaveBeenCalledTimes(1)` falha com `0` chamadas porque `game.ts` ainda desenha `drawPlaceholder`. Algo como:
    `AssertionError: expected "drawAnimatedSprite" to be called 1 times, but got 0 times`.

---

#### Passo 6.2 — IMPLEMENTAR `src/game/game.ts` (integracao, preservando M1)

- [ ] **Editar `src/game/game.ts`.** Tres edicoes cirurgicas; o resto do arquivo M1 fica intacto.

**(a) Imports M2a** — acrescentar abaixo do bloco de imports M1 (logo apos o import de `../engine/constants`):

```ts
// --- M2a: motor de animacao (consumido, nao recriado) ---
import type { AssetStore } from '../engine/assets'
import { createAnimator, updateAnimator } from '../engine/animator'
import {
  createParticles,
  emitAmbient,
  emitBurst,
  updateParticles,
  drawParticles,
} from '../engine/particles'
import { drawParallax } from '../engine/parallax'
import { drawAnimatedSprite } from '../engine/spriteDraw'
import { ASSET_MANIFEST as _ASSET_MANIFEST_UNUSED } from '../data/assets'
import { SKY_LAYERS } from '../data/assets'
void _ASSET_MANIFEST_UNUSED // o manifest e usado por main.ts; SKY_LAYERS e usado aqui
```

> Nota: so `SKY_LAYERS` e usado dentro de `game.ts`. Se preferir evitar o import morto, importe APENAS `SKY_LAYERS`:
> ```ts
> import { SKY_LAYERS } from '../data/assets'
> ```
> (Use esta forma enxuta — remova as duas linhas `_ASSET_MANIFEST_UNUSED`. O manifest e responsabilidade de `main.ts`.)

**(b) Assinatura + estado** — alterar a assinatura de `createGame` para aceitar `store?` e instanciar `playerAnim`/`ps`:

```ts
export function createGame(
  renderer: Renderer,
  input: Input,
  level: ParsedLevel,
  store?: AssetStore,
): Game {
  const state = createStateMachine('select')
  const cam = createCamera()
  const chars = selectableChars()
  const sel: SelectState = createSelect()

  // --- M2a: animacao do player + sistema de particulas (persistem entre frames) ---
  const playerAnim = createAnimator()
  const ps = createParticles()
  // Rastreia a borda de ativacao do Humanware para emitir 1 burst no frame que ativa.
  let hwWasActive = false

  // Mutaveis: recriados no reset.
  let player: Player | null = null
  // ... (RESTO DO CORPO M1 INALTERADO) ...
```

**(c) `update('playing')`** — inserir as chamadas M2a **sem reordenar nada do M1**. O ponto de insercao e logo apos o passo M1 `2) Habilidade` (`updateAbility(...)`) e antes do passo `3) Builder`. Substituir o trecho:

```ts
      // 2) Habilidade: tempo do player (dt, escala 1).
      updateAbility(p, input, dt, { level, enemies })

      // 3) Bloco temporario do Builder -> level.tiles (sem 2o sistema de colisao).
      syncBuilderTile()
```

por:

```ts
      // 2) Habilidade: tempo do player (dt, escala 1).
      updateAbility(p, input, dt, { level, enemies })

      // 2b) M2a: animacao do player (logica pura, sem render) + particulas ambiente.
      updateAnimator(playerAnim, p, p.iframes, dt)
      emitAmbient(ps, VIEW_W, VIEW_H, dt)
      // Burst no frame em que o Humanware ACABOU de ativar (borda de subida).
      const hwActiveNow = isActive(hw)
      if (hwActiveNow && !hwWasActive) {
        emitBurst(ps, p.x, p.y - 30, 24, [COLOR_MAGENTA, COLOR_LIME])
      }
      hwWasActive = hwActiveNow
      updateParticles(ps, dt)

      // 3) Bloco temporario do Builder -> level.tiles (sem 2o sistema de colisao).
      syncBuilderTile()
```

> Importante: `COLOR_MAGENTA` e `COLOR_LIME` ja estao importados de `../engine/constants` no M1. `VIEW_W`/`VIEW_H` tambem. `isActive` ja esta importado de `./humanware`. Nada novo a importar para este trecho.
> A ordem M1 a partir do passo 3 (builder -> KeyH/updateHumanware -> enemies -> colisoes -> coin -> timer -> tickPlayerTimers -> camera -> goal -> `input.update()`) permanece **identica**. O calculo de `hwActiveNow` aqui le o estado; o gatilho `tryActivate(hw)`/`updateHumanware` continua no passo 4 do M1 logo abaixo — portanto na 1a vez o burst dispara no frame seguinte ao da ativacao, o que e o comportamento desejado (a particula acompanha o Modo ja ativo). Nao mover `tryActivate`.

**(d) `render(...)`** — o `render` M1 ja faz `renderer.clear(COLOR_BG)` no inicio. Inserir `drawParallax` logo apos o clear (cobre o fundo, em SCREEN SPACE, antes de `beginWorld`), trocar o desenho do player por `drawAnimatedSprite` com fallback + pisca de i-frames, e adicionar `drawParticles` apos `endWorld()`.

Trocar o inicio do `render`:

```ts
  function render(_alpha: number): void {
    renderer.clear(COLOR_BG)

    if (state.is('select')) {
      drawSelect(renderer, sel, chars)
      void _alpha
      return
    }

    renderer.beginWorld(cam.x, cam.y)
```

por:

```ts
  function render(_alpha: number): void {
    renderer.clear(COLOR_BG)

    if (state.is('select')) {
      drawSelect(renderer, sel, chars)
      void _alpha
      return
    }

    // M2a: parallax do ceu em SCREEN SPACE, por cima do COLOR_BG, antes do mundo.
    // store pode ser undefined (testes/loading) — drawParallax pula layers sem asset.
    if (store) drawParallax(renderer, SKY_LAYERS, store, cam)

    renderer.beginWorld(cam.x, cam.y)
```

Trocar o bloco do player M1:

```ts
    // FX da habilidade + player.
    if (player) {
      drawAbilityFx(renderer, player)
      drawPlaceholder(
        renderer,
        player.char,
        player.x,
        player.y,
        player.w,
        player.h,
        player.facing,
      )
    }

    renderer.endWorld()
```

por:

```ts
    // FX da habilidade + player.
    if (player) {
      drawAbilityFx(renderer, player)

      // M2a: pisca de i-frames — pula o desenho do sprite em frames alternados.
      const blink = player.iframes > 0 && ((player.iframes >> 2) & 1) === 1
      if (!blink) {
        const artur = store ? store.get('char.artur') : null
        if (artur) {
          // Ancora nos pes: centro horizontal + base do corpo.
          drawAnimatedSprite(
            renderer,
            artur,
            playerAnim,
            player.x + player.w / 2,
            player.y + player.h,
            player.w,
            player.h,
            player.facing,
            player,
          )
        } else {
          // Fallback M1: placeholder (sem store ou asset ausente).
          drawPlaceholder(
            renderer,
            player.char,
            player.x,
            player.y,
            player.w,
            player.h,
            player.facing,
          )
        }
      }
    }

    renderer.endWorld()

    // M2a: particulas em SCREEN SPACE, por cima do mundo, antes do HUD.
    drawParticles(renderer, ps)
```

> Tudo abaixo de `renderer.endWorld()` no M1 (HUD via `drawHud(...)` e overlays win/over) permanece inalterado — apenas inserimos `drawParticles(renderer, ps)` entre `endWorld()` e o `drawHud(...)`.
> Quando `blink === true` o sprite NAO e desenhado (nem placeholder nem animado) — e o comportamento de "pisca" do contrato.
> Quando `store` e fornecido mas `get('char.artur')` retorna `null` (asset faltando), cai no `drawPlaceholder` (fallback robusto, sem quebrar).

- [ ] **Reset:** nenhuma mudanca em `resetToSelect`. `playerAnim` e `ps` sao persistentes e seguros de reaproveitar entre rodadas (o animator e reclassificado a cada frame por `updateAnimator`; as particulas decaem por `life`). O `hwWasActive` reseta naturalmente: em `select`/`win`/`over` o ramo `playing` nao roda, mas como `isActive(hw)` fica `false` ao voltar para `playing` (novo `createHumanware()` no reset), `hwWasActive` volta a `false` no 1o frame de `playing`. (Opcional defensivo: zerar `hwWasActive = false` dentro de `resetToSelect()`.)

---

#### Passo 6.3 — IMPLEMENTAR `src/main.ts` (preload + passa store)

- [ ] **Editar `src/main.ts`** para precarregar os assets antes de criar o jogo. Substituir o arquivo inteiro por:

```ts
// src/main.ts — M0/M2a boot: precarrega assets e liga os modulos no jogo.
import { createRenderer } from './engine/render'
import { createInput } from './engine/input'
import { createLoop } from './engine/loop'
import { parseLevel } from './game/levelParser'
import { world1Zona1 } from './data/levels/world1-zona1'
import { createGame } from './game/game'
import { loadAssets } from './engine/assets'
import { ASSET_MANIFEST } from './data/assets'
import { COLOR_BG } from './engine/constants'

// 1. Canvas
const canvas = document.getElementById('game') as HTMLCanvasElement | null
if (!canvas) {
  throw new Error('canvas#game não encontrado')
}

// 2. Renderer (sets canvas.width/height internally)
const renderer = createRenderer(canvas)

// 3. Input — attach to window so keyboard events are captured everywhere
const input = createInput()
input.attach(window)

// 4. Parse the level data
const level = parseLevel(world1Zona1)

// 5. Pinta o fundo enquanto os assets carregam (evita flash branco).
renderer.clear(COLOR_BG)

// 6. Precarrega arte (chroma-key no load), depois cria e inicia o jogo.
//    O preload e async; ate la, o canvas mostra COLOR_BG.
async function boot(): Promise<void> {
  const store = await loadAssets(ASSET_MANIFEST)

  // Cria o jogo (maquina de estados comeca em 'select' — definido por createGame).
  const game = createGame(renderer, input, level, store)

  // Expose game state for E2E probe
  ;(window as any).__GAME_STATE = () => game.state.get()

  // Cria e inicia o loop de timestep fixo.
  const loop = createLoop(game.update.bind(game), game.render.bind(game))
  loop.start()
}

void boot()
```

> Notas: `loadAssets` (Task 1) carrega tudo em paralelo e resolve com um `AssetStore`. `createGame(..., store)` agora recebe o 4o arg. `COLOR_BG` pinta o canvas durante o carregamento. `__GAME_STATE` continua exposto (E2E depende). O e2e (Task 7) valida o boot real no preview.

---

#### Passo 6.4 — Rodar e ver PASSAR

- [ ] **Rodar.** `npm run test`
  - Esperado: TODOS os testes verdes — os blocos M1 de `game.test.ts` (sem store, fallback `drawPlaceholder`) seguem passando, e o novo bloco `createGame — integracao M2a (animacao)` passa: sem store nao chama `drawAnimatedSprite`; com store mock chama 1x com ancora-nos-pes e `facing`; `drawParallax`/`drawParticles` chamados 1x; pisca de i-frames pula o sprite; `update('playing')` com store nao lanca.
  - Saida esperada (resumo Vitest): `Test Files  ...passed`, `Tests  ...passed`, zero falhas.

- [ ] **Sanidade de tipos local (informativa, nao e o gate da tarefa).** O gate desta tarefa e `npm run test`. O `tsc -p tsconfig.json --noEmit` e o `build`/`e2e` do projeto inteiro sao responsabilidade da **Task 7** (verify), pois dependem de TODOS os modulos M2a (Tasks 1–5) ja existirem. Se rodar `npx tsc -p tsconfig.json --noEmit` isoladamente antes das Tasks 1–5 estarem mergeadas, havera erros de "Cannot find module '../engine/assets'" etc. — isso e esperado e nao reprova a Task 6.

- [ ] **Commit.** `git add src/game/game.ts src/main.ts tests/unit/game.test.ts && git commit -m "M2a Task 6: integra animator/particles/parallax/sprite no game + preload de assets no main (fallback placeholder sem store)"`

---

#### Resumo do contrato cumprido

- `createGame(renderer, input, level, store?: AssetStore)` — 4o arg opcional; testes M1 (3 args) seguem verdes pelo fallback `drawPlaceholder`.
- `update('playing')`: apos `updatePlayer`/`updateAbility` -> `updateAnimator(playerAnim, player, player.iframes, dt)`; `emitAmbient(ps, VIEW_W, VIEW_H, dt)`; `emitBurst(ps, player.x, player.y-30, 24, [COLOR_MAGENTA, COLOR_LIME])` na borda de ativacao do Humanware; `updateParticles(ps, dt)`. Toda a ordem M1 preservada.
- `render`: `clear(COLOR_BG)` -> `drawParallax(SKY_LAYERS)` (screen space) -> `beginWorld` -> tiles/goal/coins/enemies/`drawAbilityFx` -> player via `drawAnimatedSprite(store.get('char.artur'), playerAnim, player.x+player.w/2, player.y+player.h, player.w, player.h, player.facing, player)` (ancora nos pes) com fallback `drawPlaceholder` quando `store`/asset ausentes -> pisca de i-frames `(player.iframes>>2)&1` pula o sprite -> `endWorld` -> `drawParticles` -> `drawHud` -> overlays.
- `main.ts`: `await loadAssets(ASSET_MANIFEST)` antes de `createGame`, pinta `COLOR_BG` no preload, passa `store`. `__GAME_STATE` mantido.
- NAO recria humanware/ability/enemy/assets/animator/parallax/particles/spriteDraw/manifest — apenas importa. Gate: `npm run test`.

---

### Task 7: E2E + verificacao final M2a (DONO)

Voce e o DONO da verificacao final e do e2e. Garanta que o jogo carrega os assets reais via preload e roda fim-a-fim no preview (build em `dist/`, servido em `:4173`), e que a suite inteira (unit + tsc + build + e2e) esta verde. NAO recrie arquivos de outras tarefas (assets.ts, animator.ts, parallax.ts, particles.ts, spriteDraw.ts, src/data/assets.ts, game.ts, main.ts) — esta tarefa so MODIFICA o e2e, ADICIONA um e2e novo de assets, e roda/documenta. Em e2e ha Chromium real (preview), entao NAO precisamos de mock de canvas aqui (mocks de ctx/Image sao das tarefas unit). Gate desta tarefa = a SUITE COMPLETA do projeto.

**Files:**
- Modify: `tests/e2e/world1.spec.ts` (M1 — DONO da Task 7 pode ajustar o e2e; manter o fluxo `select -> Enter -> andar -> win`, so endurecer contra o preload assincrono de assets)
- Create: `tests/e2e/assets.spec.ts` (NOVO — DONO desta tarefa: prova que os assets reais sao servidos no preview e que o jogo nao trava no preload)
- Modify: `docs/superpowers/plans/2026-06-08-gzero-m2a.md` (documentacao da verificacao final M2a; criar a secao "Verificacao final" se o arquivo ja existir do plano, senao criar o arquivo minimo)
- Consumir (so leitura/execucao, NAO editar): `src/main.ts`, `src/game/game.ts`, `src/data/assets.ts`, `public/assets/chars/artur.png`, `public/assets/bg/sky.png`, `public/assets/bg/cosmic.png`
- Test (rodar, nao criar): `tests/unit/**/*.test.ts`

Pre-condicao (de outras tarefas, ja entregues quando a Task 7 roda): a tarefa dona do manifest ja copiou a arte real para `public/assets/...`; a tarefa de integracao ja deixou `main.ts` com `await loadAssets(ASSET_MANIFEST)` antes de `createGame(renderer, input, level, store)` e mantem `window.__GAME_STATE` exposto. A Task 7 NAO reescreve isso — apenas verifica. Se a verificacao falhar por algo dessas tarefas, a Task 7 reporta o erro exato (nao conserta arquivos de outro dono).

---

#### Passo 1 — Teste e2e novo que FALHA: assets reais servidos + sem travar no preload

Escrever `tests/e2e/assets.spec.ts`. Ele falha hoje porque (a) `public/assets/...` ainda nao existe / nao e servido no `:4173` antes das tarefas anteriores, e (b) prova que apos o preload o jogo chega em `select` (preload nao trava).

[codigo] `tests/e2e/assets.spec.ts` (COMPLETO):

```ts
// tests/e2e/assets.spec.ts
// E2E M2a — prova que os assets REAIS sao servidos pelo preview (public/ -> dist/)
// e que o preload assincrono (await loadAssets em main.ts) NAO trava o boot:
// o jogo ainda alcanca o estado 'select'.
//
// Contexto M2a (CONTRATO):
//  - Vite serve public/ na raiz: public/assets/x.png -> /assets/x.png (dev e preview).
//  - ASSET_MANIFEST (src/data/assets.ts): 'char.artur' -> /assets/chars/artur.png
//    (chromaKey), 'bg.sky' -> /assets/bg/sky.png, 'bg.cosmic' -> /assets/bg/cosmic.png.
//  - main.ts faz `await loadAssets(ASSET_MANIFEST)` ANTES de createGame; enquanto
//    carrega pode pintar COLOR_BG. window.__GAME_STATE() continua exposto.
//  - Este teste roda no preview (:4173), onde os arquivos vivem em dist/assets/...
import { test, expect } from '@playwright/test'

// As 3 URLs de asset declaradas no ASSET_MANIFEST (M2a). Servidas a partir de public/.
const ASSET_URLS = [
  '/assets/chars/artur.png',
  '/assets/bg/sky.png',
  '/assets/bg/cosmic.png',
]

test('M2a: assets reais sao servidos pelo preview (public/ -> dist/)', async ({
  request,
}) => {
  for (const url of ASSET_URLS) {
    const res = await request.get(url)
    expect(res.status(), `${url} deve responder 200`).toBe(200)
    const ct = res.headers()['content-type'] ?? ''
    expect(ct, `${url} deve ter content-type de imagem`).toContain('image')
    const body = await res.body()
    // PNG real comeca com a assinatura 0x89 0x50 0x4E 0x47 ("\x89PNG").
    expect(
      body.length,
      `${url} nao deve ser vazio (arte real, nao placeholder 0-byte)`,
    ).toBeGreaterThan(1000)
    expect(body[0], `${url} deve ter assinatura PNG`).toBe(0x89)
    expect(body[1]).toBe(0x50)
    expect(body[2]).toBe(0x4e)
    expect(body[3]).toBe(0x47)
  }
})

test('M2a: preload assincrono nao trava o boot — jogo chega em select', async ({
  page,
}) => {
  const consoleErrors: string[] = []
  page.on('console', (msg) => {
    if (msg.type() === 'error') consoleErrors.push(msg.text())
  })
  page.on('pageerror', (err) => consoleErrors.push(String(err)))

  await page.goto('/')

  const canvas = page.locator('canvas#game')
  await canvas.waitFor({ state: 'visible' })

  // O probe deve aparecer mesmo com o preload em andamento.
  await page.waitForFunction(
    () => typeof (window as any).__GAME_STATE === 'function',
    { timeout: 10000 },
  )

  // Apos o preload (await loadAssets) o createGame inicia em 'select'.
  // Damos folga (assets reais sao grandes); se travasse, estouraria o timeout.
  await page.waitForFunction(
    () =>
      typeof (window as any).__GAME_STATE === 'function' &&
      (window as any).__GAME_STATE() === 'select',
    { timeout: 15000 },
  )

  // Nenhum erro de runtime/console durante o boot+preload.
  expect(consoleErrors, 'sem erros de console no boot/preload').toEqual([])
})
```

[rodar] `npx playwright test tests/e2e/assets.spec.ts --project=chromium`

[saida esperada (FAIL antes das tarefas anteriores / antes dos assets em public/)]:
```
  1) M2a: assets reais sao servidos pelo preview (public/ -> dist/)
     Error: expect(received).toBe(expected)
     Expected: 200
     Received: 404
       at .../tests/e2e/assets.spec.ts
  2 failed
```
(Se `main.ts` ainda nao fizer preload assincrono, o 2o teste pode passar trivialmente; e o 1o que ancora o FAIL inicial. Apos as tarefas anteriores entregues, ambos passam.)

---

#### Passo 2 — Endurecer `tests/e2e/world1.spec.ts` para o preload assincrono (manter fluxo M1)

O contrato M2a torna `main.ts` assincrono (`await loadAssets`). O fluxo `select -> Enter -> andar -> win` continua valendo, mas a espera pelo probe `__GAME_STATE` precisa de timeout maior (assets reais sao grandes) e o teste deve esperar explicitamente o estado `select` ANTES de pressionar Enter (ja faz isso — so aumentamos os timeouts de 5000 -> 15000 nessas duas esperas iniciais, porque agora ha preload). NAO mudar a logica de pulos/andar/win.

[implementar] Editar SOMENTE os dois `waitForFunction` iniciais em `tests/e2e/world1.spec.ts`:

Trocar o bloco "Garante que o probe de estado esta exposto e o loop rodando":
```ts
  // Garante que o probe de estado esta exposto e o loop rodando
  await page.waitForFunction(
    () => typeof (window as any).__GAME_STATE === 'function',
    { timeout: 5000 },
  )
```
por (timeout maior — preload assincrono de assets reais em M2a):
```ts
  // Garante que o probe de estado esta exposto e o loop rodando.
  // M2a: main.ts faz `await loadAssets(...)` antes de createGame, entao o probe
  // pode demorar um pouco mais a aparecer (assets reais sao grandes). Folga ampla.
  await page.waitForFunction(
    () => typeof (window as any).__GAME_STATE === 'function',
    { timeout: 15000 },
  )
```

E trocar o bloco "1) Deve comecar na tela de selecao":
```ts
  // 1) Deve comecar na tela de selecao
  await page.waitForFunction(
    () =>
      typeof (window as any).__GAME_STATE === 'function' &&
      (window as any).__GAME_STATE() === 'select',
    { timeout: 5000 },
  )
```
por:
```ts
  // 1) Deve comecar na tela de selecao (apos o preload de assets concluir).
  await page.waitForFunction(
    () =>
      typeof (window as any).__GAME_STATE === 'function' &&
      (window as any).__GAME_STATE() === 'select',
    { timeout: 15000 },
  )
```

(Os passos 2/3/4 do teste M1 — pressionar Enter, segurar ArrowRight, pulos defensivos, assercao `win` — ficam INTACTOS. Nao mexer no `deadline` de 30s nem na logica do loop.)

[rodar] `npx playwright test --project=chromium`

[saida esperada (PASS, com os assets reais ja em public/ e main.ts integrado)]:
```
Running 3 tests using 1 worker
  ✓  tests/e2e/assets.spec.ts:... M2a: assets reais sao servidos pelo preview ...
  ✓  tests/e2e/assets.spec.ts:... M2a: preload assincrono nao trava o boot ...
  ✓  tests/e2e/world1.spec.ts:... M1: select -> escolher personagem -> andar ate vencer
  3 passed (Xs)
```

PASS.

---

#### Passo 3 — Suite unit completa (Vitest)

Confirma que TODA a logica M1 + os novos modulos M2a (assets/animator/parallax/particles/spriteDraw, testados com mocks de ctx/Image nas tarefas donas) passam em jsdom.

[rodar] `npm run test`

[saida esperada (PASS)]:
```
> vitest run
 ✓ tests/unit/constants.test.ts (...)
 ✓ tests/unit/player.test.ts (...)
 ✓ tests/unit/game.test.ts (...)
 ... (todos os arquivos tests/unit/**/*.test.ts, incluindo os novos de M2a)
 Test Files  NN passed (NN)
      Tests  MMM passed (MMM)
```

PASS. (Se algum unit falhar, e bug da tarefa dona daquele modulo — reportar o arquivo/erro exato; a Task 7 nao edita o codigo de outro dono.)

---

#### Passo 4 — Typecheck estrito (tsc) e build (Vite)

`tsc --noEmit` valida que `createGame(renderer, input, level, store?)` e as INTERFACES INTERNAS M2a casam (sem `any` solto, `noUnusedLocals`/`noUnusedParameters` ligados). O `build` confirma que `public/assets/**` vai pro `dist/` (Vite copia `public/` para a raiz de `dist/`).

[rodar] `npx tsc -p tsconfig.json --noEmit`

[saida esperada]: (sem saida = sucesso; exit 0)

[rodar] `npm run build`

[saida esperada (PASS)]:
```
> vite build
vite vX.X.X building for production...
✓ NN modules transformed.
dist/index.html                  X.XX kB
dist/assets/index-XXXXXXXX.js    XX.XX kB │ gzip: XX.XX kB
✓ built in XXXms
```

[verificar que os assets foram copiados para dist/] (estes existem APOS o build, porque o build limpa `dist/` com `emptyOutDir`):
- `dist/assets/chars/artur.png`
- `dist/assets/bg/sky.png`
- `dist/assets/bg/cosmic.png`

[rodar] `node -e "for (const p of ['dist/assets/chars/artur.png','dist/assets/bg/sky.png','dist/assets/bg/cosmic.png']) { const s=require('fs').statSync(p); if(s.size<1000) throw new Error('asset vazio: '+p); console.log('OK', p, s.size, 'bytes'); }"`

[saida esperada]:
```
OK dist/assets/chars/artur.png NNNNNNN bytes
OK dist/assets/bg/sky.png NNNNNNN bytes
OK dist/assets/bg/cosmic.png NNNNNNN bytes
```

PASS. (Nota: o JS hash do bundle de imagem mostrado pelo Vite e o do `src/`; as imagens em `public/` NAO passam pelo pipeline de hash — sao copiadas verbatim para `dist/assets/...` mantendo o nome/URL, que e exatamente o que o `ASSET_MANIFEST` espera.)

---

#### Passo 5 — E2E completo no preview real (Playwright)

O `playwright.config.ts` ja roda `npm run build && npm run preview` (porta 4173) via `webServer`. Garantir o navegador instalado e rodar os 3 specs juntos.

[instalar chromium se preciso] `npx playwright install --with-deps chromium` (no Windows, `--with-deps` e ignorado; use `npx playwright install chromium`)

[rodar] `npm run test:e2e`

[saida esperada (PASS)]:
```
Running 3 tests using 1 worker
  ✓  tests/e2e/assets.spec.ts  M2a: assets reais sao servidos pelo preview (public/ -> dist/)
  ✓  tests/e2e/assets.spec.ts  M2a: preload assincrono nao trava o boot — jogo chega em select
  ✓  tests/e2e/world1.spec.ts  World 1 Zona 1 (M1): select -> escolher personagem -> andar ate vencer
  3 passed (Xs)
```

PASS.

---

#### Passo 6 — Documentar a verificacao final M2a

Registrar o resultado verde da suite no plano M2a. Se `docs/superpowers/plans/2026-06-08-gzero-m2a.md` ja existir (criado pela tarefa que escreveu o plano), ADICIONAR a secao abaixo ao final; senao criar o arquivo com este conteudo.

[implementar] Append/Create em `docs/superpowers/plans/2026-06-08-gzero-m2a.md`:

```markdown
## Verificacao final M2a (Task 7)

Arte real animada para o Artur (abordagem D: animacao procedural sobre arte-base de IA),
com background parallax (ceu claro do mundo 1) e particulas. Outros personagens/inimigos
seguem placeholder (fallback via drawPlaceholder).

### Gate da suite completa (tudo verde)
- `npm run test` — Vitest (jsdom): logica M1 intacta + modulos novos M2a
  (assets/animator/parallax/particles/spriteDraw) com mocks de ctx/Image.
- `npx tsc -p tsconfig.json --noEmit` — typecheck estrito (strict, noUnusedLocals/Params)
  com `createGame(renderer, input, level, store?)` e as interfaces internas M2a.
- `npm run build` — Vite copia `public/assets/**` verbatim para `dist/assets/**`
  (URLs do ASSET_MANIFEST preservadas; sem hashing das imagens de public/).
- `npm run test:e2e` — Playwright no preview (:4173, build+preview):
  - `tests/e2e/assets.spec.ts`: os 3 PNGs reais respondem 200 com assinatura PNG e
    tamanho > 1KB; o preload assincrono (`await loadAssets` em main.ts) NAO trava o
    boot — o jogo alcanca `select` sem erro de console.
  - `tests/e2e/world1.spec.ts`: fluxo M1 preservado (select -> Enter -> andar -> win),
    com timeouts iniciais ampliados para acomodar o preload de assets reais.

### Assets reais (servidos por public/ -> dist/)
- `/assets/chars/artur.png` (chromaKey: off-white -> transparente no load)
- `/assets/bg/sky.png` (camada de parallax do mundo 1, factor 0.3)
- `/assets/bg/cosmic.png` (no manifest para uso futuro; nao usado no mundo 1)

### Fallback / robustez
- `store` e opcional em `createGame` (4o arg): testes unit passam sem store
  (fallback drawPlaceholder); o jogo real preload via main.ts antes de createGame.
- I-frame piscar preservado no draw do sprite; toda a ordem M1 de update intacta.

Status: VERDE (unit + tsc + build + e2e).
```

[rodar] `npm run test && npx tsc -p tsconfig.json --noEmit && npm run build && npm run test:e2e`

[saida esperada]: as 4 etapas concluem com exit 0; e2e reporta `3 passed`.

PASS — suite inteira verde.

---

#### Passo 7 — Commit

[rodar]:
```
git add tests/e2e/world1.spec.ts tests/e2e/assets.spec.ts docs/superpowers/plans/2026-06-08-gzero-m2a.md
git commit -m "test(m2a): e2e de assets reais + preload nao-travante; verificacao final verde"
```

(Os arquivos de arte em `public/assets/**` sao commitados pela tarefa dona do manifest, nao por esta. `.m2-samples/` permanece gitignored.)

---

### Notas de execucao / armadilhas

- **Sem mock de canvas no e2e**: este e2e roda em Chromium real (preview), entao canvas/Image funcionam de verdade — os mocks de `ctx`/`Image` (spies) sao exclusivos dos testes unit (jsdom) das outras tarefas; nao introduzir mocks aqui.
- **public/ pode nao existir hoje**: a Task 7 NAO cria `public/assets/**` (e da tarefa dona do manifest). Se `npm run build`/e2e falharem com 404 nos assets, e sinal de que a tarefa dona ainda nao copiou a arte — reportar, nao consertar aqui.
- **emptyOutDir**: `vite.config.ts` tem `emptyOutDir: true`, entao checar `dist/assets/**` somente DEPOIS do build (o `dist/` antigo e apagado no build).
- **reuseExistingServer**: localmente o `webServer` reaproveita um preview ja rodando; se uma porta 4173 antiga estiver presa, encerre-a antes do `test:e2e` para nao testar build velho.
- **Escopo**: a Task 7 so toca `tests/e2e/world1.spec.ts`, cria `tests/e2e/assets.spec.ts` e documenta. Qualquer falha em `src/**` e bug da tarefa dona daquele arquivo — reportar arquivo+erro exato, nao editar.

---

## Questoes em Aberto
- A tarefa de integração (game.ts/main.ts) chama drawAnimatedSprite passando player.x+player.w/2 (centro) e player.y+player.h (pé) — esta API assume esses valores como x/y. Confirmar que o dono de game.ts segue exatamente isso, senão a âncora fica deslocada.
- drawAnimatedSprite recebe w/h por assinatura do contrato mas não os usa para escalar (a altura é fixa em SPRITE_DRAW_H=90). Mantive a assinatura exata; se o contrato pretendia usar w/h, reavaliar — mas o texto diz explicitamente para usar SPRITE_DRAW_H.
- Não há tipo Body exportado em animator.ts para o parâmetro body; usei o shape inline { vx, vy, onGround } igual ao classifyAnim do contrato. Se animator.ts exportar um tipo nomeado para esse body, vale importá-lo para consistência.
- Os modulos M2a consumidos (src/engine/assets.ts, animator.ts, parallax.ts, particles.ts, spriteDraw.ts, src/data/assets.ts) ainda nao existem no disco — sao das Tasks 1–5. O codigo de integracao usa as assinaturas CANONICAS do contrato; se alguma Task divergir do contrato, ajustar os imports/chamadas correspondentes.
- O teste de pisca usa o predicado exato do contrato `(player.iframes>>2)&1`: com iframes=4 -> nao desenha, iframes=8 -> desenha. Confirmar que a Task de verificacao (Task 7) usa o MESMO predicado para o e2e visual.